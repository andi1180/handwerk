# Valooro Handwerk

Eigenständiges Repo mit **eigenem Supabase-Projekt** (getrennt vom Hotel-Projekt). Migrationen starten frisch ab `0001` und werden **manuell** über das Supabase-Dashboard (SQL Editor) angewendet — nie lokal ausführen, nie `supabase db reset`.

Technische Doku: siehe [TECH.md](TECH.md).

## Build-History

- **Schritt 1 ✅ DB-Fundament (Migration 0001)** — 7 Tabellen, RLS, explizite GRANTs, Mandanten-Isolation, i18n-Felder, Verify-Gate.
- **Schritt 2a ✅ App-Scaffold + Supabase-Clients + i18n + CSS-Tokens** — Next.js 15 (App Router, React 19, TS strict), 3 Supabase-Clients (Browser/Server via `@supabase/ssr`, `service_role` via `@supabase/supabase-js`), typsicherer i18n-Layer (`de`), Valooro-Token-System in `globals.css`. Noch ohne Auth/Features.
- **Schritt 2b ✅ Auth + geteilter Login + geschützte Portal-Shell** — `@supabase/ssr` auf `0.12.x`, Middleware-Session-Refresh + Schutz von `/portal/*`, Login-Seite (Client, `signInWithPassword`), `getCurrentBusiness()` über authentifizierten Server-Client (RLS-erzwungen, kein `service_role`), Portal-Shell mit Sidebar + Logout, Test-Seed. Noch ohne Module/Features.
- **Schritt 3 ✅ Auftrag anlegen + Auftragsliste (manueller Pfad)** — Sidebar-Nav (Client, Aktiv-Zustand via `usePathname`) um „Aufträge" erweitert; Auftragsliste (Server Component, AUTHENTICATED Client, RLS + defensiver `business_id`-Filter, `created_at DESC`); Status-Badge-Komponente; Anlage-Formular (Client, `div + onClick`, kein `<form>`); Route Handler `POST /api/portal/orders` mit `business_id` aus Session (Isolationsregel), `status='draft'`. Mobile-first. Kein Capture/Media, keine Generierung.
- **Schritt 4a ✅ Storage-Fundament (0002) + Auftrags-Detailseite + Medien-Liste** — Bucket `order-media` (privat) + tenant-skopierte `storage.objects`-Policies (erstes Pfad-Segment = `business_id`), Verify-Gate `0002`; klickbare Auftragsliste; Detailseite (Server Component, mobile-first) mit stickyem Kopf (Kundenname + Status-Badge), Stammdaten und Medien-Liste (RLS, `sort_order` ASC, Thumbnails via server-seitiger Signed-URLs aus privatem Bucket); Query-Helper `getOrderById`/`getOrderMedia`. Reine Anzeige — kein Capture/Upload (4b), keine Generierung.
- **Schritt 4b ✅ Foto-Capture + Upload (isolations-sicher, zweistufig)** — Client-Kompression (`lib/media/compress.ts`, Canvas → JPEG, max 1500px/Q 0.8); Capture-Komponente (Client, `div + onClick`, native Kamera via verstecktem `<input capture="environment">`, Vorschau + Stichwort + Tag, optimistische In-Memory-Queue + `router.refresh()`, Storage-Upload mit 2 Retries). Zweistufig: Datei direkt in den privaten Bucket unter RLS-skopiertem Pfad `{business_id}/{order_id}/{uuid}.jpg`, Metadaten via Route Handler `POST /api/portal/orders/[id]/media` (`business_id` aus Session, Pfad-Validierung, `sort_order` fortlaufend). Detailseite bleibt Server Component. Nur Foto — Video folgt in 4c, keine Generierung.
- **Schritt 4c ✅ Video-Capture + Längen-Check** — denselben Flow wie 4b erweitert (Komponente + Route Handler, nicht dupliziert): zweiter Button „Video aufnehmen" (`<input accept="video/*" capture="environment">`); Dauer-Lesung (`lib/media/video.ts`, verstecktes `<video>` + `loadedmetadata`), `MAX_VIDEO_SECONDS = 30` — zu lang ⇒ Ablehnen (i18n `capture.videoTooLong`), kein Upload, kein Trim (MVP). Video wird **nicht** clientseitig komprimiert: Datei unverändert in den Bucket unter `{business_id}/{order_id}/{uuid}.{ext}`, Hintergrund-Queue/Retries/optimistische Liste wiederverwendet. Route Handler: `media_type ∈ {'photo','video'}`, `duration_seconds` bei Video erforderlich. Detailseite spielt Video-Items inline ab (`<video controls>`, Signed-URL). Keine neue Migration (`duration_seconds` aus 0001). Keine Generierung.
- **Schritt 4d ✅ Portal mobil-optimiert + i18n-Interpolation** — `/portal` bleibt **eine App**; das Chrome schaltet rein per Media Query (Breakpoint `max-width: 768px`). Desktop unverändert (220px-Sidebar); Mobile: sticky Top-Bar (gekürzter Betriebsname + kompakter Logout-Icon) + app-artige, fixierte Bottom-Tab-Nav (Dashboard/Aufträge, aktiv = `--gold`, `usePathname`; `PortalTabNav` teilt sich die `ITEMS` mit der Sidebar). Capture mobil-first: sticky Detail-Kopf klinkt unter die Top-Bar (`--portal-topbar-h`), große Aufnahme-Buttons (`min-height: 64px`), bildschirmfüllender Vorschau-/Stichwort-/Tag-Dialog (`.capture-draft` → `position: fixed`), vollbreiter „Neuer Auftrag", größere `.form-input` (16px → kein iOS-Zoom). `MAX_VIDEO_SECONDS` 30 → **20** (Settings-Ceiling 30). `t()` bekommt typsichere Platzhalter-Interpolation (`t(locale, key, params?)`, Overloads — ohne `params` bleibt das Leaf-Literal); `.replace()`-Stellen umgestellt. Keine neuen Features, keine Migration.

- **Schritt 5a ✅ Basis-Settings (Branding, Limits, Links, Delivery)** — erste Slice des Settings-Moduls, **keine neue Migration** (bestehende Spalten): `businesses.branding` jsonb (`primary_color`, `secondary_color`, `font`, `logo_per_page`), `businesses.settings` jsonb (`video_max_seconds`, `ig_handle`, `google_review_url`, `website_url`, `delivery_mode`), `retention_months` + `name` (Spalten). Geteilte Optionen/Grenzen/Guards in `lib/settings/options.ts`; `getCurrentBusiness` um typsicheres `branding`/`settings`/`retention_months` erweitert (jsonb → Defaults normalisiert/geclamped, kein `any`). Settings-Seite (Server Component) + Client-Form (`div + onClick`, kein `<form>`; Felder in `.card`-Gruppen: Color-Swatch+Hex, Selects, Toggle `role="switch"`, Range+Number; Client-Validierung = Server-Validierung). Route Handler `PATCH /api/portal/settings` (AUTHENTICATED, **`business_id` aus Session**, Validierung Name/Hex/Font/Delivery/Ranges → sonst 400, Update über `businesses_update`, gibt aktualisierte Werte zurück). Drittes Nav-Item „Einstellungen" (geteilte `ITEMS` → Sidebar + Mobile-Tab, Zahnrad-Icon). Video-Limit verdrahtet: Detailseite liest `settings.video_max_seconds` → Prop `maxVideoSeconds` an `capture.tsx` (Konstante nur noch Fallback). i18n `nav.settings` + `settings.*`. KEIN Logo-Upload (5b), KEIN Intro/Outro (5c), KEIN Slot-Editor (5d).

- **Schritt 6a ✅ Medien Reorder + Löschen (mobil)** — erste Slice des **mobilen Booklet-Assemblers** (ersetzt den ursprünglich geplanten Desktop-Sequenz-Builder), **keine neue Migration** (`sort_order` aus 0001, Storage-Delete-Policy aus 0002). Deps `@dnd-kit/core`/`/sortable`/`/utilities` via pnpm — **keine** React-19-Peer-Konflikte, dnd-kit wie geplant verwendet (kein Eigenbau/Fallback). Medien-Liste als Client-Komponente (`app/portal/orders/[id]/media-list.tsx`): Server-Page lädt `order_media` (RLS, `sort_order` ASC) + Signed-URLs (3600 s) → Props an `<MediaList>` (ersetzt die server-gerenderte Liste; `<Capture>` + `router.refresh()` bleiben darüber). Einheitliches **Raster quadratischer Kacheln** (`aspect-ratio 1/1`, mobil 3 Spalten), Fotos + Videos gleich groß; Video-Poster via `<video src="…#t=0.1" preload="metadata">` + Play-Overlay. **Reorder** mit dnd-kit Sortable: `TouchSensor` (Long-Press, `delay ≈ 220ms` → Tippen/Scrollen frei) + `MouseSensor` (`distance: 8`); Drop = optimistisches `arrayMove` + `PATCH`, Fehler → Rollback + Hinweis. **Tap = ansehen/abspielen** (Vollbild-Viewer, Foto groß bzw. `<video controls>`; `draggedRef` unterdrückt den Klick nach echtem Drag). **Löschen** pro Kachel (Ecken-Button, `stopPropagation`) → `window.confirm` → optimistisch + `DELETE` → `router.refresh()`, Fehler → Rollback. Route Handler: `PATCH …/media/reorder` (AUTHENTICATED, `business_id`/Order aus Session, Body `{ids}` muss exakt der Medien-Menge entsprechen → sonst 400, `sort_order = 1..n`) und `DELETE …/media/[mediaId]` (Order+Media gegen Session/`order_id` validiert, Storage `remove` + Zeilen-`delete`, RLS + Pfad-Präfix-Policy greifen). i18n `assembler.*`. KEINE Captions (6b), KEIN Finalize (6c).

- **Schritt 6b ✅ KI-Captions (Haiku 4.5)** — zweite Slice des mobilen Assemblers, **erste Anthropic-Integration**, **keine neue Migration** (`order_media.caption` aus 0001). SDK `@anthropic-ai/sdk` via pnpm; server-seitiger Client (`lib/ai/anthropic.ts`, lazily/gecacht, Key **nur** aus `ANTHROPIC_API_KEY` — nie `NEXT_PUBLIC`/Client), Modell `claude-haiku-4-5-20251001` (Haiku 4.5; **kein** effort/Thinking, schlichter `messages.create`). Caption-Generator (`lib/ai/captions.ts`): `generateCaption({mediaType, keyword, imageBase64?})` → kurzer DE-Caption (memorybook-Stil, System-Prompt „max ~8 Wörter, Fragment, keine Anführungszeichen/Emojis/Punkt"); **FOTO** = Bild (base64) + Stichwort als Vision-Input, **VIDEO** = nur Stichwort (Frame-Extraktion folgt mit dem Reel), Video ohne Stichwort → leerer Caption. `CAPTION_MAX_LENGTH` (120) in SDK-freier `lib/ai/caption-limits.ts` → client-importierbar **ohne** SDK im Client-Bundle; Storage-Helfer `lib/ai/media-caption.ts` lädt das Bild server-seitig (RLS, `storage.download` → base64). Route Handler (alle AUTHENTICATED, Order/Media gegen Session+`order_id`): `POST …/captions` (Batch über alle `caption IS NULL` — manuelle Edits unberührt, begrenzte Nebenläufigkeit max 3, gibt `{updated:{id,caption}[]}`), `POST …/media/[mediaId]/caption/regenerate` (einzeln, überschreibt), `PATCH …/media/[mediaId]/caption` (manuelles Edit, Body `{caption}`, Längen-Limit 120 → sonst 400, **keine KI**); Guard `isAiConfigured()` ⇒ 500. UI (`media-list.tsx`): Button „Captions generieren" (`.btn-dark`, deaktiviert wenn nichts fehlt) → optimistisch + `router.refresh()`; Caption-Bearbeitung im **Vollbild-Viewer** (Medium oben, Panel unten: `textarea.form-input` + Neu-generieren-Icon + Speichern; `viewing` aus `items` abgeleitet → Live-Update); dezenter Kachel-Indikator hat-Caption/fehlt. `OrderMedia`/`getOrderMedia` um `caption` erweitert. i18n `captions.*`. KEIN Finalize (6c).

- **Schritt 6b.1 ✅ Caption-Länge + Datei-Upload** — zwei kleine Anpassungen, **keine neuen Konzepte, keine Migration**. (1) **Caption-Länge** `CAPTION_MAX_LENGTH` 120 → **180** (zentral in `lib/ai/caption-limits.ts`; PATCH-Server-Limit + Client-`maxLength` referenzieren die Konstante, kein hartkodierter Wert); System-Prompt von „max ~8 Wörter" auf **„~12–15 Wörter, knappes ansprechendes Fragment"** umgestellt, `max_tokens` 64 → 128. (2) **Datei-Upload zusätzlich zur Live-Aufnahme** (`capture.tsx`): neben „Foto/Video aufnehmen" (Kamera, `capture="environment"`) jetzt „Foto/Video hochladen" über `<input>` **ohne** `capture` (Galerie/Dateiauswahl). Reine Quell-Erweiterung — dieselben Handler (`handlePhotoFile`/`handleVideoFile`) und die **gesamte nachgelagerte Pipeline** (compressImage / getVideoDuration + Längen-Check / Hintergrund-Upload + Queue/Retry + Metadaten-POST) werden wiederverwendet, **keine** Logik dupliziert. Buttons in zwei Zeilen gruppiert (Aufnehmen / Hochladen, `.capture-row`, große Tap-Targets). i18n `capture.uploadPhoto`/`capture.uploadVideo`.

- **Schritt 6c ✅ Finalize + Lifecycle-Badges (mobiler Assembler abgeschlossen)** — letzte Slice des mobilen Assemblers, **keine neue Migration** (Status-Spalte + Check-Constraint aus 0001), **keine Generierung** (Schritt 8; der Generierungs-Hook hängt sich später an den Finalize-Übergang). **Status-Übergänge:** Route Handler `POST …/finalize` (`draft → finalized`) und `POST …/reopen` (`finalized → draft`), beide ohne Body, AUTHENTICATED Client, Betrieb/Order gegen Session (401/403/404). Finalize-**Guards**: aktueller Status muss `draft` sein (Reopen: `finalized`) → sonst **409**, `UPDATE` defensiv auf den Ausgangsstatus gefiltert (keine Doppel-Übergänge); **≥ 1 `order_media`** (head-`count`) → sonst **400 `need_media`**. **Editier- vs. Abgeschlossen-Modus** (`page.tsx` leitet `isDraft`/`isFinalized` aus dem Status ab): `draft` = voller Editier-Modus (Capture sichtbar, Reorder/Löschen/Captions aktiv) + prominenter Button **„Booklet abschließen"** am Seitenende (`<FinalizeButton>`, `btn-gold`, mobil groß; Bestätigungsdialog `finalize.confirm`+`confirmText` → `POST finalize` → `router.refresh()`; ohne Medium direkt `needMedia`-Hinweis, Server prüft zusätzlich). `finalized` = Capture **ausgeblendet**, `<MediaList readOnly>` unterdrückt alle Mutations-Controls (`useSortable({ disabled })` statt Drag, keine Lösch-Buttons, kein Batch-Captions-Kopf, Caption im Viewer read-only via `<CaptionReadOnly>`), Ansehen/Abspielen bleibt; oben Banner **„Booklet abgeschlossen"** (`<FinalizeBanner>`, gold) mit **„Wieder bearbeiten"** → `POST reopen`. `<FinalizeButton>`/`<FinalizeBanner>` (`finalize-controls.tsx`) teilen sich `postAction(orderId, "finalize"|"reopen")`, keine Logik dupliziert. **Lifecycle-Badges** (`order-status-badge.tsx`) gruppieren nach Stufe statt pro Status: neutral = `draft` (in Arbeit), Gold (`--gold-light`/`--gold-border`) = `finalized`+`generated` (fertig), grünlich (neue Tokens `--green-light`/`--green-border`/`--green-text`) = `sent`+`viewed`+`shared` (gesendet); Labels unverändert. i18n `finalize.*`.

- **Schritt 6b.2 ✅ Kachel entschlackt (Tags raus, UI-only) + Caption-Auswahl** — zwei UI-Anpassungen am mobilen Assembler, **keine neuen Konzepte, keine Migration**. (1) **Tags raus (nur UI):** Tag-Auswahl (Vorher/Nachher/Prozess) aus dem Aufnahme-/Upload-Dialog (`capture.tsx`) und aus Kachel/Viewer (`media-list.tsx`) entfernt; Stichwort bleibt. Metadaten-`POST` schickt kein `tag` mehr, Insert in `media/route.ts` setzt `order_media.tag` **nicht mehr** (bleibt `null`). **`order_media.tag`-Spalte bleibt** (keine Migration; `MediaTag`-Typ + `getOrderMedia`-Select bleiben) — Feld **vorhanden, aber ungenutzt**, Reaktivierung für späteres Vorher/Nachher-Format ohne DB-Änderung möglich. (2) **Caption-Auswahl mit zusammengelegtem Indikator:** pro Kachel **ein** Indikator oben links (`.media-tile-indicator`) mit drei Zuständen — ohne Caption+nicht ausgewählt = leerer Kreis (Outline), ohne Caption+ausgewählt = gefüllter Gold-Kreis (Häkchen), mit Caption = Caption-Icon (Status, kein Auswahl-Verhalten); Trash gegenüber (oben rechts) → nur 2 Symbole. Tap auf den Indikator (nur ohne Caption) toggelt die Auswahl (`selectedIds: Set`, `stopPropagation` → kein Drag/Viewer); Tap-Body bleibt Ansehen/Abspielen, Long-Press bleibt Drag. **Smarter „Captions generieren":** mit Auswahl nur diese (uncaptioned), sonst alle ohne Caption; Label `captions.generateSelected`. Nach Erfolg Auswahl zurückgesetzt, Kacheln zeigen via `router.refresh()` das Caption-Icon. `readOnly` (finalized): keine Auswahl/kein Trash, Caption-Icon als Status. **Batch-Endpoint** (`captions/route.ts`) um optionales `{ ids?: string[] }` erweitert (`.in("id", ids)` zusätzlich zu `caption IS NULL`, gegen `order_id`/Betrieb validiert; leer/fehlend ⇒ alle). i18n: `captions.select`/`selected`/`generateSelected` neu, `mediaTag.*`/`capture.tag`/`capture.tagOptional` entfernt.

- **Schritt 6b.3 ✅ Caption-Auswahl explizit + Upload-Härtung** — zwei Robustheits-/UX-Fixes am mobilen Assembler, **keine neuen Konzepte, keine Migration**. (1) **Caption-Auswahl explizit** (`media-list.tsx`): „Captions generieren" ist **deaktiviert (grau)**, solange keine Kachel ausgewählt ist (`disableGenerate = generating || selectedCount === 0`); das 6b.2-„leer ⇒ alle"-Verhalten **entfällt im UI**, `handleGenerate` sendet **immer** konkrete `ids` (leer ⇒ kein Request). Neuer **„Alle auswählen"**-Toggle neben dem Generieren-Button (markiert alle Medien ohne Caption → mit Auswahl **„Auswahl aufheben"**), sodass „alle captionen" = **zwei Taps** bleibt; versteckt, wenn nichts fehlt. **Batch-Endpoint unverändert** (sein „leer ⇒ alle"-Fallback bleibt als Sicherheitsnetz, wird vom UI nicht mehr getriggert). (2) **Upload-Härtung** (`capture.tsx`): `runUpload` in **drei getrennte Schritte** zerlegt (Aufbereitung/Komprimierung → **Storage-Upload** → **Metadaten-POST**), jeder mit präzisem `console.error` (welcher Schritt + echte Fehlermeldung + `order_id`/`storage_path`); Listen-Hinweis konkreter (`capture.uploadError`). **Retry auf beiden Wegen:** neben `uploadWithRetry` jetzt `postMetadataWithRetry` (bis zu 2 Retries mit Backoff, geteilte Konstanten; transient = Netzwerk + **5xx**, **4xx** ⇒ sofort `PermanentError`, kein Retry). **Orphan-Cleanup:** Storage-Upload erfolgreich, Metadaten-POST endgültig fehlgeschlagen ⇒ `storage.remove([path])` (kein verwaistes File, „Erneut" startet sauber). Server (`media/route.ts`): `console.error` mit Kontext (`order_id`, Schritt, `invalid_path` mit erwartetem/erhaltenem Pfad, `insert_failed` mit Supabase-`error`) → in Vercel-Logs sichtbar. i18n `captions.selectAll`/`deselectAll`/`capture.uploadError` neu.

- **Schritt 7a ✅ Logo-Upload (Branding-Bucket, Migration 0003)** — erste Slice der **Desktop-Business-Config** (Schritt 7), **erste neue Migration seit 0002**. **Bucket `branding`** (privat, `0003`) + tenant-skopierte `storage.objects`-Policies (erstes Pfad-Segment = `business_id`, nur `authenticated`, Muster exakt wie 0002; **4 Policies** inkl. `_update`, weil Upload mit `upsert=true` überschreibt; kein `anon`/PUBLIC, `service_role` umgeht RLS). **Privat + Signed-URL statt public-read** (als Kommentar in der Migration begründet): Architektur-Konsistenz mit `order-media` — ein Zugriffsmodell, eine Isolations-Grenze; das öffentliche Booklet `/b/[token]` signiert server-seitig (`service_role`) bei jedem Request frisch, Ablauf irrelevant. **Keine Schema-Änderung** — `logo_url` lebt im bestehenden `businesses.branding`-jsonb. Verify-Gate `0003_branding_checks.sql` (Bucket privat / 4 `authenticated`-Policies / keine `anon`-Policy). Client `lib/media/logo.ts` (NEU, **nicht** `compressImage`: `prepareLogo` exportiert **PNG** → Alpha bleibt; nur png/jpeg/webp ≤ 5 MB, max 512 px, typisierter `LogoPrepareError`). **Zweistufiger Upload** (isolations-sicher wie 4b): PNG direkt in `branding` unter fixem Pfad `{business_id}/logo.png` (`upsert`), dann `POST /api/portal/settings/logo` mit `{ storage_path }` **ohne `business_id`**. Route Handler (AUTHENTICATED, `business_id` aus Session): `POST` validiert Pfad + **READ-MERGE-WRITE** (nur `logo_url` setzen), `DELETE` `storage.remove` + `logo_url=null`. **KRITISCH:** Settings-`PATCH` (5a) auf READ-MERGE-WRITE umgestellt — schreibt `logo_url` **nicht mehr** weg, mergt nur seine vier Felder; symmetrisch fassen die Logo-Endpoints nur `logo_url` an (geteilter `asRecord`-Guard in `lib/settings/options.ts`, keine Duplikate). `BusinessBranding` um `logo_url: string | null` (typsicher). Settings-UI: neue `.card`-Gruppe „Logo" (`<LogoField>`, Vorschau via server-signierter URL als Prop / optimistische `objectURL`, „Entfernen", verstecktes `<input>`, `div + onClick`, kein `<form>`); `logo_per_page`-Toggle unverändert. i18n `settings.logo.*`. KEIN Intro/Outro (7b), KEINE Backgrounds (7c).

- **Schritt 7b ✅ Intro/Outro-Text (Booklet-Inhalt, Pro-Betrieb)** — zweite Slice der **Desktop-Business-Config**, bewusst schmal: nur die Text-/Kontaktfelder, die der Renderer in Step 8 sicher braucht. **KEINE Migration** (Keys im bestehenden `businesses.settings`-jsonb, wie 5a), **KEIN** neuer Bucket, **KEIN** neuer Endpoint — der bestehende Settings-`PATCH` (read-merge-write aus 7a) wurde nur erweitert. **KEINE** Layout-/Template-/Hintergrund-Konfiguration (das ist 7c bzw. Step 8). **4 neue settings-Keys** (alle `string | null`, Default `null`, leer ⇒ getrimmt `null`): `intro_tagline` (fester Claim **unter** dem KI-Titel auf der Intro-Seite, ≤ 80; **kein** fester Intro-Titel — der ist KI), `outro_message` (Dankes-/Abschiedszeile, ≤ 300), `contact_email` (**ÖFFENTLICHE** Kontakt-Mail fürs Outro — **nicht** `business_email`/Login, E-Mail-Format wenn gesetzt), `contact_phone` (öffentliche Telefonnummer, Freitext ≤ 40). `website_url`/`ig_handle`/`google_review_url` aus 5a werden im Outro wiederverwendet, **nicht** neu angelegt. Geteilte Guards in `lib/settings/options.ts` (`CONTENT_LIMITS`, `EMAIL_REGEX`/`isEmailFormat`); `getCurrentBusiness`/`BusinessSettings` + `normalizeSettings` um die 4 Keys (`asTrimmedOrNull`, kein `any`). Route Handler `PATCH` erweitert: `business_id` weiter **nur** aus Session, 4 Keys validiert (Länge ⇒ `content_too_long`, E-Mail ⇒ `invalid_email`, sonst 400), **READ-MERGE-WRITE jetzt auch auf `settings`** (vorher voll überschrieben) — symmetrisch zum `branding`-Merge, `logo_url`-Schutz aus 7a unangetastet. UI: neue `.card`-Gruppe „Booklet-Inhalt" (`intro_tagline` TextField, `outro_message` neues `TextAreaField`, `contact_email`/`contact_phone`), `div + onClick`, **kein `<form>`**, gespeichert über den bestehenden Seiten-Save (kein separater Button); Client-Validierung = Server-Validierung. i18n `settings.content.*`. KEINE Backgrounds (7c).

- **Schritt 7c ✅ Intro/Outro-Hintergrundbilder (Pro-Betrieb)** — dritte Slice der **Desktop-Business-Config**, **migrationsfrei**: der bestehende private `branding`-Bucket (0003) wird wiederverwendet (Policies binden `bucket_id='branding'` + erstes Pfad-Segment an die `business_id`, Dateiname egal → `intro-bg`/`outro-bg` abgedeckt; **kein** neuer Bucket, **kein** Policy-Diff). **Bewusst schmal:** je **ein** Bild für Intro und Outro — **KEIN** Per-Item-/Slot-Editor (renderer-gekoppelt → **deferred bis nach Step 8**), **KEINE** Aspect-/Crop-Logik (der Renderer covert/croppt in Step 8 auf 9:16/Portrait). Client `lib/media/background.ts` (NEU, **nicht** `prepareLogo`: `prepareBackground` exportiert **JPEG q0.85** statt PNG, max **1920 px** statt 512, Input-Cap **10 MB**; nur png/jpeg/webp, typisierter `BackgroundPrepareError`). **Datenmodell:** zwei neue Keys im bestehenden `businesses.branding`-jsonb (`intro_bg_url`/`outro_bg_url`, `string | null`, Default `null`), speichern — wie `logo_url` — den **Storage-Pfad** (Signed-URL erst beim Render/Vorschau); `BusinessBranding`/`normalizeBranding` + `DEFAULT_BRANDING` ergänzt (typsicher, kein `any`). **Zweistufiger Upload** (isolations-sicher wie 7a): JPEG direkt in `branding` unter fixem Pfad `{business_id}/{slot}-bg.jpg` (`upsert`), dann `POST /api/portal/settings/background` mit `{ storage_path, slot }` **ohne `business_id`**. Geteilter `backgroundStoragePath(businessId, slot)` + Guard `isBackgroundSlot` (`lib/settings/options.ts`) → eine Quelle für Client-Upload **und** Server-Validierung. Route Handler (NEU, AUTHENTICATED, `business_id` aus Session): `POST` validiert `slot ∈ {intro,outro}` + Pfad + **READ-MERGE-WRITE** (nur den Slot-Key setzen, `logo_url`/Farben/anderer Slot unangetastet), `DELETE` (`slot` aus Body) `storage.remove` + Slot-Key auf `null`. **DRY:** der branding-READ-MERGE-WRITE liegt jetzt geteilt in `lib/settings/branding-store.ts` (`mergeBranding`, server-only, `import type` → keine Bundle-Leakage) — genutzt von **beiden** Branding-Endpoints; der **Logo-Handler delegiert** an `mergeBranding` (Verhalten unverändert). UI: neue `.card`-Gruppe „Hintergründe" mit **zwei** `<ImageUploadField>` (Intro/Outro) — aus dem 7a-`LogoField` **generalisiert**, aber **einmal** definiert und zweifach verwendet (keine Intro/Outro-Duplikation); **7a-Logo-Feld unverändert**; Vorschau via server-signierter URL als Prop / optimistische `objectURL`, „Entfernen", `div + onClick`, kein `<form>`. Page signiert Logo+Intro+Outro über einen gemeinsamen `sign()`-Helfer (`Promise.all`). i18n `settings.background.*`. **Schritt 7 abgeschlossen.** *(Tweak: Hintergrund-Vorschau auf **9:16/cover** umgestellt — `ImageUploadField` bekam optionale Props `previewAspect`/`previewFit` (Defaults = bisheriges Querformat, **Logo-Feld unberührt**), die Background-Felder setzen `9/16` + `"cover"` (Portrait, ~160px breit, Hinweis `settings.background.previewHint`); spiegelt den Step-8-Render-Crop. Reine Darstellung — keine Datei-/Crop-/Route-Änderung.)*

- **Schritt 8a-1 ✅ Booklet-Generierung (Web-Story-Daten, OHNE Render)** — erste Slice von **Schritt 8**, **keine neue Migration** (`booklets` aus 0001). Erzeugt aus einem abgeschlossenen Auftrag die `booklets`-Row: **KI-Intro (Sonnet 4.6)**, unerratbarer `access_token`, Status `generated`. **KEIN** `/b/[token]`-Render (8a-2), **KEIN** Reel (8b), **KEINE** Share-/Review-/Delivery-UI (Step 9). **Sonnet-Integration (server-only, reuse 6b):** `SONNET_MODEL = "claude-sonnet-4-6"` neben `HAIKU_MODEL` in `lib/ai/anthropic.ts`; `getAnthropic()`/`isAiConfigured()` wiederverwendet (gleicher gecachter Client, Key nur `ANTHROPIC_API_KEY`). `lib/ai/intro.ts` (NEU): `generateIntro({ itemDescription, captions, language })` → `{ title, description }`; System-Prompt DE („erster Eindruck, hochwertig": Titel ≤ ~6 Wörter + 1–2 Sätze Transformation aus `item_description` + Captions, kein Marketing-Sprech/Emojis); Sonnet liefert **nur JSON**, **defensives Parsen** (Fence-Strip + Eingrenzung auf erstes `{…}` + `try/catch`) → `IntroParseError` ⇒ Route **502**; `max_tokens 300`; **sprach-parametrisiert** über `language` (§15, `LANGUAGE_NAMES`-Map, Default = Code). **Token:** `lib/booklet/token.ts` (NEU) `generateAccessToken()` = `randomBytes(24).toString("base64url")` (≥ 24 Byte, server-only). **Route Handler** `POST /api/portal/orders/[id]/generate` (NEU): AUTHENTICATED zuerst (401/403), Order via RLS (404); Status muss `finalized` **oder** `generated` (Re-Generate erlaubt, solange NICHT versendet) ⇒ sonst **409**; defensiv ≥ 1 `order_media` ⇒ **400 `need_media`**; `isAiConfigured()` ⇒ **500 `ai_not_configured`**; Captions als Intro-Kontext (AUTHENTICATED). **ISOLATION (§14.2):** booklets-Insert/Update über **`service_role`** (0001-RLS), `business_id` **aus der geladenen Order** (RLS-validierter Session-Betrieb), **NIE aus dem Body**, jeder service_role-Zugriff strikt auf diese `business_id` gescoped. **UPSERT by `order_id`:** existierendes Booklet laden → `update` (Intro + `web_story_ready=false`, **Token behalten**) bzw. `insert` (neuer Token); `reel_url`/`review_draft`/`ig_caption`/`image_urls`/`expires_at` bleiben `null`. Order-Status → `generated` (AUTHENTICATED, defensiv `.eq("status", aktueller Status)`). Response `{ ok, token }`; Fehler `console.error` mit `order_id`+Schritt (wie 6b.3). **Reopen erweitert:** `…/reopen` akzeptiert jetzt auch `generated → draft` (Versand-Stufen weiter gesperrt; Booklet+Token bleiben). **Portal-UI** (`page.tsx` + `generate-controls.tsx` NEU): `finalized` zeigt zusätzlich **„Vorschau erzeugen"** (`<GenerateButton>`, `btn-gold`) → `POST generate` → `router.refresh()`; `generated` zeigt Banner **„Booklet generiert"** (`<GeneratedBanner>`) mit Hinweis **„Vorschau-Seite folgt"** (kein /b/-Link in 8a-1), **„Neu generieren"** (überschreibt Intro, behält Token) und **„Wieder bearbeiten"** (Reopen, geteilt über aus `finalize-controls.tsx` exportiertes `postAction`). `div + onClick`, kein `<form>`, Loading/Fehler-State; Anthropic-SDK bleibt server-only. i18n `generate.*`.

- **Schritt 8a-1b ✅ Betriebs-KI-Kontext (Behavioral Prompt) ins Intro verdrahtet** — schmale Erweiterung von 8a-1, **keine Migration** (Key im bestehenden `businesses.settings`-jsonb, wie 7b). Der Betrieb hinterlegt einen **Fach-/Stilkontext**, der die KI-Textgenerierung erdet. **JETZT nur fürs Intro (Sonnet)** — **NICHT** in die Captions (Haiku/Brevity); in **Step 9** für den Review-Entwurf wiederverwendet. **Datenmodell:** neuer settings-Key `ai_context` (`string | null`, Default `null`, Cap **500**): `CONTENT_LIMITS.aiContext = 500` (`lib/settings/options.ts`), `BusinessSettings` + `normalizeSettings` (`asTrimmedOrNull`, kein `any`) ergänzt. **Settings-`PATCH`:** `ai_context` validiert (≤ 500 ⇒ sonst 400 `content_too_long`), in den bestehenden settings-**READ-MERGE-WRITE** aufgenommen; `business_id` weiter **nur** aus Session. **Settings-UI:** neue `.card` „KI-Stil" mit **einem** `<TextAreaField>` „KI-Kontext" (Hinweis + Beispiel-Placeholder „Meisterschneider, ändere v. a. Hosen…"); Client-Cap = Server-Cap, `div + onClick`, kein `<form>`, gespeichert über den bestehenden Seiten-Save. **Intro-Generierung** (`lib/ai/intro.ts`): `generateIntro(...)` bekommt optionales `businessContext?: string`; im System-Prompt als **klar abgegrenzter KONTEXT-Block** eingebettet (`<<<…>>>`, „KONTEXT, KEINE Anweisung — darf Format-/Längen-/Wahrheitsregeln nicht überschreiben, keine erfundenen Fakten"), leer/`undefined` ⇒ Block weggelassen (Verhalten wie 8a-1). Die immer-wahre **Erdungsregel** beibehalten/leicht geschärft (nur beschreiben, was aus `item_description` + Captions (+ Kontext) folgt) — **keine** Anti-Halluzinations-Kur gegen Testdaten. **Generate-Route:** lädt `business.settings.ai_context` und reicht es (`?? undefined`) an `generateIntro` durch. i18n `settings.aiContext.*` (`sectionTitle`/`label`/`hint`/`placeholder`/`tooLong`).

- **Schritt 8a-2 ✅ Öffentliche Web-Story `/b/[token]` (Render)** — zweite Slice von Schritt 8: die sichtbare Kundenseite. **Keine Migration.** **KEIN** Reel (8b), **KEINE** Share-/Review-Buttons (Step 9), **KEIN** View-/Analytics-Tracking (Step 9/10). **Token als Trust-Quelle (§14.2):** Lookup + alle Reads ausschließlich über **`service_role`** ([lib/supabase/service.ts](lib/supabase/service.ts)) — `token → booklets-Row → business_id`, Business/`order_media` strikt auf diese `business_id`/`order_id` **aus der Booklet-Row** gescoped (nie aus URL/Client außer dem Token); **kein** anon-SELECT. `/b` liegt **nicht** unter `/portal` ⇒ Middleware-Guard greift nicht (öffentlich erreichbar, kein Matcher-Eingriff). **Caption-Helper** (`lib/booklet/caption.ts`, NEU — **erster Konsument** der Schritt-8/9-Vorgabe): `displayCaption({caption, keyword}) = caption ?? keyword`, beide leer ⇒ `null` = **kein** Overlay (server-only, Step 9 reuse). **Lader** (`lib/booklet/load.ts`, NEU, server-only): `loadPublicBooklet(token)` → diskriminiert `not_found`/`expired`/`ok`; signiert pro Request frisch (Medien aus `order-media`, `logo_url`/`intro_bg_url`/`outro_bg_url` aus `branding`, Batch `createSignedUrls`); normalisiert über die jetzt **exportierten** `normalizeBranding`/`normalizeSettings` (`lib/auth/current-business.ts`, eine Quelle). **Seite** (`app/b/[token]/page.tsx`, Server Component, `force-dynamic`, mobile-first): Vollbild-Scroll-Story (`scroll-snap-type: y mandatory`, je Sektion **`100dvh`** nicht `100vh`, `scroll-snap-stop: always`). **Intro** (BG cover/Fallback-Verlauf, Logo, KI-`intro_title`, `intro_description`, `intro_tagline`, Scrim, animierter Scroll-Hint `prefers-reduced-motion`-fest) → **Medien** (eine pro `order_media` in `sort_order`; Foto full-bleed statisch; Video `<video controls playsInline preload=metadata>` + Poster `#t=0.1`, **kein** Auto-Play; **Caption-Overlay** unten in der Safe-Zone ~16 % abgehoben + Gradient-Scrim, `null` ⇒ kein Overlay; Logo pro Seite nur bei `logo_per_page`) → **Outro** (BG, Logo, Betriebsname, `outro_message`, Kontakt-Pills `contact_email`/`contact_phone`/`website_url`; **keine** Share-/Review-Buttons). Branding: `branding.font` via `lib/booklet/fonts.ts` (`next/font`, alle vier FONT_OPTIONS selbst-gehostet, `preload:false`), `primary/secondary` als CSS-Variablen; Styles `app/b/[token]/booklet.css` (global, `booklet-`-präfixiert, nur auf der Route). Desktop: zentrierte 9:16-Portrait-Spalte vor dunklem Backdrop. **`web_story_ready`:** Generate-Route setzt es jetzt bei Insert **und** Update auf **`true`** (Renderer existiert ⇒ renderbar, Voraussetzung fürs Senden in Step 9); der Render bleibt **rein lesend**. **Portal-Vorschau-Link:** `page.tsx` lädt im Status `generated` die `access_token`-Row (AUTHENTICATED, RLS), `<GeneratedBanner>` zeigt **„Vorschau öffnen"** (`/b/[token]`, `target=_blank`, Primär-Aktion) statt des 8a-1-Platzhalters. i18n: Block `booklet.*` neu (`scrollHint`/`contactEmail`/`contactPhone`/`contactWebsite`/`expiredTitle`/`expiredText`), `generate.previewSoon` → `generate.openPreview`.

- **Schritt 8b-0 ⚠️ FFmpeg-Infra-Spike — VERWORFEN (Binary-Bundling sprengt den Vercel-Deploy; ersetzt durch 8b-0v2)** — reiner **Machbarkeits-Spike** vor der Reel-Pipeline: beweist, dass gebündeltes ffmpeg in der Function läuft und der Output abspielbar im Storage landet. **KEINE** echte Reel-Pipeline, **KEIN** Intro/Outro/Caption, **KEINE** Migration, **KEINE** Schema-Änderung; **provisorisch** (wird in 8b-1 durch das echte „Reel erstellen" ersetzt). **Dependency:** `ffmpeg-static` (pnpm); Binary-Pfad via Default-Import (`string | null`, Paket liefert eigene Typen → keine ambient-Deklaration), gegen `null` geguardet. **pnpm-10-Stolperstein:** Lifecycle-Scripts von Deps sind standardmäßig blockiert → `ffmpeg-static` in `pnpm.onlyBuiltDependencies` (package.json), sonst lädt der `postinstall` das Binary weder lokal noch auf Vercel. **KRITISCH (Binary ins Bundle):** Next tracet das Binary nicht automatisch (Laufzeit-Pfad, kein `require`) → `outputFileTracingIncludes` (Next-15-Top-Level-Key) in `next.config.ts` für die Render-Route auf `./node_modules/ffmpeg-static/ffmpeg` (Literal löst durch den pnpm-Symlink auf); **Glob-Falle:** Route-Schlüssel sind Globs, das dynamische `[id]` mit `*` abgedeckt (`/api/portal/orders/*/render-reel-test`), bracket-Variante als harmloser Fallback; eng gescoped (Build-Trace `.nft.json` enthält das Binary genau in dieser Route, nicht in den anderen Order-Functions — verifiziert). **Route** `POST …/render-reel-test` (NEU): `runtime="nodejs"` + `maxDuration=300` (Fluid Compute); Auth wie Generate (401/403, Order via RLS 404); ffmpeg → `/tmp` triviales **1080×1920**-mp4 (~2 s, `lavfi color`, h264, `yuv420p`, `+faststart`, **kein** Audio), Prozess-Timeout 120 s, defensives `chmod 0o755` (non-fatal). Output → `order-media` per **`service_role`** unter `{business_id}/{order_id}/reel-test.mp4` (`business_id` **aus der Order**, nie Body; erstes Segment deckt 0002-Policy ab), `video/mp4`, `upsert`; Antwort `createSignedUrl(3600)` → `{ ok, url }`. **Alle** Fehlerpfade `console.error` (order_id + Schritt + Message) + JSON-Code (`ffmpeg_unavailable`/`ffmpeg_failed`/`upload_failed`/`sign_failed`), `try/catch/finally` (Temp-Cleanup), kein Hängen; **kein** `order_media`-Row (Spike). **UI:** `<ReelTestButton>` (generate-controls.tsx) im Status `generated` am Seitenende → POST → „Test-Reel öffnen"-Link, Lade-/Fehler-State (`try/finally` + AbortController), geteilte `NoticeBox`. i18n `reelTest.*`.

- **Schritt 8b-0v2 ✅ FFmpeg-Runtime-Download (Binary NICHT gebundlet)** — realisiert die 8b-0-Lehre: ffmpeg wird zur **Laufzeit nach `/tmp` geladen** (aus Supabase Storage) statt ins Function-Bundle gepackt → **Vercel-Deploy bleibt schlank/grün**. **KEINE** `next.config`-Änderung (kein `serverExternalPackages`/`outputFileTracingIncludes`), **KEINE** `ffmpeg-static`-Dependency, **KEINE** Migration, **KEINE** Schema-Änderung; weiterhin reiner Spike (die `render-reel-test`-Route wird in 8b-1 ersetzt). **Teil A — `scripts/upload-ffmpeg.ts` (NEU, einmalig lokal, `pnpm dlx tsx`):** Service-Client aus `.env.local` (`SUPABASE_URL`/`NEXT_PUBLIC_SUPABASE_URL` + `SUPABASE_SERVICE_ROLE_KEY`, minimaler dependency-freier .env-Loader). Bucket **`assets`** (privat, `public:false`) **idempotent** via `createBucket` (`exists`-Fehler ignoriert) — **bewusst per Script statt SQL-Migration**, weil App-internes Infra-Asset **ohne** Mandanten-Datum/RLS (nur `service_role`-Read); bleibt außerhalb der Tenant-Nummerierung (nächste Migration weiter `0004`). **Gepinnte Quelle** (nicht erfunden, verifiziert): ffmpeg-static Release **`b6.1.1`**, Asset **`ffmpeg-linux-x64.gz`** (~28 MB gz, entpackt ~76 MB; `EXPECTED_GZ_BYTES=29_354_986`/`EXPECTED_BIN_BYTES=79_826_272`) — **statisch** gelinkt (John Van Sickle) → läuft auf Vercels Amazon Linux ohne glibc-Probleme. **gzip**, weil das rohe Binary (~76 MB) das projektweite Supabase-Limit (Default **50 MB**) überschreitet — die ~28-MB-gz passt darunter, **keine** Dashboard-Änderung. Script prüft **gzip-Magic** (`1F 8B`) + gz-Größe, **entpackt** + verifiziert das Ergebnis als **ELF** (`7F 45 4C 46`) + Größe, lädt die **gz** per `service_role` nach `assets/ffmpeg/linux-x64.gz` (`upsert`), loggt Größen+Pfad. **Teil B — Route `…/render-reel-test` (NEU):** `runtime="nodejs"` + `maxDuration=300`, Auth wie Generate (401/403/404, `business_id` aus der RLS-geladenen Order, nie Body). **`ensureFfmpeg()`** — Kern von v2: warme Instanz (`/tmp/ffmpeg` ausführbar via `access X_OK`) ⇒ Download **überspringen** (Cache); Cold Start ⇒ **gz** aus `assets/ffmpeg/linux-x64.gz` (`service_role`, `storage.download`) laden, zur Laufzeit **entpacken** (`gunzip`) und das Binary **atomar** ablegen (Temp schreiben → `chmod 0o755` → `rename`, verhindert halb-geschriebenes Cache-Binary bei parallelen Cold-Starts); Fehlschlag ⇒ **500 `ffmpeg_unavailable`** + Log. ffmpeg von `/tmp/ffmpeg` → triviales **1080×1920**-mp4 (~2 s, `lavfi color`, h264, `yuv420p`, `+faststart`, kein Audio), Timeout 120 s; Output → `order-media` per `service_role` unter `{business_id}/{order_id}/reel-test.mp4` (`upsert`, `video/mp4`), `createSignedUrl(3600)` → `{ ok, url }`. Pro Schritt `try/catch` + `console.error` (`order_id`/`step`/`message`) + JSON-Code (`ffmpeg_unavailable`/`ffmpeg_failed`/`upload_failed`/`sign_failed`); `finally` räumt nur die **Temp-mp4** auf (**NICHT** `/tmp/ffmpeg`). **Teil C:** `<ReelTestButton>` (generate-controls.tsx, Status `generated`) → POST → „Test-Reel öffnen"-Link, Lade-/Fehler-State (`try/finally` + AbortController), i18n `reelTest.*` (re-added). **Gate:** `pnpm typecheck` + `pnpm build` grün; Build-Trace verifiziert **kein** ffmpeg-Binary in `.next` (Deploy schlank). Provisorisch.

- **Schritt 8b-1a ✅ Echtes Foto-Reel (FFmpeg-Assembly + after()-Job + reel_status)** — erstes **echtes** Reel, ersetzt die provisorische `render-reel-test`-Route. Aus den **Fotos** eines generierten Booklets entsteht ein 9:16-Video (1080×1920, **3 s/Foto**, **harte Schnitte**, **ohne Ton**), als **asynchroner Hintergrund-Job** mit persistentem Status + Poll. **NUR FFmpeg, KEIN Sharp.** KEIN Intro/Outro/Captions (8b-1b), KEINE Video-Clips (8b-2), KEIN Ken-Burns (8b-3). **Migration 0004** (`booklets.reel_status` text NOT NULL default `'pending'` CHECK `in (pending,rendering,ready,failed)` + `reel_error` text; `reel_url` aus 0001; **keine** neue Policy/GRANT — `booklets`-RLS + member-`update` + `service_role` decken die Spalten; Verify `0004_reel_status_checks.sql`: Spalten + Check-Constraint). **`ensureFfmpeg()` ausgelagert** nach `lib/reel/ffmpeg.ts` (server-only; die bewährte 8b-0v2-Runtime-Download-Logik: gz aus `assets/ffmpeg/linux-x64.gz` → `gunzip` → atomar nach `/tmp/ffmpeg`, Warm-Cache; `next.config` unangetastet, Binary **nicht** gebundlet). **Render-Endpoint** `POST …/render-reel` (`runtime=nodejs`, `maxDuration=300`): Auth wie Generate (401/403/404), Status muss `generated` ⇒ sonst **409**, ≥ 1 FOTO (`media_type='photo'`) ⇒ sonst **400 `need_photos`**, `business_id` **aus der Order**; setzt **sofort** `reel_status='rendering'` (service_role) + gibt **202** zurück (Booklet fehlt ⇒ 500 `no_booklet`). Die Arbeit läuft in **`after()`** (Hintergrund nach Response, innerhalb maxDuration): `ensureFfmpeg()` → Fotos per `service_role` nach `/tmp` → **ein** ffmpeg-Aufruf (`-loop 1 -t 3` je Bild + `filter_complex` `scale …force_original_aspect_ratio=increase,crop=1080:1920,setsar=1,fps=30` ⇒ **cover**, dann `concat`; `yuv420p`/`libx264`/`+faststart`/`-an`, Timeout 240 s) → Upload nach `order-media` `{business_id}/{order_id}/reel.mp4` (`upsert`). Erfolg ⇒ `reel_url=Pfad`, `reel_status='ready'`; Fehler (jeder Schritt) ⇒ `reel_status='failed'` + `reel_error="{step}: {message}"` + `console.error`; `finally` räumt nur Temp-Fotos + reel.mp4 (NICHT `/tmp/ffmpeg`). **Status-Poll** `GET …/reel-status` (AUTHENTICATED, Order via RLS ⇒ 404): `{ status, url }` — `url` = `createSignedUrl(reel_url,3600)` über den **AUTHENTICATED** Client (0002-Policy, kein service_role) **nur** bei `ready`. **UI** (`generate-controls.tsx`/`page.tsx`): provisorischer `<ReelTestButton>` **entfernt**; neuer `<ReelButton>` im Status `generated` (**opt-in** = Kostenkontrolle) → `POST render-reel` → `reel-status` alle **~3 s** pollen → `rendering`/`ready` (Link aufs signierte reel.mp4 + „Neu erstellen")/`failed` (Fehler + „Erneut"); **Anfangsstatus vom Server** (`page.tsx` liest `reel_status`, signiert `reel_url` bei `ready`) ⇒ persistent über Reload, laufender Render nimmt den Poll automatisch wieder auf. i18n `reel.*` (ersetzt `reelTest.*`). `pnpm typecheck` + `pnpm build` grün.

- **Schritt 8b-1c ✅ Intro/Outro-Frames im Reel (+ optionales Logo-Wasserzeichen)** — das Reel bekommt einen **Intro-** und **Outro-Frame** (je ~2,5 s) mit Logo + Branding + Text; Foto-Frames optional mit Logo-Wasserzeichen. Baut auf 8b-1a/1b; **Status/Job/Upload/Output (`after()`, `reel_status`, `render-reel`/`reel-status`, `{business_id}/{order_id}/reel.mp4`) und UI/Poll unverändert**, **keine Migration**, **NUR FFmpeg, KEIN Sharp**. KEINE Clips (8b-2), KEIN Ken-Burns (8b-3). **Filtergraph-Logik ausgelagert** nach `lib/reel/frames.ts` (NEU, server-only): `bakePhotoFrame`/`bakeIntroFrame`/`bakeOutroFrame`/`assembleReel`/`assertReelAssets` — die Route bleibt reine Orchestrierung (Auth/Status/Downloads/Upload/Cleanup); der 8b-1b-Foto-Pfad (cover-crop + Caption) **unverändert** übernommen, nur ums Wasserzeichen erweitert. **Reihenfolge:** Intro (2,5 s) → Foto-Frames (je 3 s) → Outro (2,5 s), harte Schnitte; `assembleReel` nimmt jetzt **pro Frame eine eigene Dauer** (`-loop 1 -t s`), Encode (`libx264`/`yuv420p`/`+faststart`/`-an`, `concat`) wie 8b-1a. **Intro:** `intro_bg_url` (Bucket `branding`, `service_role`) cover-crop bzw. **Verlauf-Fallback** aus `primary→secondary` (`gradients`-lavfi-Quelle, in jedem Build vorhanden), Vollflächen-Scrim, **Logo prominent oben** (`overlay`, 720×200-Box, Alpha bleibt), **KI-`intro_title`** groß zentriert (Fallback Betriebsname), Akzentbalken + **`intro_tagline`** (uppercase, `primary_color`) darunter; `intro_description` bewusst weg. **Outro:** `outro_bg_url`/Verlauf, Logo, **Betriebsname** + **`outro_message`** + **Kontakt** (Telefon/Website-Host) unten in der Safe-Zone; KEINE Share-/Review-Elemente (Step 9). **Foto-Frames:** Captions unverändert, **zusätzlich** bei `branding.logo_per_page` ein dezentes **Logo-Wasserzeichen** oben links (`overlay`, 320×92-Box, analog Web-Story); Logo **einmal** geladen, mehrfach genutzt. **Neuer Vollflächen-Scrim** `assets/reel/frame-scrim.png` (1080×1920 RGBA, ~11 KB, committet, Generator `scripts/make-frame-scrim.mjs`; spiegelt `.booklet-scrim`, weil der Caption-Scrim nur unten greift) — via `outputFileTracingIncludes` **zusätzlich** getraced. **drawtext-Stolperstein:** `fontfile` steht **nicht** an erster Stelle (`textfile` zuerst) — ein führendes `fontfile=` verschluckte auf einem lokalen 8.1.1-Build die nächste Option; standardkonform und für Vercel 6.0.1 unkritisch. Zentrierung: `x=(w-text_w)/2` (mehrzeilige Blöcke links-bündig in der zentrierten Box, kein `text_align` vor ffmpeg 7.0); Akzentbalken-Zentrierung via `drawbox x=(iw-W)/2` (in drawbox meint `w` die Box-, `iw` die Frame-Breite). Pro Schritt eigener `reel_error`-Code (`assets_missing`/`download_assets`/`bake_intro`/`bake_outro`/…). Lokal mit arm64-ffmpeg verifiziert (alle Frames + Assembly → 1080×1920 DAR 9:16, h264/yuv420p, 30 fps, mute), `pnpm typecheck` + `pnpm build` grün, Tracing eng gescoped.

- **Fix 8b-1c ✅ Logo-Größen + Intro/Outro-Text sichtbar (6.0.1) + Render-Statusanzeige** — drei gezielte Korrekturen, **keine Pipeline-/Assembly-/Reihenfolge-Änderung**, keine Migration. (1) **Logo-Größen** (`lib/reel/frames.ts`): Intro/Outro-Logo-Box 720×200 → **960×340** (~1,3–1,7×, deutlich prominenter), Foto-Wasserzeichen 320×92 → **400×116** (subtil größer). (2) **Intro-/Outro-Text-BUG** — auf dem **Production-Build (ffmpeg 6.0.1**, John Van Sickle Static, s. `scripts/upload-ffmpeg.ts`) rendert der **zentrierte** Intro/Outro-`drawtext` (`x=(w-text_w)/2`, Akzent `drawbox x=(iw-W)/2`) **still nichts** (Hintergrund + Logo via `overlay` sind da, Captions liefen sauber); der **einzige** Unterschied zur funktionierenden Caption waren die zentrierenden `(…)/2`-Ausdrücke (Caption nutzt **literale x**). Fix: Intro/Outro-Text + Akzentbalken auf die **bewährte Caption-Methode** umgestellt — **links-bündig**, literales `x=SIDE_MARGIN`, `textfile`/`expansion=none`/Optionsreihenfolge unverändert. **Logo bleibt zentriert** (`overlay x=(W-w)/2` rendert auf 6.0.1 nachweislich). Lokal mit **ffmpeg 8.1.1 (ffmpeg-full)** verifiziert (Intro/Outro/Frames rendern Text sauber); der Bug tritt nur auf 6.0.1 auf. (3) **Render-Statusanzeige** (`generate-controls.tsx`/i18n `reel.stage1…4`): während `rendering` rotierende, **rein kosmetische** Stufentexte (~3,5 s, letzte bleibt stehen) + Spinner unter dem Button — verkürzt die gefühlte Wartezeit, keine echte Telemetrie (Poll unverändert). `pnpm typecheck` + `pnpm build` grün. *(Der hier noch offen gelassene **Verlauf-Fallback**-Crash (`gradients=…:type=linear`) ist in **Fix 8b-1c (2)** behoben — s. nächster Eintrag.)*

- **Fix 8b-1c (2) ✅ Verlauf-Fallback (6.0.1) + Intro-Beschreibung zurück + Kunden-Ich-Perspektive** — drei zusammenhängende Korrekturen, **keine Pipeline-/Assembly-/Reihenfolge-Änderung**, keine Migration. (1) **Verlauf-Fallback 6.0.1** (`lib/reel/frames.ts`, `backgroundInput`): `gradients`-lavfi-Quelle **ohne** `:type=linear` — die `type`-Option kam erst mit **ffmpeg 6.1**, auf dem Production-Build (6.0.1) crashte sie den Filter ⇒ Intro/Outro scheiterte für Betriebe **ohne** Intro/Outro-Hintergrundbild (die im vorigen Eintrag offen gelassene Baustelle). `linear` ist ohnehin der Default → keine Optik-Änderung. (2) **`intro_description` zurück im Reel-Intro** (`bakeIntroFrame`): war in 8b-1c bewusst weg, ist aber das Herz der Personalisierung — Reihenfolge jetzt **Logo → `intro_title` → `intro_description` (1–2 Sätze) → `intro_tagline`** (Titel bottom-anchored hält Titel↔Akzent eng; Beschreibung + Tagline darunter top-anchored, bewährte links-bündige drawtext-Methode wie Titel/Caption mit literalem x). **Intro-Frame-Dauer 2,5 s → 4 s** (`render-reel`-Route `INTRO_SECONDS`), damit die persönliche Story lesbar ist; die Route lädt `booklets.intro_description` zusätzlich und reicht sie durch. (3) **Kunden-Ich-Perspektive** (`lib/ai/intro.ts`) — der **Kern**: das Intro ist aus der **Ich-Perspektive des teilenden Kunden** geschrieben („Ich habe bei {Betrieb} … lassen — …"), geschlechtsneutral, **nicht** als Selbstdarstellung des Betriebs (sonst wird es nicht geteilt). `generateIntro` bekommt zusätzlich `businessName` (`businesses.name`, von der `generate`-Route durchgereicht); System-Prompt nennt den Betrieb beim Namen als **Erlebnis** des Kunden. Gilt für `intro_title` (kurz, persönlich) **und** `intro_description` (1–2 Sätze Ich-Story), strikt geerdet auf `item_description` + Captions (keine erfundenen Gefühle); `ai_context` (8a-1b) bleibt Betriebs-**FACHkontext** und überschreibt die Ich-Perspektive **nicht**. Wirkt auf **Web-Story UND Reel** (gemeinsamer Intro-Text); greift nur bei **NEU** generierten Intros (gespeicherte `booklets`-Werte bleiben). Lokal mit **ffmpeg-full** verifiziert (Intro/Outro rendern mit Verlauf-Fallback + Beschreibung sauber, 1080×1920 h264/yuv420p), `pnpm typecheck` + `pnpm build` grün.

- **Schritt 8b-1b ✅ Caption-Overlays auf den Foto-Frames des Reels** — brennt pro Foto die Caption unten in der Safe-Zone **ins Reel** (mute-safe, Text trägt die Story). Baut auf 8b-1a; **Status/Job/Upload/Output (`after()`, `reel_status`, `render-reel`/`reel-status`, `{business_id}/{order_id}/reel.mp4`) unverändert**, **keine Migration**, **NUR FFmpeg, KEIN Sharp**. KEIN Intro/Outro (8b-1c), KEINE Clips (8b-2), KEIN Ken-Burns (8b-3). **Schrift MITGELIEFERT (Kern):** Vercel hat keine System-Fonts/kein fontconfig → die Schrift liegt im Repo (`assets/fonts/PlusJakartaSans-SemiBold.ttf`, ~130 KB, OFL daneben; SemiBold = `font-weight:600` der Web-Story-Caption) und wird **explizit per `fontfile=`** referenziert (drawtext über libfreetype, **kein** fontconfig-Lookup). Per-Betrieb-Font im Reel ist spätere Politur. **Tracing:** `next.config.ts` bindet Font **+ Scrim** via `outputFileTracingIncludes` gezielt in die `render-reel`-Function (Glob `/api/portal/orders/*/render-reel` + Bracket-Fallback) — **kleine** Dateien, **kein** Deploy-Größenproblem (anders als das ffmpeg-Binary 8b-0); Trace verifiziert (`.nft.json` listet Font+Scrim nur in dieser Route). Laufzeitpfad relativ zu `process.cwd()`. **Caption-Text:** `displayCaption(media)` (`caption ?? keyword`; beide leer ⇒ `null` = sauberes Foto, kein Scrim) aus `lib/booklet/caption.ts` — **dieselbe** Quelle wie die Web-Story (kein Drift). **Rendering = Variante B (ffmpeg `drawtext`)**, bewusst statt Sharp/SVG (librsvg bräuchte ebenfalls fontconfig). **Zweistufig** (jeder ffmpeg-Aufruf simpel + einzeln diagnostizierbar): (1) **pro Foto ein 1080×1920-PNG backen** — cover (`scale=…increase,crop=1080:1920,setsar=1`); mit Caption zusätzlich **Scrim** (`overlay`) + **Branding-Akzentbalken** (`drawbox`, `primary_color`) + **`drawtext`** (weiß, SemiBold, links-bündig, Safe-Zone); ohne Caption nur cover-crop. (2) **Frames → Reel** (`-loop 1 -t 3` je Frame, `setsar=1,fps=30,format=yuv420p` [vereinheitlicht RGB/RGBA → `concat` braucht EIN Format], harte Schnitte, `libx264`/`yuv420p`/`+faststart`/`-an` wie 8b-1a). **Scrim** = committetes `assets/reel/caption-scrim.png` (1080×1920 RGBA, ~10 KB, transparent 0–42 % → schwarz 0.72; spiegelt `.booklet-caption-scrim`), erzeugt per `scripts/make-caption-scrim.mjs` (reines Node/zlib) — deterministisch, verifizierbar, getraced (kein Laufzeit-`geq`). **Layout:** Safe-Zone ~16 % (`BOTTOM_MARGIN=307`), `SIDE_MARGIN=80` links-bündig (robust ohne `text_align`), Fontgröße 52, `line_spacing=12`, Schatten; **mehrzeiliger Umbruch** (`wrapCaption`, greedy, konservativ) für bis ~180 Zeichen → echte Newlines in eine **`textfile`** (kein Filtergraph-Escaping; `expansion=none`), vertikale Verankerung über `text_h` (`y=h-335-text_h`). **Diagnose statt Garbage:** Font fehlt/`drawtext` fehlt ⇒ `reel_status='failed'` + klares `reel_error` (`font_missing`/`bake_frames`), nie leerer/unleserlicher Text. Route lädt Fotos jetzt mit `caption,keyword`, reicht `primary_color` (normalisiertes Hex) durch. **Lokal mit arm64-ffmpeg verifiziert** (Sample-Frames: cover-crop, Scrim, Akzentbalken, weiße mehrzeilige Caption inkl. Umlaute/`—` lesbar; Assembly → 6 s, h264/yuv420p, 1080×1920 DAR 9:16, 30 fps, mute). **Fallback** (falls Vercel zickt): Variante A (Sharp + SVG mit eingebettetem Font) — **erst nach Rücksprache**. `pnpm typecheck` + `pnpm build` grün, Tracing eng gescoped.

- **Schritt 8b-2a-1 ✅ Render-Ticker erweitert (rein kosmetisch, Frontend)** — nur UI-Text, **keine** Backend-/Render-/Poll-Logik. Die rotierenden Stufentexte unter dem Reel-Button (`generate-controls.tsx`, `REEL_STAGES`) von 4 auf **14 sequenzielle** Meldungen erweitert (`reel.stage1…14` in `lib/i18n/de.ts`), die der echten Pipeline folgen (Vorbereiten → Medien → Intro/Logo → Fotos/Captions → Clips/Hochformat/Ton → Assembly → Outro → Feinschliff). Intervall `REEL_STAGE_MS` 3500 → **4000 ms** → die ~1-Min-Renders zeigen jetzt eine sinnvolle Sequenz statt schneller Wiederholung; die letzte Meldung bleibt stehen (bestehendes `Math.min(i+1, len-1)`, kein Zurückspringen/Wiederholen), gestoppt wie gehabt über den `reel-status`-Poll. `pnpm typecheck` + `pnpm build` grün.

- **Schritt 8c ✅ Medien-Anzahl-Limit (Settings + Capture-Enforcement + Server-Guard)** — pro-Betrieb-Limit für die Anzahl Fotos/Videos je Auftrag, unter einem **harten Plattform-Ceiling**. **Keine Migration** (Keys im bestehenden `businesses.settings`-jsonb, wie 5a/7b/8a-1b). **Ceiling-Logik** (`lib/settings/options.ts`): `PHOTO_COUNT = {min:1,max:20,default:10}`, `VIDEO_COUNT = {min:1,max:10,default:3}` — `max` ist das **Plattform-Ceiling** (Kostenschutz; wandert später ins Admin-Portal), der pro-Betrieb-Wert ist die Einstellung darunter (analog `VIDEO_SECONDS`). **Datenmodell:** `BusinessSettings` + `normalizeSettings` (`lib/auth/current-business.ts`) um `photo_max_count`/`video_max_count` erweitert — aus jsonb lesen, auf `[min..Ceiling]` clampen, Default bei fehlend/ungültig (kein `any`, gleiches Muster wie `video_max_seconds`). **Settings-`PATCH`** (`app/api/portal/settings/route.ts`): `photo_max_count ∈ [1,20]` / `video_max_count ∈ [1,10]` via `intInRange` ⇒ sonst 400 (`invalid_photo_count`/`invalid_video_count`); in den bestehenden settings-**READ-MERGE-WRITE** aufgenommen (nicht überschreiben); `business_id` weiter **nur** aus Session. **Settings-Form** (`settings-form.tsx`): in der bestehenden „Aufnahme"-Gruppe (wo die Video-Länge sitzt) zwei `RangeNumberField` „Max. Fotos pro Auftrag" + „Max. Videos pro Auftrag" (Range + gekoppeltes Zahlenfeld); Client-Validierung = Server (1..Ceiling). **ENFORCEMENT Capture** (der eigentliche Punkt): die Detailseite (`page.tsx`) zählt die geladenen `order_media` getrennt nach `media_type` und reicht `photoMax`/`videoMax`/`photoCount`/`videoCount` an `capture.tsx`; dort werden „Foto aufnehmen/hochladen" **deaktiviert** (grau, `pointer-events:none`, `aria-disabled`, `tabIndex -1`) + Hinweis (`capture.limitReached {type,max}`), sobald `photoCount + in-flight-Foto-Queue-Items ≥ photoMax` (analog Video) — **in-flight** = die optimistischen Queue-Items des jeweiligen Typs, damit man nicht mehrere einreiht, wenn nur 1 Slot frei ist. **Server-Guard** (Wahrheit, `app/api/portal/orders/[id]/media/route.ts`): vor dem Insert die vorhandenen `order_media` des jeweiligen `media_type` dieser Order zählen (`count: "exact", head: true`); `≥ business.settings.{photo,video}_max_count` ⇒ **400 `limit_reached`** + `console.error`. Client-Disable ist UX, dieser Check ist der **harte Riegel** (Client umgehbar). i18n: `settings.photoMaxCount`/`videoMaxCount` (+ `*Hint`), `settings.errPhotoCount`/`errVideoCount`, `capture.limitReached`/`photosLabel`/`videosLabel`. `pnpm typecheck` + `pnpm build` grün.

- **Schritt 8b-2a ✅ Video-Clips ins Reel (Assembly, OHNE Clip-Captions)** — das Reel verwebt jetzt **Video-Clips** mit den Fotos: normalisiert auf 9:16, **stumm**, **interleaved nach `sort_order`**. Baut auf 8b-1a/1b/1c; **Status/Job/Upload/Output (`after()`, `reel_status`, `render-reel`/`reel-status`, `{business_id}/{order_id}/reel.mp4`) + UI/Poll unverändert**, **keine Migration**, **NUR FFmpeg, KEIN Sharp**. Fotos + Captions (8b-1b) und Intro/Outro-Frames (8b-1c) **unverändert**; KEINE Clip-Captions/Wasserzeichen (8b-2b), KEIN Ken-Burns (8b-3). **Media laden** (`render-reel/route.ts`): jetzt **ALLE** `order_media` (Fotos UND Videos), `sort_order` ASC (vorher nur `media_type='photo'`); Guard **`need_photos` → `need_media`** (≥ 1 Foto ODER Video ⇒ sonst 400). **Pro Item normalisieren** (`lib/reel/frames.ts`): jedes Item bekommt **dieselbe kanonische Form** (`CANON_ENCODE`: libx264/yuv420p/`-an`, 30 fps CFR, SAR 1:1, 1080×1920) — sonst bricht concat. **Foto** = `bakePhotoFrame` (unverändert) → **`encodeStillSegment`** (NEU; PNG `-loop 1 -t 3` → mp4-Segment). **Video** = **`normalizeClip`** (NEU): cover-crop (`scale=…increase,crop=1080:1920,setsar=1`; `autorotate` greift VOR den Filtern → Hochformat-Handyclips aufgerichtet), `fps=30,format=yuv420p`, **Audio gestrippt** (`-an`), **Dauer-Cap min(Clip, 6 s)** via `-t 6` als **INPUT-Option** (`MAX_CLIP_SECONDS=6`, Tempo; 8b-3 konfigurierbar). **Assembly = concat-Demuxer** statt concat-Filter: **`assembleReel` durch `concatSegments` ersetzt** — **Intro → [Items in `sort_order`: Foto-Stills + Clip-Segmente gemischt] → Outro**, jedes Item ein formatgleiches mp4-Zwischensegment, gefügt per Dateiliste (`-f concat -safe 0 -i list.txt -c copy -movflags +faststart`). Demuxer + `-c copy` (kein Re-Encode, schnell, isoliert je Item) statt eines großen concat-Filters mit gemischten Image-/Video-Inputs — heterogene Medien gehen **nur format-identisch** durch concat (`encodeStillSegment`/`normalizeClip` garantieren die EINE Form). **Diagnose-Schritte** umbenannt/neu: `download_photos`→**`download_media`**, `bake_frames`→**`build_segments`** (Foto-Frame+Encode bzw. Clip-Normalisierung, mit `item {i} ({media_type})`-Kontext im `reel_error`), `ffmpeg`→**`concat`**; Rest (`assets_missing`/`ensure_ffmpeg`/`download_assets`/`bake_intro`/`bake_outro`/`upload`/`mark_ready`) unverändert. i18n `reel.needPhotos`→`reel.needMedia` + `reel.hint` (nennt Clips bis 6 s). **`ensureFfmpeg`/`service_role`/`business_id` aus Order/Session** unverändert. Lokal mit ffmpeg 8.1.1 verifiziert: normalisierter Landscape-Clip-mit-Audio → 1080×1920/SAR 1:1/yuv420p/30 fps/**exakt 6 s**/**0 Audio-Spuren**; `-c copy`-concat Still+Clip+Still → 1080×1920/yuv420p/30 fps/**exakt 12 s**/mute (concat-Demuxer + cover/fps sind alt+stabil, kein 6.0.1-Stolperstein). `pnpm typecheck` + `pnpm build` grün, Tracing (Font + beide Scrims) unverändert.

- **Schritt 8b-2b ✅ Captions + Logo-Wasserzeichen auf Video-Clips (geteilte Overlay-Kette)** — die in 8b-2a noch **nackten** Clip-Segmente bekommen **dieselbe** Overlay-Behandlung wie die Foto-Segmente (8b-1b/1c): **Caption-Scrim + Branding-Akzentbalken + Caption-drawtext** und das **optionale Logo-Wasserzeichen** (`logo_per_page`) — über den **Video-Stream** gebrannt. Fotos/Intro/Outro + gesamte Assembly/Orchestrierung (`concatSegments`, `after()`, `reel_status`, `render-reel`/`reel-status`, Output-Pfad, UI/Poll) **unverändert**, **keine Migration**, **NUR FFmpeg, KEIN Sharp**, KEIN Ken-Burns (8b-3). **ZUERST verifiziert:** die Foto-Captions rendern via **ffmpeg `drawtext`** im `filter_complex` (Scrim per `overlay`, Akzent per `drawbox`, Text per `drawtext=textfile=…:fontfile=…`) — **nicht** Sharp/Canvas ⇒ Filter-Logik wiederverwendbar. **GETEILTE Overlay-Kette** (`lib/reel/frames.ts`): Overlay-Erzeugung in neuen Helfer **`buildCaptionOverlay`** gezogen (nimmt Basis-Label + ersten freien Input-Index → `{ extraInputs, parts, outLabel }`), den **Foto- (`bakePhotoFrame`) UND Clip-Pfad (`normalizeClip`)** nutzen — **nicht dupliziert**; `bakePhotoFrame` erzeugt **byte-identische** Filtergraph-Strings wie zuvor (reines Refactoring). **`normalizeClip`-Erweiterung:** drei neue Parameter (analog `bakePhotoFrame`) `caption`/`logoPath`/`primaryColor`; **Caption-Text** = `displayCaption(media)` (`caption ?? keyword`, beide leer ⇒ `null` ⇒ **kein** Overlay — wie beim Foto, sauberer Clip kein Scrim) aus `lib/booklet/caption.ts` (dieselbe Quelle wie Web-Story/Fotos, kein Drift); **Logo** nur bei `branding.logo_per_page` (Gate in der Route, identisch zum Foto), gleiche Box/Position. **Schneller 8b-2a-Pfad bleibt** (ohne Caption **und** Logo ⇒ unverändertes `-vf COVER,fps,format`); erst bei nötigem Overlay der `filter_complex`-Pfad: `[0:v]COVER,fps=30[base]` → `buildCaptionOverlay` → `[…]format=yuv420p[v]`, `-map [v]`, `…CANON_ENCODE`; `-t maxSeconds` bleibt **INPUT-Option** **vor** `-i input` (cappt nur den Clip; Scrim/Logo sind Einzelbild-Inputs danach). **Statisch über die volle Clip-Dauer** (kein `enable=`): Scrim/Logo-PNGs sind Einzelbilder, `overlay` (`eof_action=repeat`, Default) hält sie je Frame, `drawtext` ohne `enable=` rendert je Frame; mute-safe (`-an`). **6.0.1-sicher** (wie die Foto-Caption seit 8b-1b/1c, nicht neu erfunden): **literales** `x`, **links-bündig**, **keine** zentrierten `(…)/2`-Ausdrücke (der 8b-1c-Crash); `textfile`/`fontfile` (**kein** fontconfig), Escaping via `textfile` (`expansion=none`). **Kanonische Form unverändert** (1080×1920, yuv420p, h264, 30 fps CFR, SAR 1:1, stumm; `format=yuv420p` letzter Filter + `CANON_ENCODE`) ⇒ Clip-Segmente bleiben **format-identisch** zu den Stills, **concat-Demuxer (`-c copy`)** fügt weiter verlustfrei. **Route** (`render-reel`) reicht im Video-Zweig `caption: displayCaption(item)`, `logoPath: logoPerPage ? logoLocal : null`, `primaryColor` durch — exakt wie der Foto-Zweig; `ensureFfmpeg`/`service_role`/`business_id aus Order/Session` unverändert. Lokal mit ffmpeg-full 8.1.1 verifiziert: Landscape-Clip-mit-Audio → Overlay-Pfad → 1080×1920/SAR 1:1/yuv420p/30 fps/**exakt 6 s**/**0 Audio-Spuren**, Caption (inkl. Umlaute/`—`) + Akzentbalken + Logo sichtbar eingebrannt (Frame-Extraktion geprüft); `-c copy`-concat Still+Clip-Overlay+Still → **12 s**, durchgängig 1080×1920/yuv420p/30 fps. `pnpm typecheck` + `pnpm build` grün, Tracing (Font + beide Scrims) unverändert.

- **Schritt 8b-2c ✅ Intro-Frame-Layout robust (kein Caption-Überlapp)** — behebt den **Überlapp** zwischen KI-Beschreibung und Settings-Tagline im Reel-**Intro** (`bakeIntroFrame` in `lib/reel/frames.ts`). Reines Layout — **keine Migration**, **keine** Pipeline-/Assembly-/Reihenfolge-Änderung, **Web-Story unverändert**, nur das Intro-Frame (nicht Foto-/Clip-/Outro-Frames). **Problem:** Reihenfolge Logo → Titel → Beschreibung (KI, variabel lang) → Tagline (Settings); Beschreibung top-anchored (`y=952`), Tagline an festem `y=1240` direkt darunter → eine lange Beschreibung wuchs in die Tagline-Zone (feste y + variabler Text). **Fix (zwei entkoppelnde Maßnahmen):** (1) **Tagline FEST nahe dem unteren Rand gepinnt**: `TAGLINE_TOP` (1240, top-anchored) → `TAGLINE_BOTTOM` (1760, **bottom-anchored** via `y=${TAGLINE_BOTTOM}-text_h`) ⇒ Position von der Beschreibungslänge **entkoppelt**. (2) **Beschreibung NUR fürs Reel-Intro gekürzt** (neuer Helfer `truncateForIntro`, `REEL_DESCRIPTION_MAX_CHARS=140` ≈ ≤ 4 gewrappte Zeilen): am letzten Wortende vor dem Limit schneiden (kein abgehacktes Wort), Satz-/Trennzeichen am Ende strippen, „…" anhängen; kurze Beschreibungen unverändert. Die in `booklets` gespeicherte `intro_description` wird **NICHT** angefasst — nur die Render-Kopie wird gekürzt, die Web-Story zeigt weiter die volle Länge. Mit beidem endet die (gekürzte) Beschreibung weit oberhalb der gepinnten Tagline ⇒ **kein Überlapp**, egal wie lang die KI-Ausgabe. **Leere Blöcke sauber** (bestehende `if (description)`/`if (tagline)`-Guards): fehlt einer, entsteht durch die festen Positionen **keine Geisterlücke**. **6.0.1-sicher** beibehalten: literales `x`, links-bündig, `textfile`/`fontfile`, keine zentrierten Ausdrücke. `pnpm typecheck` + `pnpm build` grün.

- **Schritt 8d ✅ Reel-Status in der Auftragsliste sichtbar** — die Liste zeigte nur `order.status`; jetzt **zusätzlich** den `booklets.reel_status`, damit „Reel fehlt" sofort auffällt. **Zwei Achsen, klar getrennt:** `order.status` (Lifecycle: draft→…→shared) ist die **Liste-Wahrheit**; `reel_status` (pending/rendering/ready/failed, aus 0004) ist die **Render-Achse** — die neue Pill zeigt sie **nur im Auftrags-Status `generated`** (Booklet existiert, noch nicht versendet — genau das Fenster, in dem „Reel fehlt" relevant ist). **Keine Migration.** **Daten** (`app/portal/orders/page.tsx`): nach der Order-Query eine **zweite Query** auf `booklets` (`order_id, reel_status`, `.in("order_id", generatedIds)`) über den **AUTHENTICATED Client** (RLS, booklets member-lesbar — **kein** `service_role`); nur für die generierten Aufträge geladen → `Map<order_id, reel_status>`. **Anzeige** (neue `<ReelStatePill>`, `components/reel-state-pill.tsx`, reine Präsentation/Server-Component-fähig, Muster wie `order-status-badge.tsx`): in der Zeile **neben** dem Status-Badge (gemeinsamer wrap-Flex), nur bei `order.status==='generated'` — `draft`/`finalized` (noch kein Booklet) und `sent`/`viewed`/`shared` (ausgeliefert) ⇒ **keine** Pill. Zustände: **pending/null → „Reel fehlt" (Amber, aufmerksamkeitsstark)** ← der Kern, `rendering → „Reel rendert …" (blau)`, `ready → „Reel fertig" (grün)`, `failed → „Reel fehlgeschlagen" (rot)`. **Tokens** (`globals.css`): neue `--amber-*`/`--blue-*`/`--red-*` (analog zu `--green-*` aus 6c; Grün wiederverwendet). `ReelStatus`-Typ bleibt **eine Quelle** (aus `generate-controls.tsx`, re-exportiert über die Pill). i18n `orderStatus.reelMissing`/`reelRendering`/`reelReady`/`reelFailed` (keine Inline-Strings). `pnpm typecheck` + `pnpm build` grün.

- **Schritt 9a ✅ Share-Sheet auf der Web-Story (Kunden-Teilen, der WOM-Kern)** — die öffentliche Web-Story `/b/[token]` bekommt ihre **Teilen-Mechanik**: Reel als **Datei**, Story als **URL**, WhatsApp, Link kopieren, Download-Fallback. **Keine Migration.** KEINE Review-/IG-Caption-Aktionen (9b), KEIN View-/Analytics-Tracking (Step 9/10). **Daten** (`app/b/[token]/page.tsx` bleibt Server Component): der Lader `lib/booklet/load.ts` liest zusätzlich `booklets.reel_status` + `reel_url` und signiert den Reel-Pfad (Bucket `order-media`, `{business_id}/{order_id}/reel.mp4`) **nur bei `reel_status='ready'`** server-seitig per **`service_role`** — im **selben `signPaths`-Batch** wie die Medien (ein Round-Trip, 3600 s), sonst `null`; neues Feld `PublicBookletData.reelSignedUrl`. **Token bleibt einzige Vertrauensquelle (§14.2)** — alle Reads strikt auf `business_id`/`order_id` der Booklet-Row gescoped. **Kanonische Story-URL** aus den Request-Headern (`x-forwarded-host` ?? `host`, `x-forwarded-proto` ?? `https`; Seite ist `force-dynamic`). Beides als Props (`storyUrl`/`reelSignedUrl`/`locale`) an die **NEUE Client-Komponente `<ShareBar>`** (`app/b/[token]/share-bar.tsx`, `"use client"`), gerendert im **Outro** (nach der Abschiedsnachricht, vor den Kontakt-Pills). **ShareBar (SSR-sicher** — `window`/`navigator` nur in Handlern/Effects, nie beim Render; Buttons `div + onClick` via `<Pressable>` mit `role="button"` + Enter/Space): Reihenfolge wie Pflichtenheft §4 — **(1) „Reel teilen"** (nur wenn `reelSignedUrl`): `fetch` → `new File([blob],"reel.mp4",{type:"video/mp4"})` → `navigator.canShare({files})` **+** `navigator.share({files,title})` (öffnet IG/TikTok-Composer), sonst **Download-Fallback** (Anchor `download`); Ladezustand, **Nutzer-Abbruch = kein Fehler**; Capability-Probe im `useEffect` (Dummy-`File` an `canShare`) wählt optimistisch das Label (kann teilen ⇒ „Reel teilen", sonst ⇒ „Reel herunterladen"); `reelSignedUrl` null ⇒ Button **gar nicht** gerendert (bewusst „kastriert", kein Fake). **(2) „Story teilen"** (URL): `navigator.share({url,title,text})`, sonst Fallback = Link kopieren. **(3) „WhatsApp"**: Deeplink `https://wa.me/?text=<encodeURIComponent(message + " " + storyUrl)>` via `window.open(...,"_blank","noopener,noreferrer")`. **(4) „Link kopieren"**: `navigator.clipboard.writeText` → „✓ Link kopiert"-Flash (~2 s, Timer im `useRef`, beim Unmount geräumt). **Stil** (`booklet.css`): prominente Teilen-Sektion im Outro, „Reel teilen" als **Hauptaktion** (`.booklet-share-primary`, `--bk-primary`), Story/WhatsApp/Link als Outline-Kacheln; das Outro darf jetzt über `100dvh` **hinauswachsen** (`.booklet-section--outro`: `min-height:100dvh; height:auto; overflow:visible`; Snap-Punkt oben) — full-bleed Medien-Sektionen unverändert. i18n `share.*` (`heading`/`shareReel`/`download`/`shareStory`/`whatsapp`/`copyLink`/`copied`/`sharing` + `shareTitle`/`message` für `navigator.share`/WhatsApp, Kunden-Perspektive), Labels über `t(locale, "share.…")`. `pnpm typecheck` + `pnpm build` grün.

- **Schritt 9a.1 ✅ Teilen-Section kompakter (nur CSS)** — die Outro-Teilen-Section war zu groß (überdeckte halben Handy-Schirm); **reines Layout, kein funktionaler Code** (`share-bar.tsx`/Markup unangetastet). Nur `booklet.css`: `.booklet-share` `margin-top` 32→18, `gap` 12→8; Heading kleiner (`clamp(.82,3.4vw,1rem)`→`clamp(.78,3.2vw,.92rem)`); Hauptaktion „Reel teilen" `min-height` 56→46, `padding` 15/20→11/18, `font-size` 1.05→.95rem; Outline-Kacheln (`.booklet-share-btn`) `min-height` 64→48, `padding` 10/6→7/5, `gap` 6→4, `font-size` .8→.72rem, Row-`gap` 10→8; Icons via CSS auf 18×18 begrenzt. Alle Buttons bleiben. `pnpm typecheck` + `pnpm build` grün.

- **Schritt 9b ✅ Review-Entwurf (Sonnet) + IG-Caption auf der Web-Story (WOM-Verstärker)** — personalisierter Google-Review-Entwurf + IG-Caption-Vorschlag. **Bei der Generierung erzeugt und im Booklet gespeichert — die öffentliche Seite bleibt KI-frei** (kein Live-KI-Call beim Aufruf). **Keine Migration** (`booklets.review_draft` + `ig_caption` aus 0001). **§8.6-Leitplanken sind PFLICHT** (Pflichtenheft): (1) Framing **„Vorschlag, gern in deinen Worten anpassen"** — **nicht** „diesen Text einfügen" (verbatim-identische KI-Reviews triggern Googles Spam-Erkennung); (2) **NIEMALS an eine Belohnung gekoppelt** (harter Google-ToS-Verstoß) — **kein** Belohnungs-Text irgendwo (UI + Strings). **(1) Generierung erweitert:** `generateReviewDraft` (Sonnet, `lib/ai/review.ts` NEU) — Google-Review aus dem **konkreten** Booklet-Inhalt (`item_description` + Captions + `intro_title`/`description`), Betriebsname genannt, **Ich-Perspektive des Kunden** („Ich war bei {Betrieb} …", wie das Intro/FIX 8b-1c), Deutsch, NATÜRLICH/SPEZIFISCH/glaubwürdig, 2–4 Sätze, **keine** übertriebenen Superlative/Floskeln (sonst Spam-Optik); `ai_context` (8a-1b) als **KONTEXT-Block**, keine Anweisung (Injection-Hygiene exakt wie beim Intro); kein JSON (einzelner Textblock, `cleanReview` strippt umschließende Quotes), `max_tokens 400`. `languageName` (§15) nach `lib/ai/language.ts` ausgelagert (eine Quelle für Intro **und** Review; `intro.ts` nutzt jetzt den Import). `buildIgCaption` (**TEMPLATE, KEIN KI-Call**, `lib/booklet/ig-caption.ts` NEU) — aus `intro_title` + `@{ig_handle}` (nur wenn gesetzt, führende `@` normalisiert) + kleinem kuratierten DE-Hashtag-Set; reine Funktion, kann nicht fehlschlagen. **Generate-Route** (`…/generate`): nach dem Intro **Review (Sonnet) + IG-Caption (Template)** erzeugt und in **beide** Upsert-Zweige (`review_draft`/`ig_caption`) mitgeschrieben (Re-Generate überschreibt, wie das Intro). **Review-Generierung NON-FATAL** (eigener `try/catch` + `console.error`): ein Sonnet-Fehler dort setzt `review_draft=null` und blockiert die Booklet-Generierung **nicht** (Web-Story funktioniert ohne Vorschlag; Re-Generate versucht erneut) — bewusst entkoppelt, da supplementär; das Intro bleibt der kritische Pfad (502 bei Fehler). `business_id` weiter **aus der Order** (§14.2), `service_role`-Upsert unverändert. Bestehende Booklets bekommen Review/IG erst beim **nächsten** Generieren. **(2) Seiten-Daten** (`lib/booklet/load.ts`): `review_draft` + `ig_caption` aus der Booklet-Row mitgeladen + durchgereicht (`PublicBookletData.reviewDraft`/`igCaption`); `settings.google_review_url`/`ig_handle` kamen schon über `normalizeSettings`. Token bleibt einzige Vertrauensquelle, `service_role` nur server-seitig. **(3) Web-Story-UI** (im **Outro**, `<ShareBar>` aus 9a erweitert — **DRY**, kein zweiter Client, geteiltes `copyText`/`flashCopied`/`<Pressable>`, SSR-sicher): neue Props `reviewDraft`/`googleReviewUrl`/`igCaption`; ein einzelner `copiedKey`-State (`"link"|"ig"|"review"`) ersetzt das 9a-`copied`-Boolean. **IG-Caption** — nur wenn `igCaption` vorhanden — als Panel **nahe „Reel teilen"** (`.booklet-ig`: Label + `white-space:pre-line`-Text + „Caption kopieren"). **„Google-Bewertung schreiben"** — nur wenn `googleReviewUrl` **UND** `reviewDraft` vorhanden — **unter** dem Share-Sheet (`.booklet-review`): Klick → `review_draft` in die Zwischenablage (zuerst, Doc fokussiert ⇒ zuverlässig) → `google_review_url` im neuen Tab (`window.open`, Protokoll-Guard); **Hinweis** „Vorschlag — pass ihn gern in deinen Worten an." (§8.6 #1). `div + onClick`, Valooro-`--bk-*`-Tokens wie 9a. **i18n** `review.*` (`button`/`hint`/`copied`) + `igCaption.*` (`label`/`copy`/`copied`) in `de.ts` über `t(locale, …)`, `locale` aus `booklet.language` — **kein** Belohnungs-Text in den Strings. `pnpm typecheck` + `pnpm build` grün.

- **Schritt 9d ✅ Teilen-Sektion nur für die Kunden-Sicht (URL-Marker, kein Share-State)** — die Teilen-Aktionen (9a/9b) erscheinen **nur auf dem ausgelieferten Link** (Kunde), **nicht** auf dem **geteilten** Link (Empfänger). Unterschieden über einen **URL-Marker `?c=1`**, **KEIN** Share-State (**kein** `first_shared_at`, **keine** DB-Spalte), **keine Migration**. **Marker = UI-Schalter, KEIN Auth-Gate (§14.2):** der `access_token` bleibt der **alleinige** Zugriffsschutz; der Marker schaltet **ausschließlich UI**, **kein DB-Zugriff** hängt daran (`loadPublicBooklet` unverändert, Marker fließt nirgends in eine Query). Geteilte Quelle/Guard in `lib/booklet/customer-view.ts` (NEU, plain-Modul ohne `service_role`/Secrets → auch im Client importierbar): `CUSTOMER_VIEW_PARAM`/`_VALUE`, `CUSTOMER_VIEW_QUERY` (`"c=1"`) + `isCustomerViewParam(searchParams)` — **eine** Quelle fürs Lesen (Seite) und Schreiben (Vorschau-Link). **Seite** (`app/b/[token]/page.tsx`, Server Component): liest jetzt zusätzlich `searchParams` (Next-15-`Promise`) → `isCustomerView` → Prop an `OutroSection`. **`isCustomerView=true`** (markierter Link): normales Outro **+ volle Teilen-Sektion** (`<ShareBar>`, 9a/9b unverändert). **`isCustomerView=false`** (nackter Link): **NUR** `<ShareBar>` weggelassen (`{isCustomerView ? … : null}`) — das **normale Settings-Outro** (Logo, `outro_message`, Website/E-Mail/Telefon-Pills) bleibt **exakt** wie definiert, Intro + Medien identisch. **Share teilt die NACKTE URL:** `storyUrl` wird bewusst **ohne** Marker gebaut (`/b/[token]`, kein `?c=1`) → „Story teilen"/„Link kopieren"/WhatsApp (`share-bar.tsx` unverändert, bekam schon immer die nackte URL) führen jeden Empfänger automatisch in die **Empfänger-Sicht**. **Vorschau-Link** (`generate-controls.tsx`, Status `generated`) hängt den Marker an (`/b/${token}?${CUSTOMER_VIEW_QUERY}`), damit der Betrieb in der Vorschau die **volle Kunden-Sicht** sieht (E-Mail/QR hängen ihn in 9c an). `pnpm typecheck` + `pnpm build` grün.

- **Schritt 9c-1 ✅ Ausliefern — sent-Status + Billing-Event + Booklet-E-Mail (Resend)** — der **manuelle Auslieferungs-Pfad**: ein generierter Auftrag wird ausgeliefert → Status `generated → sent`, **Billing-Event** geschrieben, **E-Mail** mit dem Booklet-Link an den Kunden. **Keine Migration** (`orders.status`/`booklets.sent_at`/`billing_events` aus 0001). QR-Druckansicht ist 9c-2; View-/Share-Analytics (`booklet_events`) + Stufen `viewed`/`shared` bleiben offen. **Setup:** dep `resend` (pnpm; reines JS-Paket, **kein** Lifecycle-Script → nicht in `onlyBuiltDependencies`); neue Env `RESEND_API_KEY` (server-only) + `BOOKLET_BASE_URL` (öffentliche Booklet-Domain, z. B. `https://b.valooro.com`), beide in `.env.example`. **E-Mail-Schicht** (`lib/email/`, server-only): `resend.ts` (lazily gecachter Client, Key **nur** aus `RESEND_API_KEY`, nie `NEXT_PUBLIC`; Muster wie `lib/ai/anthropic.ts`); `booklet-email.ts` `sendBookletEmail({to,customerName,businessName,bookletUrl,replyTo?})` — Absender **`"{Betrieb}" <hello@valooro.com>`** (valooro.com verifiziert; Betriebsname nur Anzeigename, RFC-5322-sicher gequotet), **reply-to = öffentliche `contact_email`** (Kunde antwortet an den Betrieb, nicht Valooro); Inhalt DE, **rich für Deliverability** (personalisierter Betreff, Anrede mit Kundenname, ein Satz Kontext, **Button** „Booklet ansehen" + Klartext-Fallback, Signatur), **HTML + Plaintext** (escaped); i18n-ready (MVP de, `order.language` später). **Link:** `${BOOKLET_BASE_URL}/b/${access_token}?${CUSTOMER_VIEW_QUERY}` — mit **`?c=1`** (Kunden-Sicht, `customer-view.ts` aus 9d wiederverwendet); ohne Env Fallback auf den Request-Origin (dev). **Route Handler** `POST …/deliver`: AUTHENTICATED (401/403), Order via RLS (404), **`business_id` NUR aus der Order** (§14.2); Guard `status==='generated'` ⇒ sonst **409**; Booklet fehlt ⇒ 500 `no_booklet`. Ablauf: (1) `orders.status → sent` (AUTHENTICATED, **defensiv `.eq('status','generated')`** → kein Doppel-Versand) ⇒ Fehler 500 `status_failed`; (2) `booklets.sent_at = now()` (AUTHENTICATED, **nicht-blockierend**, geloggt); (3) **Billing-Event** `'booklet_sent'` über **`service_role`** — `billing_events` hat für `authenticated` **kein INSERT-Grant** (0001: nur SELECT, „Schreiben serverseitig"), `business_id` aus der Order, **nicht-blockierend** (laut geloggt); (4) **E-Mail** nur bei `customer_email`, **NICHT-BLOCKIEREND** ⇒ Fehlschlag setzt `emailFailed:true`, `sent` steht trotzdem. **Abweichung von der Spec-Notiz „alles AUTHENTICATED":** der Billing-Insert **muss** `service_role` nutzen (RLS verbietet `authenticated`-INSERT auf `billing_events`); Status-/`sent_at`-Updates bleiben AUTHENTICATED. Response `{ sent:true, emailSent:bool, emailFailed?:true }`. **UI** (`deliver-controls.tsx` NEU + `page.tsx`): `<DeliverButton>` (Status `generated`, Seitenende, **finale Aktion** nach Reel; `btn-gold`, mobil groß) → `window.confirm` mit **bewussten Warnungen (KEIN Block)**: Reel nicht `ready` ⇒ „Trotzdem ausliefern?", keine E-Mail ⇒ „Auslieferung per QR (folgt)"; bestätigen → `POST deliver` (AbortController-Timeout) → `router.refresh()`; `emailFailed` ⇒ einmaliger `window.alert`. `<DeliveredBanner>` (Status `sent`, Kopf): grünes Banner „Ausgeliefert am {Datum}" (`--green-*`, 6c) + Vorschau-Link (`/b/[token]?c=1`). Status `sent` = **read-only** (`isDraft=false` ⇒ Capture aus, `MediaList readOnly`, keine Mutations-Controls); die Seite lädt `access_token`+`sent_at` jetzt auch für `sent`. i18n `deliver.*`, **kein** Belohnungs-Bezug. `pnpm typecheck` + `pnpm build` grün.

- **Schritt 9c-2 ✅ QR-Druckansicht — Handover am Tresen (Bon-tauglich)** — druckoptimierte, **Bon-Drucker-taugliche** Seite (QR + Kundenname + Hinweis), die den **No-E-Mail-Auslieferungspfad** schließt: scannen statt mailen. Der QR kodiert den **`?c=1`-Kunden-Booklet-Link**. **Keine Migration.** Damit ist 9c (manuelle Auslieferung) vollständig; **View-/Share-Analytics** (`booklet_events`) + Stufen `viewed`/`shared` bleiben offen. **Setup:** deps `qrcode` + `@types/qrcode` (pnpm; reines JS-Paket, **kein** Lifecycle-Script → nicht in `onlyBuiltDependencies`). **QR-Druckseite** (`app/portal/orders/[id]/qr/page.tsx`, Server Component, `force-dynamic`): **ISOLATION (RLS)** — `getCurrentBusiness` (kein Betrieb ⇒ `notFound()`; Middleware schützt `/portal/*` schon vor anonym), Order (`customer_name`,`status`) + Booklet (`access_token`) über den **AUTHENTICATED Client** (RLS-skopiert; fremde/fehlende ⇒ `notFound()`); **Guard:** Booklet muss existieren **und** Status ∈ {`generated`,`sent`} ⇒ sonst `notFound()` (vor der Generierung kein Link zum Drucken). **Link** `customerUrl = ${BOOKLET_BASE_URL}/b/${access_token}?${CUSTOMER_VIEW_QUERY}` — **derselbe `?c=1`-Kunden-Link wie die E-Mail** (9c-1), `CUSTOMER_VIEW_QUERY` aus `lib/booklet/customer-view.ts` (9d); Basis-URL bevorzugt `BOOKLET_BASE_URL`, sonst Request-Origin (x-forwarded-host/proto) — **identische Logik** zur deliver-Route. **QR SERVER-SEITIG** via `QRCode.toString(url,{type:'svg',errorCorrectionLevel:'M',margin:1,width:256,color:{dark:'#000',light:'#fff'}})` → per `dangerouslySetInnerHTML` eingebettet (scharf dank viewBox + `crispEdges`, **kein** Client-JS für den Code; `'M'` robust gegen Druck-/Scan-Fehler; server-generiert + nicht nutzergesteuert ⇒ XSS-unkritisch). **Druck-Karte** (schmal + S/W, `.qr-card` in `globals.css`): Betriebsname, „Für {customer_name}", QR (CSS-fix 240px, SVG füllt Container), kurzer Hinweis. **Kein Logo** (Thermodruck = S/W, kein Verlass auf Farbe/Logo; Betriebsname als Text = verlässlicher Identifier). **Druck-Verhalten:** Client-Button „Drucken" (`qr-print-button.tsx`, `window.print()` — einzige Client-Komponente, damit die Seite Server Component bleibt); **`@media print`** (`globals.css`) blendet Portal-Chrome (`.portal-sidebar`/`.portal-topbar`/`.portal-tabnav`) **und** den Druck-Button (`.qr-no-print`) aus, `.portal-main`-Padding 0, Karte randlos/zentriert ⇒ **nur** die QR-Karte druckt. **Zugang** (`page.tsx`): Link „QR drucken" (`btn-outline`, neuer Tab) auf der Detailseite, sobald ein Booklet existiert (`(isGenerated || isSent) && bookletToken`) → `/portal/orders/[id]/qr`. i18n `qr.*` (`printButton`/`forCustomer {name}`/`hint`), keine Inline-Strings. `pnpm typecheck` + `pnpm build` grün.

- **Schritt 10a ✅ Analytics Write-Pfad — booklet_events + Lifecycle viewed/shared** — erfasst Booklet-Interaktionen in `booklet_events` und rückt den Auftragsstatus monoton vorwärts (`sent → viewed → shared`). **Keine Migration** (`booklet_events` + `event_type`/`channel`/`ip_hash` aus 0001, Status-Enum aus 0001). Das **Dashboard** (10b) bleibt offen. **Event-Taxonomie** (`event_type`/`channel`, geteilt in `lib/booklet/events.ts` — **plain**/secret-frei → client-importierbar): `viewed`/— (Seite geöffnet), `shared`/`reel`|`story`|`whatsapp`|`copy` (Teilen-Wege), `link_click`/`website`|`review`|`ig` (Outro-Website/Google-Review/IG-Caption). `event_type` bleibt im 0001-Wertebereich, die Spezifik trägt der `channel`. `parseBookletEvent(eventType, channel)` prüft Typ **und** erlaubte Kombination gegen eine feste Whitelist (`null`-channel nur für `viewed`) ⇒ ungültig 400; Client nutzt nur die Typliterale (`import type`), Validierung server-seitig. **Öffentlicher Endpoint** `POST /api/b/[token]/event` (NEU): **`service_role`** + **Token-als-Vertrauensquelle (§14.2)** — `token → booklet → business_id`/`order_id`, NIE ein Client-Wert; ungültiger Token ⇒ **404 kein Write**; Body gegen Whitelist ⇒ sonst **400 kein Write**; **`ip_hash`** = `sha256("{IP_HASH_SALT}:{ip}")` server-seitig (`x-forwarded-for`/`x-real-ip`, `node:crypto`), **nie roh**, ohne IP ⇒ `null`; Insert `{booklet_id,business_id,event_type,channel,ip_hash}` (booklet_events hat **kein** `authenticated`-INSERT-Grant). **Lifecycle** (monoton, defensiv gefiltert, nie zurück/überspringen): `viewed` → `orders 'sent'→'viewed'` (`.eq('status','sent')`) + `booklets.viewed_at` falls null; `shared` → `orders {sent,viewed}→'shared'` (`.in('status',[…])`) + `first_shared_at` falls null; `link_click` rückt **nicht** vor. **Fire-and-forget:** Endpoint antwortet schnell (`{ok:true}`), nicht-kritische Insert-/Update-Fehler nur geloggt. **Client feuert** über geteilten Helfer `lib/booklet/track.ts` (`trackBookletEvent(token, type, channel?)` — POST, **nicht awaitable**, schluckt Fehler, `keepalive:true`, Token aus URL-Pfad, **kein** `business_id` im Client): `<ViewTracker>` (`view-tracker.tsx`, rendert nichts) feuert beim Mount **einmalig** `viewed` (`useRef`-Guard gegen StrictMode-Doppel-Mount) für **ALLE** Öffner (Kunde UND Empfänger, unabhängig von `?c=1` — Reichweite zählt); `share-bar.tsx` feuert je Teilen-Aktion (`shared/reel`|`story`|`whatsapp`|`copy`, `link_click/ig`, `link_click/review`; der interne Copy-Fallback von „Story teilen" feuert **kein** zweites `copy`-Event); `<TrackedLink>` (`tracked-link.tsx`) feuert `link_click/website` beim Outro-Website-Klick (natives `<a>`-Navigieren unverändert). Env `IP_HASH_SALT` (server-only) in `.env.example`. `pnpm typecheck` + `pnpm build` grün.

- **Schritt 10a.1 ✅ No-Track-Marker — betriebs-eigene Booklet-Aufrufe nicht tracken** — verhindert, dass der **Betrieb** beim Öffnen/Testen des eigenen Booklets `viewed`/`shared`-Events auslöst und den Auftragsstatus (`sent → viewed → shared`) fälschlich vorrückt. **Keine Migration.** **Marker `p=1`** in `lib/booklet/events.ts` (Tracking-Domäne, **plain**/secret-frei → client-importierbar): `NO_TRACK_PARAM`/`NO_TRACK_VALUE`/`NO_TRACK_QUERY` (`"p=1"`) + `hasNoTrackMarker(search)` (`URLSearchParams`, `p === "1"`) — **eine Quelle** für Lese-Check (Client) und das Anhängen an Betriebs-Links. **KEIN Auth-Gate** (wie `c=1`/§9d): der `access_token` bleibt der alleinige Zugriffsschutz, `p=1` schaltet **ausschließlich** das Tracking ab, **kein** DB-Zugriff hängt daran. **Unterdrückung zentral** in `lib/booklet/track.ts`: `trackBookletEvent` bricht **früh ab** (`typeof window !== "undefined" && hasNoTrackMarker(window.location.search)` ⇒ `return`) **vor** dem `fetch` → kein Request, kein Event-Insert, **kein** Status-Vorrücken; bewusst im **einen** Helfer statt in den drei Aufrufern (`view-tracker.tsx`/`share-bar.tsx`/`tracked-link.tsx`), sodass kein Aufrufer den Check vergessen kann. **Betriebs-Links mit `&p=1`** (zusätzlich zu `c=1`): Portal-Vorschau-Link (`generate-controls.tsx`, Status `generated`) + „Ausgeliefert"-Banner-Link (`deliver-controls.tsx`, Status `sent`) → `…/b/${token}?c=1&p=1`. **Kunden-Links UNVERÄNDERT** (nur `c=1`, weiter getrackt): E-Mail-Link (9c-1, `deliver/route.ts`) + QR-Link (9c-2, `qr/page.tsx`) — das ist der ganze Punkt: nur betriebs-eigene Aufrufe sind ausgenommen. **Share** teilt weiter die **nackte** URL (§9d) → Empfänger geteilter Links tragen keinen Marker und werden normal getrackt. `pnpm typecheck` + `pnpm build` grün.

- **Schritt 10b ✅ Analytics-Dashboard (Startseite) + Valooro-Logo im Sidebar-Header** — die `/portal`-Startseite (vorher Platzhalter „Willkommen, {name}") wird die **business-weite Analytics-Übersicht**; plus das Valooro-Logo oben in der Sidebar. **Keine Migration** (`booklet_events` + Status-Enum aus 0001). Schließt den Analytics-Write-Pfad (10a/10a.1) mit der **Anzeige** ab; das Aggregat ist bewusst MVP. **TEIL A — Dashboard** (`app/portal/page.tsx`, Server Component): **Daten ausschließlich über den AUTHENTICATED Client** (RLS skopiert auf den Betrieb + defensiver `business_id`-Filter, **KEIN** `service_role`). **Funnel** aus `orders` über **drei head-`count`-Queries** (`select("*", {count:"exact", head:true})`): Ausgeliefert = `status ∈ {sent,viewed,shared}`, Angesehen = `{viewed,shared}`, Geteilt = `shared`. **Engagement** aus **EINER** `booklet_events`-Query (`event_type,channel,ip_hash`) → **JS-Aggregation**: Views total = `count(viewed)`, Views unique = distinct `ip_hash` bei `viewed` (JS-`Set`, `null` zählt nicht), Shares/Kanal = `count(shared)` je `channel` (reel/story/whatsapp/copy), Klicks/Kanal = `count(link_click)` je `channel` (website/review/ig). **Single-fetch + JS-Aggregation ist bewusst MVP** (Doc-Kommentar im Code) — bei Skalierung später SQL-Aggregat/RPC mit REVOKE-EXECUTE-Konvention, jetzt nicht. Funnel- + Events-Queries laufen **parallel** (`Promise.all`). **Darstellung = Zahlen-Karten + CSS-Balken, KEINE Chart-Library:** **Headline** prominent (`.dashboard-hero`, gold) = **Share-Rate** `Math.round(Geteilt/Ausgeliefert*100)` % (Division-Guard `delivered>0`, sonst 0) + die zwei Zahlen darunter — DIE Kernkennzahl der Produkt-These. **Funnel** (Ausgeliefert→Angesehen→Geteilt) als drei `<BarRow>` (CSS-Balken, Breite relativ zu Ausgeliefert). **Shares nach Kanal** + **Klicks** je als `<BarRow>`-Gruppe (Breite relativ zum Gruppen-Max, `Math.max(1,…)` gegen Division durch 0). **Aufrufe** eindeutig vs. gesamt als zwei `<Stat big>` nebeneinander. **Leerer Zustand** (`hasData = delivered>0 || events.length>0` ⇒ sonst freundlicher `dashboard.empty`-Hinweis). Begrüßungs-Header „Willkommen, {name}" (`portal.welcome`) bleibt oben. Desktop-first 2-Spalten-Raster (`.dashboard-grid`), mobil einspaltig stapelnd (Breakpoint 768px, bestehende Tokens). Präsentations-Helfer `<Stat>`/`<BarRow>` server-seitig im selben File. i18n-Block `dashboard.*` (`shareRate`/`shareRateHint`/`funnel`/`delivered`/`viewed`/`shared`/`sharesByChannel`/`clicks`/Kanal-Labels `reel`/`story`/`whatsapp`/`copy`/`website`/`review`/`ig`/`views`/`uniqueViews`/`totalViews`/`empty`), Kanal-Labels via `t(…, \`dashboard.${channel}\`)` (literale Unions wie `reel-state-pill.tsx`), keine Inline-Strings. **TEIL B — Valooro-Logo** im Sidebar-Header (`app/portal/layout.tsx`): `<img className="portal-sidebar-logo" src="/valooro.png" alt={app.name}>` **über** dem Brand-Text (Text bleibt als Fallback). Datei liegt unter **`/public/valooro.png`** (getracktes App-Branding) — **NICHT** die untracked `Logo.png` aus dem Repo-Root verwenden/anfassen; fehlt die Datei, bricht nur das Bild, Build bleibt grün. `eslint-disable @next/next/no-img-element` (statisches Branding-Asset, Muster wie die bestehenden `<img>`-Stellen). CSS: `.dashboard-*` + `.portal-sidebar-logo` in `globals.css`. `pnpm typecheck` + `pnpm build` grün.

- **Schritt 10b.1 ✅ Sidebar-Header aufgeräumt (Logo statt Text + Trennlinie + Abstand)** — reines Layout, **keine Migration, keine Logik**. Im Desktop-Sidebar-Header (`app/portal/layout.tsx`) den `.portal-sidebar-brand`-Brand-Text („Valooro Handwerk") **entfernt** — nur noch das `<img src="/valooro.png">`-Logo (kein Text-Fallback mehr); darunter eine dezente **Trennlinie** (`<hr className="portal-sidebar-divider">`, 1px `var(--border)`, volle Sidebar-Breite minus 20px-Padding) + Abstand zu den `PortalNav`-Menüpunkten. CSS (`globals.css`): `.portal-sidebar-brand` gelöscht, `.portal-sidebar-logo`-`margin-bottom` entfernt (Divider trägt jetzt den Abstand: `margin: 16px 20px 20px`). **Mobile Top-Bar unverändert** (zeigt weiter `business.name`). `pnpm typecheck` + `pnpm build` grün.

- **Root-Route Redirect ✅** — `app/page.tsx` leitet per `redirect('/portal')` (next/navigation, Server Component) sofort auf `/portal` weiter. `handwerk.valooro.com` landet damit direkt im Portal (eingeloggt) bzw. wird von der Middleware auf `/login` umgeleitet (nicht eingeloggt).

- **Self-Service-Registrierung (Option C) ✅** — neue Betriebe registrieren sich selbst und starten auf **`status='pending'`**; das Portal bleibt gesperrt, bis ein Admin **manuell** freischaltet (`update businesses set status='active' where business_email='…';`). **Migration 0005** ([0005_pending_status.sql](supabase/migrations/0005_pending_status.sql) + Verify): erweitert **nur** den `businesses_status_check` um `'pending'` (`drop`/`add constraint`), **DEFAULT bleibt `'active'`** (bestehende Betriebe unverändert aktiv), keine neue Tabelle/Policy/GRANT. **Registrierungs-Seite** ([app/register/page.tsx](app/register/page.tsx), Client, `div + onClick`, kein `<form>`, Optik wie /login): Betriebsname/E-Mail/Passwort(≥8)/Wiederholung, Client-Validierung, dann (1) `supabase.auth.signUp` → (2) `POST /api/auth/register { businessName, email, userId }` → (3) Erfolgsmeldung (**kein** Auto-Login/Portal-Redirect); Link „Bereits registriert? Anmelden". **Route Handler** ([app/api/auth/register/route.ts](app/api/auth/register/route.ts), `POST`, **`service_role`**, bewusst **nicht** unter `/portal`): Validierung (Felder/Name ≤ 100/E-Mail-Format ⇒ 400), `business_email` existiert ⇒ **409 `email_taken`**, `slug` = slugify(name, ≤50) + zufällige 4-stellige Zahl, Insert `businesses` (`status:'pending'`, `default_language:'de'`; `23505` ⇒ 409) + `business_users` (`role:'owner'`). **Rollback:** `business_users.user_id`-FK auf `auth.users` — erfundener `userId` scheitert ⇒ `businesses`-Insert wird **zurückgerollt** (kein verwaister Betrieb, der die E-Mail dauerhaft mit 409 blockiert). **Admin-Mail (NON-FATAL)** via Resend an **fest hinterlegte** `andreas.dax@valooro.com` ([lib/email/admin-notification.ts](lib/email/admin-notification.ts), Betreff „Neue Registrierung: {name}" + Freischalt-SQL; **kein `ADMIN_EMAIL`-Env**). **pending = gesperrt (zweistufig):** (1) **[middleware.ts](middleware.ts)** prüft für eingeloggte Nutzer auf Portal-**Seiten** (`startsWith('/portal')`) den Betriebs-Status (`business_users → businesses(status)`, RLS, fail-safe: bei Fehler/null **keine** Umleitung) ⇒ `pending` → `redirect('/pending')` (früh, vor dem Layout-Render). (2) **[getCurrentBusiness](lib/auth/current-business.ts)** ⇒ `redirect('/pending')` bei `status==='pending'` — Backup für Seiten **und** der eigentliche Guard für alle `/api/portal/*`-Routen (rufen es ebenfalls, fallen aber nicht unter den `/portal`-Match der Middleware; 307, keine Mutation). `/pending`+`/register` liegen außerhalb `/portal` ⇒ keine Schleife. **[app/pending/page.tsx](app/pending/page.tsx)** ruft `getCurrentBusiness` **NICHT** auf (Schleifen-Schutz), nur `auth.getUser()` + geteilter `LogoutButton`. i18n `register.*`/`pending.*` + `login.noAccount`/`login.register`. `pnpm typecheck` + `pnpm build` grün.

- **Inbound-Webhook / roapp-Connector (§12) ✅ LIVE — beide Events end-to-end verifiziert** — `order.created` legt den Auftrag automatisch als `draft` an (Name/E-Mail aus roapp-API, `consent_given=false`, `external_ref=id_label`); `order.picked_up` schickt die Booklet-Mail **nur** bei API-`status.name === "Abgeholt"` (Zwischenstatus wie „Fertig zur Abholung" ⇒ No-op `noop_status`); Auth via Pfad-Secret, Anreicherung über **einen** roapp-API-Call, Doppelversand-Schutz über defensiven Status-Filter + `count`-Check. **Offene Folgeschritte** (Details TECH.md): (1) `x-signature`/HMAC noch ungeprüft (Pfad-Secret = einzige Auth); (2) `item_description` bleibt bei Auto-Anlage leer (roapp-`custom_fields`-Mapping pro Betrieb offen); (3) `ROAPP_API_KEY` global/Single-Tenant ⇒ bei Betrieb #2 auf pro-Betrieb-Spalte umstellen. — vendor-neutraler Inbound-Webhook, der Aufträge **automatisch anlegt** (`order.created`) und **ausliefert** (`order.picked_up`); additiv zum manuellen Pfad. **Keine Migration** (`businesses.webhook_secret` aus 0001). **Endpoint** [app/api/webhook/[secret]/route.ts](app/api/webhook/[secret]/route.ts) (`POST`, bewusst **nicht** unter `/portal` → kein Session-Kontext; Middleware lässt ihn durch, Redirects greifen nur für `/portal`): **AUTH = Pfad-Secret (§14.2)** `[secret]` → `businesses.webhook_secret` → `business_id` (über **`service_role`**), diese `business_id` ist die **EINZIGE Vertrauensquelle**, **NIE** aus dem Payload; alle DB-Zugriffe `service_role`, strikt darauf gescoped. **ROBUSTHEIT (§12):** ungültiges/fehlendes Secret ⇒ **404** (einziges hartes Gate), **alles andere 200 + Status-String** (`{status}`), damit roapp **keine Retry-Stürme** macht; No-op-/Fehlerfälle geloggt (`lookup_failed`/`bad_body`/`ignored_event`/`no_object_id`/`enrich_failed`/`already_exists`/`insert_failed`/`noop_status`/`order_not_found`/`not_generated`/`already_sent`/`no_booklet`/`status_failed` + Erfolge `created`/`sent`/`sent_no_email`). **Klassifizierung** ([lib/roapp/events.ts](lib/roapp/events.ts), **PLAIN**/secret-frei, vendor-neutral): `parseWebhookBody` liest `event_name` (Fallback `event`/`type`) + `context.object_type`/`object_id` (Fallback Top-Level, number→string); `classifyEvent` nur für `object_type==='order'` → `.created` ⇒ `created`, `.status.changed` ⇒ `picked_up`. **WICHTIG:** numerische Payload-Status-ID (`metadata.new.id`) wird **ignoriert** — sie ≠ API-`status.id`; Status entscheidet der **API-`status.name`**. **Anreicherung** ([lib/roapp/client.ts](lib/roapp/client.ts), server-only, kein `any`): `getRoappOrder` = **EIN** Call `GET https://api.roapp.io/orders/{object_id}` mit `Authorization: Bearer ${ROAPP_API_KEY}` (AbortController-Timeout 10 s) → Antwort enthält eingebettetes `client` (email/first_name/last_name/name) + `status.name` + `id_label` (= `external_ref`), **kein** zweiter Contact-Call; **wirft** bei fehlendem Key/Nicht-2xx/Timeout/JSON-Fehler ⇒ Endpoint 200 `enrich_failed`. `parseRoappOrder` entpackt optionale `{data:…}`-Hülle; `ROAPP_PICKED_UP_STATUS_NAME` (Env, Default `"Abgeholt"`). **Branch `order.created`** (wie [orders/route.ts](app/api/portal/orders/route.ts), aber `service_role`): `customer_name` = first+last → `client.name` → `external_ref` → `"Kunde"` (NOT NULL), `customer_email`=`client.email`, `external_ref`=`id_label`, `language`=`business.default_language`, `status='draft'`; **§13.5:** `consent_given` **IMMER false** / `consent_at` **null** (Consent gehört an den Tresen); `item_description=null` (roapp-Custom-Field-IDs pro Betrieb verschieden, kein Mapping); **IDEMPOTENT** über `external_ref` ⇒ existiert schon ⇒ `already_exists` (Dedup nur bei vorhandenem `external_ref`). **Branch `order.picked_up`** (deliver-Pfad **REPLIZIERT, nicht geteilt** — Portal-[deliver/route.ts](app/api/portal/orders/[id]/deliver/route.ts) ist session-gebunden): nur wenn **API-`status.name === ROAPP_PICKED_UP_STATUS_NAME`** ⇒ sonst `noop_status`; Order per `external_ref`+`business_id` (sonst `order_not_found`); nur `generated` liefert aus (draft/finalized ⇒ `not_generated`, sent/viewed/shared ⇒ `already_sent`); Status `generated→sent` **defensiv gefiltert + `count`-Check** gegen Doppelversand (`count===0` ⇒ `already_sent`, keine Nebenwirkungen); dann **nicht-blockierend** wie im Vorbild: `booklets.sent_at`, **Billing-Event `'booklet_sent'`** (`service_role`, da `billing_events` kein `authenticated`-INSERT-Grant hat), **Booklet-E-Mail** via [sendBookletEmail](lib/email/booklet-email.ts) (Link `${BOOKLET_BASE_URL}/b/${access_token}?${CUSTOMER_VIEW_QUERY}`, `replyTo=settings.contact_email`). **Env:** `ROAPP_API_KEY` (server-only, neu) + optional `ROAPP_PICKED_UP_STATUS_NAME` in `.env.example`; `BOOKLET_BASE_URL` existierte schon. **Secret setzen** (ops, keine Migration): [supabase/scripts/set_webhook_secret.sql](supabase/scripts/set_webhook_secret.sql) (`gen_random_uuid()` per `business_email`, nur wenn `webhook_secret is null` ⇒ überschreibt nie + liest aus; URL `https://handwerk.valooro.com/api/webhook/<secret>`). **`x-signature` (HMAC) NICHT geprüft** — Pfad-Secret ist die Auth fürs MVP, HMAC-Prüfung als Folgeschritt in TECH.md notiert. `pnpm typecheck` + `pnpm build` grün.

- **Schritt 11 ✅ analytics_events-Fundament (Migration 0006) + roapp-Description-Mapping + Intro-Umbau** — drei zusammenhängende Änderungen. **(1) Migration 0006** ([0006_analytics_events.sql](supabase/migrations/0006_analytics_events.sql) + [Verify](supabase/verify/0006_analytics_events_checks.sql)): neue Tabelle `analytics_events` (`business_id`, `event_type`, `source`, `external_ref`, `payload jsonb`, `created_at`; Indizes `(business_id,created_at)`/`(business_id,event_type)`) — generisches, betriebs-skopiertes **Sach-Event**-Fundament (weder `billing_events` noch `booklet_events`). RLS-/GRANT-Stil **EXAKT wie billing_events**: eine SELECT-Policy für Mitglieder, **kein** `authenticated`-INSERT/UPDATE/DELETE (nur `service_role` schreibt), `revoke from anon/public`. **ZWEI nicht verhandelbare Eigenschaften** (Migrations-Kommentar + TECH.md): **RETENTION-FEST** (vom Retention-Löschjob §13.4 NIEMALS gelöscht — aktuell existiert noch kein Cleanup-Job; wird einer gebaut, MUSS er die Tabelle ausnehmen) und **PII-FREI** (`payload`/`external_ref` enthalten NIE Name/E-Mail/Telefon, nur Sachtext + technische Referenz — Bedingung für die Retention-Ausnahme). Offener Folgeschritt: weitere Module schreiben eigene `event_type`s in **dieselbe** Tabelle. **(2) roapp-Description** ([lib/roapp/client.ts](lib/roapp/client.ts)): `RoappOrder.raw_description` (string|null) aus `custom_fields[<ID>]`, Feld-ID **nicht hardcodet** → Env `ROAPP_DESCRIPTION_FIELD_ID` (Default `"f842212"`, Atelier Dax); defensives Parsen (`readCustomField`, deckt String **und** `{value}`-Objekt ab, **kein `any`**). `.env.example` ergänzt. **(3) Webhook handleCreated** ([app/api/webhook/[secret]/route.ts](app/api/webhook/[secret]/route.ts)): nach dem Idempotenz-Check `item_description = raw_description` (ROH, unverändert; `consent_given` bleibt false §13.5) **+** bei nicht-leerem Roh-Text ein `analytics_events`-Insert via `service_role`, **NICHT-BLOCKIEREND** (wie der Billing-Insert im deliver-Pfad): `business_id` aus dem aufgelösten Betrieb (§14.2, NIE Payload), `event_type='order_description'`, `source='roapp'`, `external_ref=id_label`, `payload={raw_text}`; PII-frei; bei `already_exists` greift der frühe Return ⇒ kein Doppel-Insert. **(4) Intro-Umbau** ([lib/ai/intro.ts](lib/ai/intro.ts)): weg von der Ich-Perspektive — das Modell schreibt den **GANZEN Absatz inkl. persönlicher Anrede** als **einen** zusammenhängenden Text (kein hartkodierter Anrede-Satz + KI-Text konkateniert). Neue Inputs: `customerName` → **Vorname** (erster Namensteil, **WÖRTLICH**, nie korrigiert — „Doroyhea" bleibt „Doroyhea"; kein Vorname ⇒ Anrede ohne Namen) + `itemDescription` als gefilterter Kontext; `businessName` als Intro-Input **entfernt**. Prompt: TITEL (≤ ~6 Wörter, keine Anrede/Zahlen) + ABSATZ (Anrede „Hallo {Vorname}," → Einleitungssatz mit Kleidungsstück **nur wenn aus der Notiz eindeutig**, sonst neutral „an deiner Kleidung" → Detailteil aus Kundensicht „deine/dein"); Notiz-Regeln (nur Art der Arbeit, **Zahlen/Maße/Kürzel strikt ignorieren** — keine Zahl im Text, Wortlaut nicht übernehmen); leerer Fall ⇒ Notiz-Block ganz weg. **`title`/`description` bleiben** (`booklets.intro_title`/`intro_description`; Web-Story-Heading, Reel-Intro-Frame, IG-Caption `buildIgCaption` hängen am Title — `description` trägt jetzt den vollen Anrede-Absatz); `max_tokens` 300→400. `generate`-Route lädt `customer_name` + reicht `customerName` durch; greift nur bei **NEU** generierten Intros. **Tailor-Framing bewusst** (Prompt auf Schneideratelier zugeschnitten; aktueller Tenant Atelier Dax, `ai_context` erdet Fachsprache) — für nicht-textile Betriebe später zu generalisieren. **Migration 0006 muss vor dem Live-Gang manuell im Supabase-SQL-Editor angewendet werden.** `pnpm typecheck` + `pnpm build` grün.

- **Schritt 12 ✅ KI-Kurzbeschreibung auf der Auftragskachel (Migration 0007)** — sehr kurzer, KI-generierter Einzeiler (Art der Arbeit, ~3–6 Wörter) auf jeder Auftragskachel, damit Mitarbeiter in der Liste auf einen Blick sehen, worum es geht. **EINMALIG bei der Anlage** aus der Roh-Beschreibung via Haiku erzeugt + gespeichert — **NICHT beim Rendern** der Liste (sonst ein KI-Call pro Kachel pro Aufruf). **(1) Migration 0007** ([0007_order_short_summary.sql](supabase/migrations/0007_order_short_summary.sql) + [Verify](supabase/verify/0007_order_short_summary_checks.sql)): eine Spalte `orders.short_summary text default null` (nullable) + `comment on column`; **keine** neue Policy/GRANT/Index (0001-`orders`-RLS `for all` deckt Lesen/Schreiben). Bewusst nullable: scheitert/entfällt die Generierung ⇒ `null` ⇒ Kachel-Platzhalter. **(2) Generator** ([lib/ai/short-summary.ts](lib/ai/short-summary.ts), server-only): `generateShortSummary(rawDescription, language) → string | null` via **Haiku 4.5** (`HAIKU_MODEL`, schlichter `messages.create`, `max_tokens 48`, wie Captions 6b); **leerer Input ⇒ sofort null** (kein sinnloser Call); Sprache = `orders.language` (§15); Prompt-Regeln **analog Intro** (nur Art der Arbeit, Stichwort-Fragment; Preise/Zahlen/Maße/Kürzel strikt ignorieren, **keine Zahl im Output**; Wortlaut nicht übernehmen); `cleanSummary` (Quotes/Punkt strippen, Cap `SHORT_SUMMARY_MAX_LENGTH=60`), leer nach Cleanup ⇒ null. **(3) Beide Anlage-Pfade, nicht-blockierend** (Haiku-/Update-Fehler ⇒ geloggt, Auftrag bleibt angelegt, `short_summary` bleibt null): **manuell** ([app/api/portal/orders/route.ts](app/api/portal/orders/route.ts)) generiert **vor** dem Insert (try/catch) und schreibt `short_summary` **in den Insert mit** (AUTHENTICATED Client, kein `service_role`) — vor-Insert, weil nicht idempotenz-gegated + Redirect-zur-Liste soll sofort den Kurztext zeigen; **Webhook** ([app/api/webhook/[secret]/route.ts](app/api/webhook/[secret]/route.ts), `handleOrderCreated`) gibt jetzt die Insert-`id` zurück und macht **nach** Insert+`analytics_events` (nur bei `raw_description`) ein **`service_role`-Update** strikt auf die neue Order + `business_id` (§14.2, NIE Payload) — Update-nach-Insert, weil idempotenz-gegated ⇒ Haiku-Call nur für genuin neue Aufträge, nie für `already_exists`. **(4) Kachel** ([app/portal/orders/page.tsx](app/portal/orders/page.tsx)): lädt `short_summary` (AUTHENTICATED, RLS) und zeigt **nur das gespeicherte Feld** (KEINE Live-Generierung) unter dem Kundennamen — gesetzt ⇒ Kurztext (`--text-primary`, einzeilig/Ellipsis), sonst **dezenter Platzhalter** „Keine Beschreibung" (`orders.noDescription`, `--text-secondary`+italic+`opacity .7`). i18n `orders.noDescription`. **Migration 0007 muss vor dem Live-Gang manuell im Supabase-SQL-Editor angewendet werden.** `pnpm typecheck` + `pnpm build` grün.

- **Block A ✅ UI-Verbesserungen (Teilen-Sektion + Medien-Aktionen)** — reine Frontend-Politur, **keine** DB/Migration/Webhook-Berührung. **(Punkt 9)** Google-Bewertungs-Button im Outro-Share-Sheet (`app/b/[token]/share-bar.tsx`) bekommt einen farbigen **„Google"-Wortmarken-Schriftzug** (G blau, o rot, o gelb, g blau, l grün, e rot — pro Buchstabe ein `<span>` mit Brand-Hex, `aria-hidden`; Komponente `GoogleWordmark` ersetzt `StarIcon`) — sofort erkennbar, **ohne** offizielles Logo-Asset; Button als **weißer, leicht erhobener** Button hervorgehoben. i18n `review.button` → „Bewertung schreiben" (das „Google" trägt der Schriftzug); `review.hint` erklärt den Clipboard-Mechanismus (Entwurf wird bei Klick kopiert → nur noch einfügen). **§8.6** eingehalten (Vorschlag/in deinen Worten, **kein** Belohnungs-Bezug). **(Punkt 10)** Die `.booklet-share`-Sektion (`booklet.css`) wird eine **helle Karte** (`rgba(255,255,255,0.88)`, `border-radius`, Schatten, `backdrop-filter: blur`) — lesbar über unruhigem Outro-Hintergrund; innerhalb der Karte **dunkle Schriftfarben** (lokale Vars `--share-ink`/`--share-ink-soft`/`--share-line`: Heading, Sekundär-Buttons, IG-Panel, Review-Hint umgestellt; Reel-Gold-Hauptaktion unverändert). Kompakt (`max-width: 360px`), nimmt nicht die ganze Seite ein. **TODO-Kommentar** (NICHT gebaut): Karten-Deckkraft/-Farbe künftig pro Betrieb konfigurierbar. **(Punkt 11)** `share.message` (WhatsApp + `navigator.share`-Text) → fester, schlichter Text ohne dynamischen Kontext, neugierig statt spammy; Sprache = Booklet-Sprache. **(Punkt 13)** Medien-Aktionszeile (`app/portal/orders/[id]/media-list.tsx` + `globals.css`): „Halten zum Verschieben" (`assembler.reorderHint`) raus aus der Zeile → dezenter, zentrierter Hinweistext (`.media-reorder-hint`) **UNTER** dem Raster; Aktionszeile (`.media-actions`) nur noch **zwei gleich breite Buttons** (Alle auswählen `btn-outline` + Captions generieren `btn-dark`, `flex: 1 1 140px` + `flex-wrap` ⇒ stapeln statt überlaufen auf ~380px); Auswahl-Zähler als kleine Zeile darüber. `pnpm typecheck` + `pnpm build` grün.

- **Block B ✅ Settings-/E-Mail-/Listen-/Connector-Politur** — vier kleine Portal-Funktionen, **keine Migration** (alles über bestehende Spalten/`settings`-jsonb), **kein Webhook-Pfad geändert**, `business_id` weiter nur aus Session/RLS. **(Punkt 6)** `settings.contact_email` (existierte + war bereits `replyTo` im Versand, hatte aber nur ein Feld in „Booklet-Inhalt") jetzt **ganz oben in der „Betrieb"-Gruppe** beim Firmennamen, Label „Kontakt-E-Mail (für Antworten der Kunden)" + Hinweis (darf von Login abweichen); altes Feld + i18n-Key `settings.content.contactEmail` **entfernt** (eine Quelle). **Default beim ersten Anzeigen** = Login-E-Mail `businesses.business_email`, falls `contact_email` noch null ⇒ `replyTo` greift ab dem ersten Speichern; `getCurrentBusiness` lädt dafür `business_email` (neues Feld in `CurrentBusiness`/`BusinessRow`/Select/Return). **(Punkt 2)** `sendBookletEmail` bekommt optionalen `websiteUrl`; der **manuelle** Deliver-Handler reicht `settings.website_url` durch → Website (Betriebsname verlinkt · klickbare Domain) als seriöser Abschluss unter der Signatur (HTML + Plaintext), Helfer `normalizeWebsite` (Protokoll ergänzen/strippen), leer ⇒ kein Block. **Optional** ⇒ Webhook-Deliver-Pfad **unangetastet** (ohne `websiteUrl`). **(Punkt 5)** Status-Filter (`<select>`, EINE Auswahl: „Alle" Default + alle Status) **über** der Auftragsliste; Statusliste jetzt Laufzeit-Array `ORDER_STATUSES` in `order-status-badge.tsx` (Typ `OrderStatus` daraus abgeleitet, neuer Guard `isOrderStatus`). Liste lädt server-seitig ⇒ Filter auch **server-seitig**: `page.tsx` liest `searchParams.status` (Next-15-`Promise`), validiert gegen `ORDER_STATUSES`, hängt bei gültig `.eq("status", …)` an die Query; Client-Komponente `<OrderStatusFilter>` navigiert per `router.push("?status=…")` (kein `<form>`). Leeres Filter-Ergebnis ⇒ Hinweis (`orders.emptyFiltered`), Leiste bleibt. **(Punkt 3)** Toggle „roapp-Connector aktivieren" → `settings.connector_roapp_enabled` (boolean, `settings`-jsonb); **Default AN für bestehende Betriebe** (`normalizeSettings` + PATCH behandeln nur expliziten `false` als „aus": `!== false`), damit sich das live-Webhook-Verhalten nicht still ändert — Flag steuert **aktuell nur die Button-UX** (Webhook bewusst unangetastet, funktionales Gaten = Folgeschritt). Deliver-Button (`<DeliverButton>`, Prop `connectorEnabled`): **AN** ⇒ prominent (`btn-gold`) + **Safe-Mode-Rückfrage** (`deliver.connectorActive`) vor dem Senden; **AUS** ⇒ sekundär (`btn-dark`, dezent) + normale Bestätigung; Reel-/E-Mail-Hinweise in beiden Fällen erhalten (kein harter Block), kein `<form>`. i18n `settings.contactEmail`/`contactEmailHint`/`connectorRoapp`/`connectorRoappHint`, `orders.filterLabel`/`filterAll`/`emptyFiltered`, `deliver.connectorActive`. `pnpm typecheck` + `pnpm build` grün.

- **Block C / Schritt 1 ✅ Kurzlink `/s/<code>` für Booklets (Migration 0008)** — statt des langen kryptischen Links (`/b/<24-Byte-Token>?c=1`) **überall, wo der Link geteilt/kopiert/versendet wird** (E-Mail, WhatsApp-Share, Kopieren, QR) ein kurzer `https://handwerk.valooro.com/s/<code>`, der **serverseitig** aufs echte Booklet weiterleitet; die interne Route `/b/[token]` bleibt unverändert erreichbar. **(1) Migration 0008** ([0008_booklet_short_code.sql](supabase/migrations/0008_booklet_short_code.sql) + [Verify](supabase/verify/0008_booklet_short_code_checks.sql)): Spalte `booklets.short_code text` (nullable) **mit inline-`unique`** — das UNIQUE erzeugt **zugleich den B-Tree-Index** für den Redirect-Lookup (`where short_code=$1`), **kein** separater Index; mehrere NULLs erlaubt (alte Booklets kollidieren nie). **Nullable, kein Backfill:** alte Booklets fallen auf den langen Link zurück, neue bekommen einen Code; **keine** neue Policy/GRANT (`booklets`-RLS + `service_role` decken die Spalte). **(2) Code-Generierung** ([lib/booklet/short-code.ts](lib/booklet/short-code.ts), server-only): `generateShortCode()` = 7 Zeichen aus **56-Symbol-Alphabet ohne verwechselbare Zeichen** (`0/O`,`1/l/I` raus), ≈1.7e12 Kombinationen — nur ein Lookup-Schlüssel (nicht sicherheitskritisch wie `access_token`), Eindeutigkeit per DB-UNIQUE **+ Retry**. **Kollisions-sicherer Insert** ([generate/route.ts](app/api/portal/orders/[id]/generate/route.ts), Insert-Zweig): Schleife (max. `SHORT_CODE_MAX_ATTEMPTS=5`), bei `23505` **auf `short_code`** (Code + `short_code` in `message`/`details`) neuer Code + Retry, jeder andere Fehler bricht ab (`order_id`-Kollision via `existing`-Check ausgeschlossen, `access_token`-Kollision bei 24 Byte ~unmöglich). Re-Generate (Update-Zweig) lässt `short_code` **unberührt**. **(3) Redirect-Route** ([app/s/[code]/page.tsx](app/s/[code]/page.tsx), Server Component, `force-dynamic`, **öffentlich/keine Session**, nicht unter `/portal`): `short_code`-Lookup via **`service_role`** (analog `/b/[token]`-Read), **liest NUR** `access_token`, `redirect()` (307) auf `/b/<access_token>`; nicht gefunden ⇒ `notFound()` (saubere 404, kein Stacktrace; **Page** statt Route-Handler bewusst dafür). **Token bleibt alleiniger Zugriffsschutz** (§14.2), Code kein Auth-Gate. **(4) §9d gewahrt (Entscheidung „§9d wahren, alles kürzen"):** der Redirect **reicht den Marker `?c=1` durch** (`isCustomerViewParam` wiederverwendet) — markierter Kurzlink (E-Mail/QR) ⇒ Kunden-Sicht (Teilen-Sektion), **nackter** Kurzlink (vom Kunden geteilt) ⇒ Empfänger-Sicht (ohne Bewertungs-Prompt, §8.6/Google-ToS). **(5) Zentraler Helfer** ([lib/booklet/share-link.ts](lib/booklet/share-link.ts), plain/secret-frei): `bookletShareLink({base,accessToken,shortCode,customerView})` → `shortCode` ⇒ `${base}/s/<code>`, sonst Fallback `${base}/b/<token>`; `customerView` hängt `?c=1` an. Eingesetzt in **E-Mail** ([deliver/route.ts](app/api/portal/orders/[id]/deliver/route.ts) + Webhook-Deliver [webhook/[secret]/route.ts](app/api/webhook/[secret]/route.ts), beide laden `short_code`, `customerView:true`; `booklet-email.ts` nutzt die eine URL für Button **und** sichtbaren Text), **QR** ([qr/page.tsx](app/portal/orders/[id]/qr/page.tsx), `customerView:true`) und **Share-Bar** ([load.ts](lib/booklet/load.ts) reicht `shortCode` durch → [b/[token]/page.tsx](app/b/[token]/page.tsx) baut `storyUrl` nackt, `customerView:false`; `share-bar.tsx` unverändert). Portal-Vorschau-Links (`&p=1` No-Track) **nicht** gekürzt (Redirect reicht `p=1` nicht durch). **Migration 0008 muss vor dem Live-Gang manuell im Supabase-SQL-Editor angewendet werden.** `pnpm typecheck` + `pnpm build` grün.

- **Block C / Schritt 2 ✅ Warn-Badge „Abgeholt, aber Booklet nicht versendet" (Migration 0009)** — meldet roapp einen Auftrag als **„Abgeholt"**, OBWOHL bei uns noch **kein Booklet versendet** wurde (Auftrag noch `draft`/`finalized`), erscheint auf der Auftragskachel ein **roter Warn-Badge**: der Kunde hat abgeholt, aber kein Booklet bekommen — der Betrieb soll es fertigmachen und manuell senden. **(1) Migration 0009** ([0009_order_pickup_pending.sql](supabase/migrations/0009_order_pickup_pending.sql) + [Verify](supabase/verify/0009_order_pickup_pending_checks.sql)): Spalte `orders.picked_up_at timestamptz default null` (nullable) + `comment on column`; **keine** neue Policy/GRANT (`orders`-RLS `for all` aus 0001 + `service_role` decken die Spalte). Kein Wert = keine Warnung. **(2) Flag-Logik im Webhook** ([handlePickedUp](app/api/webhook/[secret]/route.ts), KERN): nachdem die Order per `external_ref` gefunden ist und `status.name === "Abgeholt"` feststeht, nach **unserem** Auftragsstatus verzweigen — additiv, der defensive Doppelversand-Schutz `generated→sent` bleibt unangetastet: `generated` ⇒ ausliefern (wie bisher, **kein** Flag); **`draft`/`finalized`** ⇒ `picked_up_at = now()` (`service_role`, Order + `business_id` gescoped, NIE Payload §14.2; nicht-blockierend, idempotent), **keine Mail** ⇒ Response `flagged_pickup_pending`; **`sent`/`viewed`/`shared`** ⇒ **nichts** (kein Flag, keine Mail; Reparatur-Rückläufer darf nicht warnen / doppelt mailen) ⇒ `already_delivered_noop`; nicht gefunden ⇒ `order_not_found` (keine Karte ⇒ kein Badge möglich). **(3) Flag-Reset beim Versand:** der manuelle Deliver-Pfad ([deliver/route.ts](app/api/portal/orders/[id]/deliver/route.ts)) setzt im `generated→sent`-Update `picked_up_at = null` mit (Badge verschwindet beim Nachversand); der Webhook-Deliver-Zweig (`generated→sent`) ebenso sicherheitshalber. **(4) Badge** ([pickup-pending-badge.tsx](components/pickup-pending-badge.tsx) + [orders/page.tsx](app/portal/orders/page.tsx)): Liste lädt zusätzlich `picked_up_at` (AUTHENTICATED, RLS), rendert `<PickupPendingBadge>` im Badge-Wrap neben dem Status-Badge — **nur** wenn `picked_up_at` gesetzt **UND** `status NOT IN (sent, viewed, shared)` (**Doppel-Sicherung** gegen ein theoretisch nicht zurückgesetztes Flag). Roter Pill (`--red-*`), Text „Abgeholt – Booklet nicht versendet" + dezenter `abgeholt am TT.MM.`-Zusatz; **nicht klickbar**, rein visuell; Server-Component-fähig (Muster wie `order-status-badge.tsx`). i18n `orderStatus.pickupPending`/`pickupPendingDate`. **Migration 0009 muss vor dem Live-Gang manuell im Supabase-SQL-Editor angewendet werden** (sonst scheitert das `picked_up_at`-Update im Webhook — non-fatal, aber der Badge erscheint nie). `pnpm typecheck` + `pnpm build` grün.

- **Block C / Schritt 3 ✅ Pagination + Quick-Filter-Leiste der Auftragsliste** — die Auftragsliste ([app/portal/orders/page.tsx](app/portal/orders/page.tsx)) bekommt **server-seitige Pagination** (20 Karten/Seite) + drei **Quick-Filter** neben dem Status-Dropdown (Block B). **Keine Migration, kein Webhook berührt** — reine Lese-/Filter-Logik; `business_id` weiter nur aus Session (AUTHENTICATED Client, RLS). Server Components default; Pagination + Quick-Filter `<Link>`-basiert (kein Client-State, **kein `<form>`**). **(1) Geteilte Quelle** ([lib/orders/filters.ts](lib/orders/filters.ts)): `QuickFilter`-Typ + `isQuickFilter`-Guard + `ORDERS_PAGE_SIZE = 20` + `buildOrdersUrl({status?,quick?,page?})` (eine URL-Logik für Pagination-Links **und** Overshoot-Redirect; `page` erst ab 2 angehängt). **(2) Zwei Filter-Achsen, mutually exclusive:** `?status=` (Dropdown, ein Status) vs. `?quick=` (Mehrfach-/Sonderbedingungen — eigener Param, **nicht** über `?status=`). **Quick hat Vorrang** (`activeStatus = !activeQuick && isOrderStatus(...)`) ⇒ bei aktivem Quick zeigt das Dropdown sichtbar „Alle"; jede Navigation baut eine **frische URL** (Quick-Link → nur `?quick=`, Dropdown-`onChange` → nur `?status=`), daher nie beides gleichzeitig aktiv, „zuletzt benutzter gewinnt" automatisch. **(3) Drei Quick-Filter** ([components/order-quick-filters.tsx](components/order-quick-filters.tsx), Server Component, `<Link>`): inaktiv → `?quick=<key>`, **aktiv → `/portal/orders`** (Toggle aus), genau einer aktiv (`data-active`/`aria-current`); Filterwechsel droppt `?page=` ⇒ Seite 1. Definitionen (server-seitig in WHERE übersetzt): **`flagged`** „Geflaggt" = `picked_up_at` gesetzt UND `status IN (draft,finalized,generated)` (exakt die Warn-Badge-Bedingung aus Schritt 2; `NOT IN {sent,viewed,shared}` == `IN {draft,finalized,generated}` ⇒ via `.in(...)`, kein `.not(...,"in",...)`-Quoting), **`drafts`** „Entwürfe" = `status='draft'`, **`ungenerated`** „Nicht generiert" = `status IN (draft,finalized)`. Stil: dezente, **gold-umrahmte** Chips (`.quick-filter`, `--gold-border`; aktiv `--gold-light`+`--gold`+`#8A7320`). **(4) Pagination:** `?page=` (1-indexiert, ungültig⇒1), aktiver Filter auf **eine** Query (`.select(..., {count:"exact"})` ⇒ `data` via `.range(from,from+19)` + **gefilterte** Gesamtzahl in **einem** Round-Trip), `created_at DESC`; `totalPages = max(1, ceil(count/20))`. **Respektiert den Filter** (gleicher Filter vor `count`+`range`, Links führen ihn mit). **Overshoot-Redirect:** `page > totalPages` ⇒ `redirect(buildOrdersUrl({…,page:totalPages}))` ⇒ nach der Weiche `orders.length===0` ⟺ `total===0` (keine leere Seite N). [components/orders-pagination.tsx](components/orders-pagination.tsx) (Server Component): „Zurück"/„Weiter" als `<Link>` (bei 1 Seite gar nicht gerendert), Rand-Seiten als `data-disabled`-`<span>`, „Seite {page} von {total}". **(5) Layout** (`.orders-header`, vertikaler Stack statt der früheren horizontalen `.orders-toolbar`): Titel → Status-Dropdown → „Neuer Auftrag" → Quick-Filter; (2)–(4) ab `total > 0 || hasActiveFilter`, leeres Filter-Ergebnis behält die Leiste (`orders.emptyFiltered`, jetzt generisch „Keine Aufträge für diese Auswahl."), leerer Betrieb ohne Filter nur Onboarding-Card. i18n `orders.quickLabel`/`quickFlagged`/`quickDrafts`/`quickUngenerated`/`pagination`/`prevPage`/`nextPage`/`pageOf`. `pnpm typecheck` + `pnpm build` grün.

- **Pre-E2E-Fixes ✅ (Review 3.1 + 7.1 + zwei UX-Bugs)** — vier gezielte Korrekturen vor dem E2E-Test ([REVIEW.md](REVIEW.md)). **Keine Migration, kein Webhook-Pfad geändert**, Isolation unangetastet (`business_id` aus Session/RLS, `service_role` nur server, kein `<form>`, kein `any`). **(1) Deliver-Doppelversand-Schutz** ([deliver/route.ts](app/api/portal/orders/[id]/deliver/route.ts), REVIEW 3.1): das `generated→sent`-Update prüfte nicht, ob eine Zeile getroffen wurde → bei Doppelklick/Race liefen **Billing-Event + Kunden-E-Mail doppelt**. Jetzt `{ count: "exact" }` + bei `count===0` sofort `{ sent:true, alreadySent:true }` (200) **vor** Billing/E-Mail — **exakt wie der Webhook-Pfad** (`handlePickedUp`); `picked_up_at=null`-Reset bleibt. **(2) Status-Dropdown ↔ Quick-Filter-Sync** ([order-status-filter.tsx](components/order-status-filter.tsx)/[order-quick-filters.tsx](components/order-quick-filters.tsx)/[orders/page.tsx](app/portal/orders/page.tsx)): bei aktivem Quick zeigte das Dropdown „Alle", ein erneutes Wählen war ein **No-op** (`<select>` feuert kein `onChange` für den gesetzten Wert) → zurück zu „Alle" nur umständlich. Ursache war **nicht** die URL-Logik (der „Alle"-Pfad ging schon auf die nackte Liste), sondern dass „Alle" nicht auslösbar war. Fix: beide Komponenten bauen die Ziel-URL über **`buildOrdersUrl`** (eine Quelle; Dropdown droppt immer `?quick=`, Quick immer `?status=`); damit „Alle" bei aktivem Quick **wählbar** wird, zeigt das Dropdown dann eine **deaktivierte Platzhalter-Option** (`__quick__`, `orders.filterQuickActive`) statt „Alle" als Wert ⇒ „Alle" ist ein echter Wechsel und setzt **beide Achsen in EINEM Schritt** zurück; `onChange` nutzt `isOrderStatus(next)` (kein Cast). **ÄNDERT** das frühere „bei aktivem Quick zeigt das Dropdown sichtbar ‚Alle'" aus Block C / Schritt 3 (jetzt Platzhalter). **(3) Logout in die Bottom-Tab-Nav + Soft-Confirm** ([logout-button.tsx](app/portal/logout-button.tsx)/[layout.tsx](app/portal/layout.tsx)/[portal-nav.tsx](app/portal/portal-nav.tsx)/[globals.css](app/globals.css)): der prominente Logout-Button der mobilen **Top-Bar entfernt** (Top-Bar zeigt nur noch den Betriebsnamen); Logout jetzt **dezent als vierter Tab** in `PortalTabNav` (`<LogoutButton variant="tab">`, Icon+Label wie die anderen Tabs). `LogoutButton`: `compact:boolean` → `variant:"full"|"tab"`; **Soft-Confirm** `window.confirm(t("nav.logoutConfirm"))` im `onClick` (Projekt-Konvention, kein `<form>`) — greift in **beiden** Varianten (auch Desktop-Sidebar). CSS: `.portal-tab` um `background/border/font-family/cursor`-Resets ergänzt (`<button>` = `<a>`-Tab optisch). i18n `nav.logoutConfirm`. **(4) Reel nach Versand renderbar** ([render-reel/route.ts](app/api/portal/orders/[id]/render-reel/route.ts)/[orders/[id]/page.tsx](app/portal/orders/[id]/page.tsx), REVIEW 7.1): Reel-Render war nur bei `generated` erlaubt → liefert ein Betrieb **vor** dem Render aus (Status→`sent`, nicht reopenbar), blieb das Reel dauerhaft un-renderbar (Sackgasse). Der 409-Guard erlaubt jetzt **`RENDERABLE_STATUSES=[generated,sent,viewed,shared]`** (alle Stufen mit Booklet). **KRITISCH — Reel-Render status-unabhängig ab `generated`, Order-Status UNBERÜHRT:** der Render erzeugt NUR das Reel-Artefakt (`booklets.reel_*`+Storage), **kein** Zurücksetzen auf `generated`, **kein** Statuswechsel, **kein** Nachversand, **keine** erneute E-Mail; das Reel erscheint im bestehenden Booklet unter demselben Link (Kunde sieht es beim nächsten Öffnen). Die Detailseite surfacet den `<ReelButton>` jetzt für `canRenderReel=generated|sent|viewed|shared` (Booklet-/Reel-Daten + `ready`-Signed-URL für alle diese Stufen geladen), sonst wäre der Guard nicht erreichbar; `reel-status`-Poll war schon status-agnostisch. `pnpm typecheck` + `pnpm build` grün.

- **Layout-Umbau Auftrags-Detailseite ✅ (NUR UI — Positionierung + Benennung, KEINE Logik)** — die verstreuten Aktions-Buttons + drei Top-Banner der Detailseite in eine kohärente Anordnung gebracht (Basis: FLOW_REDESIGN.md-Ist-Analyse, untracked). **Explizit nur Frontend:** alle Route Handler/API-Calls identisch verdrahtet; Status-Maschine, finalize→generate-Zwei-Schritt, Doppelversand-`count`-Guard, `access_token`/`short_code`-Erhalt bei Re-Generate, FIX 7.1 (Reel ab `generated`, Order-Status unberührt), Webhook-Semantik, Quick-Filter-Logik **unangetastet** — **KEIN Chaining**, kein Route-Merge, keine Migration. Betroffen: [page.tsx](app/portal/orders/[id]/page.tsx), [finalize-controls.tsx](app/portal/orders/[id]/finalize-controls.tsx), [generate-controls.tsx](app/portal/orders/[id]/generate-controls.tsx), [deliver-controls.tsx](app/portal/orders/[id]/deliver-controls.tsx), [de.ts](lib/i18n/de.ts). **(1) Umbenennung:** „Vorschau erzeugen" → **„Booklet erstellen"** (`generate.generate` — der Schritt erstellt das echte Booklet, keine Vorschau); der finalize-Button heißt **ebenfalls** „Booklet erstellen" (`finalize.button` — begrifflich EINE Aktion in zwei Tippen, Hinweistexte `finalize.hint`/`generate.hint` erklären die Schritte); „Vorschau öffnen" → **„Booklet ansehen"** (`generate.openPreview`, Key unverändert), „Wieder bearbeiten" → **„Bearbeiten"** (`finalize.reopen`). **(2) Aktionszone unten = nächste fällige Aktion je Status:** `draft` → großer Gold-Button „Booklet erstellen" (= `POST finalize`, **OHNE `window.confirm`** — der widersprüchliche Dialog entfällt, der Schritt ist per Reopen voll reversibel; `finalize.confirm`/`confirmText` entfernt; `need_media`-Guard client+server unverändert); `finalized` → „✓ Medien abgeschlossen" (`finalize.done`) + großer „Booklet erstellen" (= `POST generate`) + kleiner „Bearbeiten" (NEU `<ReopenButton>`, ersetzt `FinalizeBanner`) — zugleich Recovery-Punkt bei generate-502; `generated` → **Reel-Block** (eigener Abschnitt mit „Reel"-Überschrift `reel.title`, `<ReelButton>` funktional unverändert) + „Booklet ausliefern" (`<DeliverButton>` inkl. Safe-Mode-/Reel-/E-Mail-**Confirm UNVERÄNDERT** — Versand irreversibel); `sent`/`viewed`/`shared` → kein Erstell-/Versand-Button, Reel-Block bleibt (FIX 7.1, Nachträglich-Rendern). **(3) Top-Banner entfernt** (`FinalizeBanner`/`GeneratedBanner`/`DeliveredBanner` gelöscht) → kleine **obere Aktionsleiste** unter dem Sticky-Head für **alle** Booklet-Stufen (`generated|sent|viewed|shared` + `bookletToken`): „Booklet ansehen" (`/b/[token]?c=1&p=1`, server-gerendert) + „QR drucken" (**jetzt auch bei `viewed`/`shared`** — schließt FLOW_REDESIGN-Defekt 5, vorher nur generated/sent); bei `generated` zusätzlich klein „Neu generieren"/„Bearbeiten" (NEU `<GeneratedActions>`, Client-Fragment mit der bisherigen Banner-Logik); Versand-Stufen zeigen die frühere Banner-Info als schmale Zeile „✓ Ausgeliefert am {Datum}". Kein `<form>`, kein `any`, Server Components wo möglich. `pnpm typecheck` + `pnpm build` grün.

- **Stichwort ins Caption-Feld vorbefüllen ✅ (NUR UI, keine Migration/Logik)** — behebt die Verwirrung, dass das bei der Aufnahme getippte **Stichwort** im Caption-Editor als separate Zeile ÜBER einem leeren Feld stand (Feld wirkte leer). Jetzt seedet der `CaptionEditor` ([app/portal/orders/[id]/media-list.tsx](app/portal/orders/[id]/media-list.tsx)) die `textarea` mit `media.caption ?? media.keyword ?? ""` → das Stichwort steht direkt **im** Feld als initialer, frei editierbarer Text (kann zur manuellen Kurzbeschreibung werden); die separate „Stichwort: …"-Kontextzeile ist entfernt. **ENTSCHEIDUNG — `keyword` bleibt eigene Spalte** (kein Merge bei der Aufnahme): das Stichwort ist weiter Roh-Input für die KI-Generierung (`generateCaption({ keyword })`) **und** Anzeige-Fallback (`displayCaption = caption ?? keyword`, Web-Story/Reel); würde man es schon bei der Aufnahme in `caption` schreiben, gälten Stichwort-Kacheln fälschlich als „beschriftet" → raus aus `missingCount`/Auswahl-Kreis, **nicht** mehr KI-batch-wählbar (bricht den bewussten Auswahl-Flow aus 6b.2/6b.3). Ohne Speichern bleibt `caption` null ⇒ Kachel bleibt KI-wählbar, Booklet zeigt weiter das Stichwort; **KI-Generierung überschreibt das Feld komplett** (gewollt, keine Nachfrage — der Nutzer wählt die Bilder bewusst). `CaptionReadOnly` (Abgeschlossen-Modus) zeigt analog `caption ?? keyword` (= Kundensicht). Gilt automatisch für **alle** Medien (Foto/Video, künftige Vorher/Nachher/Prozess). Dead i18n `capture.keyword` entfernt (`capture.keywordOptional` = Aufnahme-Eingabe bleibt). Kein `<form>`, kein `any`. `pnpm typecheck` + `pnpm build` grün.

- **PWA-Setup + Favicon ✅ (NUR Frontend/Assets, keine Migration/Logik)** — die App ist als Homescreen-PWA installierbar und bekommt ein vollständiges Favicon-Set. **(1) Favicon** `public/favicon.svg` (konzentrische Gold-Kreise `#C4A95B`, Vektor) + generierte PNGs in `public/icons/`: `favicon-16.png`/`favicon-32.png`, `apple-touch-icon.png` (180×180), `icon-192.png`/`icon-512.png` — alle auf Anthrazit-Grund `#3A3A3A` (App-Hintergrundfarbe). **(2) Manifest** `public/manifest.json`: `name` „Valooro Handwerk", `short_name` „Valooro", `display: standalone`, `start_url: /portal`, `theme_color`/`background_color` `#3A3A3A`, Icon-Einträge 192/512 (512 zusätzlich `purpose: any maskable`). **(3) Head-Tags** im Next-`metadata`-Export ([app/layout.tsx](app/layout.tsx)): `manifest: "/manifest.json"` + `icons` (`favicon.svg` als primäres `image/svg+xml`, PNG-Fallbacks 16/32, `apple-touch-icon`). Reine statische Assets + Metadata — kein Code/Logik, keine DB-Berührung. `pnpm typecheck` + `pnpm build` grün.

- **UI-Politur: Betriebs-Logo im Kopf + Refresh-Button ✅ (NUR Frontend, keine Migration/Logik)** — zwei kleine Verbesserungen. Daten weiter über den AUTHENTICATED Client (RLS, `business_id` aus Session), kein `<form>`, kein `any`, Server Components wo möglich. **Geteilter Helfer** [lib/branding/logo.ts](lib/branding/logo.ts): `getBusinessLogoUrl(business)` signiert `branding.logo_url` aus dem privaten `branding`-Bucket (1 h, AUTHENTICATED — gleiche Policy/Signier-Logik wie die Settings-Vorschau 7a, aber **eine Quelle**, nicht mehr dupliziert), `null` ⇒ Name-Fallback. **(Punkt 3a) Logo in der Mobile-Top-Bar** ([layout.tsx](app/portal/layout.tsx)): die schlanke mobile Top-Bar zeigt jetzt linksbündig das **signierte Logo** (`.portal-topbar-logo`, `max-height 32px`, `object-fit: contain`), falls hochgeladen — sonst weiter den Betriebsnamen (`.portal-topbar-brand`); Desktop-Sidebar (Valooro-Logo) unverändert; `eslint-disable @next/next/no-img-element` (Muster wie 7a/10b). **(Punkt 3b) Dashboard-Kopf statt „Willkommen"** ([page.tsx](app/portal/page.tsx)): die `portal.welcome`-Begrüßung (`.dashboard-welcome`) **entfernt** → konsistenter Seitenkopf `.dashboard-head` (oben links): Logo bzw. Betriebsname-Fallback + `<h1>` **„Dashboard"** (`dashboard.title`), gleiche Optik wie die „Aufträge"-Überschrift (`.dashboard-title`); Logo im bestehenden `Promise.all` parallel signiert, `header` einmal gebaut + in Leer- und Daten-Zustand gerendert; `portal.welcome` bleibt ungenutzt im Dictionary. **(Punkt 4) Refresh-Button der Auftragsliste** ([components/orders-refresh-button.tsx](components/orders-refresh-button.tsx) NEU + [orders/page.tsx](app/portal/orders/page.tsx)): Client-Insel (`"use client"`, einzige auf der Server-Component-Seite) — Icon-Button (Kreispfeil) oben rechts in neuer Titelzeile `.orders-title-row` (h1 links, Button rechts, `space-between`); Klick ⇒ `router.refresh()` in `useTransition` ⇒ holt per Webhook frisch reingekommene Aufträge nach (ohne vollen Reload), während des Übergangs `disabled` + `data-pending` ⇒ rotierendes Icon (`@keyframes`, `prefers-reduced-motion`-fest); dezent (40×40 rund, `--border`→Hover `--gold-border`), mobil gut erreichbar; i18n `orders.refresh`, `dashboard.title`. `pnpm typecheck` + `pnpm build` grün.

- **Schritt 13 ✅ Vorher/Nachher/Prozess-Kategorisierung (Migration 0010)** — jedes BILD bekommt eine **Kategorie** (`before`/`after`/`process`), die den **festen Booklet-Aufbau** steuert; Videos sind IMMER `process`. **(1) Migration 0010** ([0010_media_category.sql](supabase/migrations/0010_media_category.sql) + [Verify](supabase/verify/0010_media_category_checks.sql)): Spalte `order_media.category text NOT NULL DEFAULT 'process' CHECK (category in ('before','after','process'))` + `comment`; **keine** neue Policy/GRANT (0001-`order_media`-RLS `for all` + `service_role` decken die Spalte). **before/after je max 1 pro Auftrag = App-enforced** (bewusst KEIN DB-Constraint: Testphase, ein schreibender Server-Pfad, Default `process` kollidiert nie). NOT NULL + Default `process` ⇒ Altmedien korrekt = Prozess. **(2) Typen/Helfer:** `MediaCategory` + `category` in `OrderMedia`/`getOrderMedia` ([queries.ts](lib/orders/queries.ts)); **geteilter** `orderBookletMedia()` ([lib/booklet/media-order.ts](lib/booklet/media-order.ts), generisch über `{category}`): bringt sort_order-sortierte Medien in die feste Reihenfolge **before → process (sort_order) → after** — EINE Quelle für Web-Story UND Reel (analog `displayCaption`). **(3) Upload mit Kategorie** ([capture.tsx](app/portal/orders/[id]/capture.tsx) + [media/route.ts](app/api/portal/orders/[id]/media/route.ts)): im Aufnahme-/Upload-Entwurf (alle 4 Buttons) ein **Kategorie-Selektor** (Vorher/Nachher/Prozess, Default Prozess) — **nur Foto**, Video kein Selektor + serverseitig immer `process`. belegter before/after-Slot ⇒ Option deaktiviert (`hasBefore`/`hasAfter` aus der Server-Page + **in-flight** Queue-Items des Typs, reine UX); Metadaten-`POST` schickt `category`. Server: `resolveCategory` (Video⇒`process`, Foto⇒gewählt/Default), **HARTER Riegel** before/after max 1 ⇒ `category_taken` 400 (Client-Disable umgehbar), Insert mit `category`. **(4) Kategorie-Wechsel** (neue Route [media/[mediaId]/category/route.ts](app/api/portal/orders/[id]/media/[mediaId]/category/route.ts) PATCH): AUTHENTICATED, Media via RLS gegen `order_id`, Body `{category}`, Video nur `process` (`video_process_only`), before/after max 1 via `.neq("id",…)`-Count (`category_taken`); im **Vollbild-Viewer** als Toggle-Reihe (Foto, Editier-Modus) → optimistisch + `router.refresh()`. **(5) Anzeige gruppiert** ([media-list.tsx](app/portal/orders/[id]/media-list.tsx)): GANZ OBEN zwei **große, fixe Slots** nebeneinander (`.media-ba-row` 2-Spalten, je halbe Breite) — links Vorher, rechts Nachher, leerer **Platzhalter** (gestrichelt + Label/Hinweis) oder das Bild mit Label-Band; DARUNTER **„Prozess"**-Überschrift + das bisherige 3er-Raster. **Reorder NUR process** (dnd-kit `SortableContext` über process-ids; `handleDragEnd` sortiert die process-Teilmenge, PATCHt aber das **volle** Set in Booklet-Ordnung [before, …process, after] → die reorder-Route bleibt „exakte Menge"-validiert, sort_order folgt der Render-Reihenfolge); before/after fix. Geteilter `TileVisual` (Inhalt) für `SortableTile` UND `BeforeAfterSlot` (nicht dupliziert); Auswahl/Caption/Löschen gelten auch für before/after. **(6) Booklet-Reihenfolge** ([load.ts](lib/booklet/load.ts) + [render-reel/route.ts](app/api/portal/orders/[id]/render-reel/route.ts)): beide selecten `category` + wenden `orderBookletMedia` an ⇒ Web-Story UND Reel rendern Intro → before → process → after → Outro. **(7) KI-Captions** ([captions.ts](lib/ai/captions.ts)/[media-caption.ts](lib/ai/media-caption.ts) + Batch/Regenerate-Routes): `CaptionInput.category` + `categoryHint` qualifiziert before als **Ausgangszustand/Vorher**, after als **Ergebnis/Nachher** im Haiku-Prompt; process unverändert; Stichwort fließt wie bisher mit ein (before/after bekommen denselben Stichwort-/Caption-Flow). **(8) Validierung verschärft** ([finalize](app/api/portal/orders/[id]/finalize/route.ts) + [generate](app/api/portal/orders/[id]/generate/route.ts)): „≥ 1 Medium" → **„≥ 1 `process`-Medium"** (`.eq("category","process")`, Videos zählen als process) ⇒ Auftrag mit NUR before/after ist **nicht** erstellbar (`need_process` 400, client+server); Buttons bekommen `processCount` statt `mediaCount`, `need_process`-Mapping in [generate-controls](app/portal/orders/[id]/generate-controls.tsx). i18n `mediaCategory.*`, `capture.category`/`categoryTaken`, `assembler.slotEmpty`/`processEmpty`/`categoryLabel`/`categoryError`, `finalize.needProcess`/`generate.needProcess`. **Bestehender `tag`-Spalte (vorher/nachher/prozess, ungenutzt seit 6b.2) unangetastet** — `category` ist die neue, englisch-wertige Quelle. Kein `<form>`, kein `any`, `business_id` aus Session/RLS, `service_role` nur server. **Migration 0010 muss vor dem Live-Gang manuell im Supabase-SQL-Editor angewendet werden.** `pnpm typecheck` + `pnpm build` grün.

- **UI-Fixes: Logo-Kopf + Intro-Text-Karte ✅ (NUR Frontend, keine Migration/Logik)** — drei Anzeige-Korrekturen. **Fix 1** doppeltes Dashboard-Logo: auf Mobile zeigten Top-Bar (`.portal-topbar-logo`) **und** Dashboard-Kopf (`.dashboard-head-logo`) das Logo untereinander → Dashboard-Kopf-Logo/-Name auf Mobile (`@media max-width:768px`) ausgeblendet (Top-Bar trägt es, Kopf nur noch Titel); Desktop behält das Kopf-Logo. **Fix 2** Logos zentriert: `.portal-topbar` `space-between → center` (Logout sitzt in der Bottom-Tab-Nav), `.dashboard-head` `flex-start → center` (+ `text-align:center`). **Fix 3** Frosted-Karte hinter dem Intro-Beschreibungstext: geteilte Klasse **`.booklet-frost`** (halbtransparent-weiß + `backdrop-filter:blur` + Radius/Schatten + `--share-ink*`-Vars) aus der `.booklet-share`-Logik (Block A/Punkt 10) extrahiert; `.booklet-share` ([share-bar.tsx](app/b/[token]/share-bar.tsx)) und die neue `.booklet-desc-card` ([page.tsx](app/b/[token]/page.tsx), wrappt `.booklet-desc`) nutzen sie gemeinsam ⇒ konsistent, dunkle Schrift im Intro. Kein `<form>`, kein `any`. `pnpm typecheck` + `pnpm build` grün.

- **Booklet-Flow vereinfacht: finalize + generate zusammengelegt, `finalized` entfernt (Migration 0011)** — der zweistufige Bau-Flow („Booklet abschließen" → `finalized` → „Vorschau erzeugen" → `generated`) wird **ein** Schritt: ein Klick **„Booklet erstellen"** führt direkt `draft → generated` aus; der Zwischenstatus **`finalized` entfällt komplett**. Grundlage FLOW_REDESIGN.md (1.2/1.3: „Vorschau erzeugen" war nie eine Vorschau, sondern der Bau-Schritt; `finalized` schrieb **nur** den Status, kein Artefakt — niemand profitierte vom Verweilen). **Neue Status-Maschine: `draft → generated → sent → viewed → shared`.** **AUSLIEFERUNG UNVERÄNDERT** (Webhook-Auto-Versand bei „Abgeholt", Safe-Mode-Confirm, Doppelversand-`count`-Guard, deliver-Pfad) — geändert wurde nur der Weg nach `generated`. **Backend:** [generate/route.ts](app/api/portal/orders/[id]/generate/route.ts) Guard jetzt `draft`||`generated` (statt `finalized`||`generated`); `need_process` sitzt hier; **Fehlersicherheit** — Order-Status wird ERST am Ende (nach KI-Calls + booklets-Upsert) auf `generated` gesetzt ⇒ scheitert etwas, bleibt der Auftrag sauber `draft` (kein `finalized`-Limbo), Retry möglich; Token/Kurzlink bei Re-Generate erhalten. [finalize/route.ts](app/api/portal/orders/[id]/reopen/route.ts) **entfernt**. [reopen/route.ts](app/api/portal/orders/[id]/reopen/route.ts) nur noch `generated → draft`. Webhook [handlePickedUp](app/api/webhook/[secret]/route.ts): Warn-Flag nur noch bei **`draft`** (war `draft`||`finalized`), Rest unverändert. **State-Maschine:** [order-status-badge.tsx](components/order-status-badge.tsx) `'finalized'` aus `ORDER_STATUSES`+`STATUS_STYLES` raus (gold = nur noch `generated`); Quick-Filter [filters.ts](lib/orders/filters.ts): `flagged` = `picked_up_at` + `status IN (draft,generated)`, der mit `drafts` deckungsgleich gewordene **`ungenerated` entfernt**. **Migration 0011** ([0011_orders_status_drop_finalized.sql](supabase/migrations/0011_orders_status_drop_finalized.sql) + Verify) verengt `orders_status_check` auf die 5 Werte (drop/add wie 0005, Alt-`finalized`→`draft`, DEFAULT `draft`, keine Policy/GRANT) — Defense-in-depth, **muss manuell angewendet werden**. **Button-Layout** ([page.tsx](app/portal/orders/[id]/page.tsx)/[generate-controls.tsx](app/portal/orders/[id]/generate-controls.tsx)/[deliver-controls.tsx](app/portal/orders/[id]/deliver-controls.tsx), 2-Spalten `.booklet-actions-row`): **draft** = links „Booklet erstellen" (aktiv, POSTet generate, kein confirm) + rechts „Reel erstellen" **gesperrt** (`<LockedReelButton>`, Klick → `reel.lockedHint`); **generated** = links „Booklet erstellen" **grau/erledigt** (`<CreateBookletButton disabled>`, „✓ Booklet erstellt") + rechts echter `<ReelButton>`, darunter schmäler „Bearbeiten" (`<ReopenButton>`) + „Ausliefern" (`<DeliverButton>`, **Logik unverändert**); **sent/viewed/shared** = nur Reel-Block (FIX 7.1), Ansehen/QR oben. `GeneratedActions` (+ separates „Neu generieren") entfällt — Regeneration via „Bearbeiten" → „Booklet erstellen"; `finalize-controls.tsx` gelöscht (`ReopenButton` → `generate-controls.tsx`). i18n: `finalize.*`→`reopen.{button,error}`, `generate.created`+`reel.lockedHint` neu, `orderStatus.finalized`/`orders.quickUngenerated`/`generate.regenerate` raus. **Darf nicht brechen geprüft:** Doppelversand-`count`-Guard, Token/Kurzlink-Erhalt, FIX 7.1 (Render lässt Order-Status unberührt), Webhook-Semantik, Quick-Filter-Exklusivität, „Reel braucht generated" (im Entwurf gesperrt). Kein `<form>`, kein `any`, `business_id` aus Session/RLS. **Migration 0011 vor dem Live-Gang manuell im Supabase-SQL-Editor anwenden.** `pnpm typecheck` + `pnpm build` grün.

- **UX-Fixes aus dem Praxistest: Reel-Viewer-Ausweg + kompaktere Teilen-Sektion ✅ (NUR Frontend, keine Migration/Logik)** — zwei Praxistest-Korrekturen, kein `<form>`, kein `any`, Render-/Poll-/Tracking-/Generierungs-Logik unangetastet. **FIX 1 — Reel-Ansicht hatte keinen Zurück-Weg** ([generate-controls.tsx](app/portal/orders/[id]/generate-controls.tsx)): „Reel ansehen" öffnete das fertige Reel als rohe mp4 in einem **neuen Browser-Tab** (`<a target="_blank">`) ⇒ Sackgasse ohne Schließen/Zurück. Jetzt ein **In-App-Overlay** `<ReelViewer>` (Vollbild, `<video controls autoPlay playsInline>`, z-index 70) mit **klarem Schließen-X oben rechts** (`<button>`, `aria-label` `reel.close`, **Notch-/Safe-Area-sicher** via `env(safe-area-inset-top/right)`), das **zurück zur Detailseite** führt; Escape + Backdrop-Klick schließen ebenfalls, Klick aufs Video schließt nicht (`stopPropagation`). Der „Reel ansehen"-Button ist von `<a>` auf `<button onClick={setViewerUrl}>` umgestellt (`PlayIcon`/`CloseIcon` neu, ungenutztes `ExternalLinkIcon` entfernt). i18n `reel.close`. **FIX 2 — Outro/Teilen-Sektion zu hoch (Buttons erst nach Scrollen sichtbar):** **(a)** der **IG-Caption-Block** ([share-bar.tsx](app/b/[token]/share-bar.tsx)) ist per Flag `SHOW_IG_CAPTION = false` **aus der UI ausgeblendet, NICHT gelöscht** — `buildIgCaption`-Generierung, `copyIgCaption` und das Event-Tracking (`link_click/ig`) bleiben vollständig im Code (das `&&`-Gate hält `copyIgCaption`/`InstagramIcon`/`CopyIcon` als „benutzt" ⇒ keine Unused-Warnung), Reaktivierung (evtl. später pro Betrieb) = Flag auf `true`. **(b)** die verbleibende Teilen-Sektion (Reel/WhatsApp/Website/Story/Link/Google-Bewertung) **kompakter** ([booklet.css](app/b/[token]/booklet.css)): `.booklet-share` margin/padding/gap reduziert, Buttons `min-height` 46/48 → 42, Paddings enger; zusätzlich Outro-Umfeld gestrafft (`.booklet-outro-message`/`.booklet-contact`-Abstände kleiner, **Outro-Logo** auf 88px via `.booklet-section--outro .booklet-logo` — **Intro-Logo unverändert**). Frosted-Karte (`.booklet-frost`) bleibt; Ziel ~380×800 ohne Scrollen. `pnpm typecheck` + `pnpm build` grün.

- **Control-Center-Umbau der Auftrags-Detailseite + Zurück-Buttons ✅ (Frontend + EINE Backend-Zeile für Stale-Reel)** — bündelt die verstreuten Aktions-Buttons (oben + unten) in **eine** Aktionszone am Seitenende und behebt drei Sackgassen (fehlende Zurück-Wege). **Layout/UX**; Routen, Status-Maschine (`draft → generated → sent → viewed → shared`), generate/reopen/render-reel/reel-status/deliver-Logik, Doppelversand-`count`-Guard, `access_token`/`short_code`-Erhalt, FIX 7.1, Webhook-Semantik **unverändert**; keine Migration, kein `<form>`, kein `any`, `business_id` aus Session/RLS, `service_role` nur server. **(1) Obere Aktionsleiste entfernt** ([page.tsx](app/portal/orders/[id]/page.tsx)) — oben nur noch der Auftrags-Kopf; alle Folge-Aktionen im `.booklet-cc`-Block unten: **`draft`** = EIN „Booklet erstellen" über die volle Breite (ohne Prozess-Medium **gar kein** Button, `need_process` bleibt client+server); **`generated`** = (1) grau „✓ Booklet erstellt" → (2) „Reel erstellen"/grau „✓ Reel erstellt" → (3) „Bearbeiten" | „Ausliefern" (`.booklet-actions-row`) → (4) Ansehen-Zeile (Haarlinie abgesetzt) „Booklet ansehen" · optional „Reel ansehen" · „QR drucken"; **`sent`/`viewed`/`shared`** = analog, Slot 1 = „✓ Ausgeliefert am {Datum}" statt grauem Button, **kein** Slot 3, Reel-Slot bleibt (FIX 7.1), Ansehen-Zeile bleibt (schließt FLOW_REDESIGN-Lücke 5). **(2) Reel-Buttons getrennt** ([generate-controls.tsx](app/portal/orders/[id]/generate-controls.tsx)): altes kombiniertes `<ReelButton>` → **`<ReelCreateButton>`** (Render+Poll, fertig ⇒ grau „✓ Reel erstellt" via geteiltem `<DoneButton>`, **kein „Neu erstellen" mehr**; bei Poll-`ready` zusätzlich `router.refresh()`, damit Watch-Button + Deliver-Warnung den Server-Stand bekommen) + **`<ReelWatchButton>`** (Slot 4, öffnet das In-App-`<ReelViewer>`-Overlay). `<LockedReelButton>`/`LockIcon` + i18n `reel.lockedHint`/`recreate`/`title` **entfernt**, `reel.created` neu. **(3) Stale-Reel = „einfacher Ansatz" (EINE Backend-Zeile, keine Migration):** der **Re-Generate-Zweig** ([generate/route.ts](app/api/portal/orders/[id]/generate/route.ts), `existing`-Update) setzt zusätzlich `reel_status='pending'` + `reel_url=null` ⇒ nach „Bearbeiten" → erneut „Booklet erstellen" ist der Reel-Button wieder aktiv („als neu zu erstellen behandeln"), das auf altem Intro basierende Reel verschwindet von der öffentlichen Seite (zeigt nur `ready`), nächster Render `upsert`t dieselbe Datei (kein Orphan). Bewusst statt „Reel veraltet"-Hinweis gewählt (ohne Render-Zeitstempel kein zuverlässiger Detektor; ein eingebranntes veraltetes Reel auszuliefern ist schlechter). **(4) Zurück-Buttons (NUR Portal-Sicht):** Reel = In-App-X (bleibt); **QR** ([qr/page.tsx](app/portal/orders/[id]/qr/page.tsx)) „← Zurück zum Auftrag" (`<Link>` → `/portal/orders/${id}`, `.qr-actions`, **bildschirm-only** in `.qr-no-print`); **Booklet-Vorschau** ([b/[token]/page.tsx](app/b/[token]/page.tsx)) schwebender „← Zurück zum Auftrag" (`.booklet-portal-back`, fixed top-left, safe-area-sicher) **NUR bei `?p=1`** (Guard `isNoTrackParam` neu in [events.ts](lib/booklet/events.ts)) — der echte Kunden-Link (`?c=1`, ohne `p`) bekommt **KEINEN**; Linkziel via neuem `PublicBookletData.orderId` ([load.ts](lib/booklet/load.ts)), Marker **kein** Auth-Gate (Token bleibt Schutz §14.2). Detail-Links „Booklet ansehen"/„QR drucken" öffnen jetzt **same-tab** (kein `target="_blank"`) ⇒ sauberer Rundlauf statt Orphan-Tabs. i18n `reel.created`/`qr.back`/`booklet.backToOrder`. **Ersetzt** das vorige „Button-Layout der Detailseite" (LockedReelButton + obere Leiste). `pnpm typecheck` + `pnpm build` grün.

- **Telefonnummer in der „Betrieb"-Settings-Gruppe ✅ (NUR UI-Reorg, keine Migration/Logik)** — `settings.contact_phone` **existierte bereits vollständig**: Typ `BusinessSettings.contact_phone` + `normalizeSettings` (`asTrimmedOrNull`, leer ⇒ `null`) in [current-business.ts](lib/auth/current-business.ts), Validierung + READ-MERGE-WRITE im Settings-`PATCH` ([route.ts](app/api/portal/settings/route.ts), Cap `CONTENT_LIMITS.contactPhone = 40`), und das **Booklet-Outro rendert die Nummer schon** ([app/b/[token]/page.tsx](app/b/[token]/page.tsx)): `contact_phone` als `tel:`-Pill (whitespace im `href` gestrippt) neben E-Mail/Website, konditional (`{contact_phone ? … : null}` + `hasContact`-Boolean) ⇒ leer ⇒ keine leere Zeile. **Geändert nur die Position des UI-Felds** ([settings-form.tsx](app/portal/settings/settings-form.tsx)): das Telefon-Feld saß in „Booklet-Inhalt" (Label `settings.content.contactPhone`) und ist jetzt — analog zur Kontakt-E-Mail (Block B, Punkt 6) — **in der „Betrieb"-Gruppe direkt unter der Kontakt-E-Mail** (öffentliche Kontaktdaten gebündelt, eine Quelle). Neues `TextField type="tel"` (Union um `"tel"` erweitert), `maxLength = CONTENT_LIMITS.contactPhone`, Label `settings.contactPhone` + Hinweis `settings.contactPhoneHint`; alter `settings.content.contactPhone`-i18n-Key **entfernt**. State/Save/Validierung (`contactPhone`-State, `contact_phone` im PATCH-Body, Längen-Check) **unverändert**. Kein `<form>`, kein `any`. `pnpm typecheck` + `pnpm build` grün.

- **Warn-Hinweis auf geflaggten Auftragskarten neu gestaltet (Überlauf-Fix) ✅ (NUR Frontend, keine Migration/Logik)** — der „Abgeholt – Booklet nicht versendet"-Warner (Block C / Schritt 2) war ein **überbreiter Inline-Pill** (`whiteSpace: nowrap`) in der rechten Statuszeile der Auftragskachel und lief auf schmalen Mobile-Viewports (~380px) über den **rechten Kartenrand** hinaus, verdrängte den Kartentext und sah kaputt aus (Praxistest). **Warn-Bedingung identisch** (`picked_up_at` gesetzt UND `status NOT IN {sent, viewed, shared}`, Doppel-Sicherung beibehalten) — **nur die Darstellung** geändert. **(1)** Der überbreite Inline-Pill **entfällt** aus dem Badge-Wrap; der normale Status-Badge („Entwurf" etc.) bleibt unverändert an seiner Stelle. **(2)** Die geflaggte Karte bekommt eine **dezente rote Umrandung** (`.card-flagged` in [globals.css](app/globals.css), gedämpftes Warnrot `rgba(192,57,43,0.5)`, im Hover `var(--red-text)`; steht hinter `.card-link`/`:hover` ⇒ überschreibt die Gold-Border auch im Hover). **(3)** Am **unteren Kartenrand** eine **Volle-Breite-Hinweiszeile** ([pickup-pending-badge.tsx](components/pickup-pending-badge.tsx) → `.order-pickup-warning`): Warn-Icon + „Abgeholt am TT.MM. – Booklet nicht versendet", rot, `--red-border`-Hairline oben, **kein `nowrap`** ⇒ bricht auf ~380px sauber um (kein Abschneiden, kein Überlauf). Dafür ist die Auftragskarte ([orders/page.tsx](app/portal/orders/page.tsx)) jetzt ein **Flex-Spalten-Container** (Zeile [Name + Badges] oben, Hinweiszeile unten); ungeflaggte Karten rendern nur die Zeile (optisch unverändert), `flaggedDate` (string|null) narrowt für die Zeile. **(4)** Weiterhin **nicht klickbar** — rein visuell (die Karte selbst verlinkt zum Auftrag). i18n: die getrennten `orderStatus.pickupPending`/`pickupPendingDate` zu **einem** `orderStatus.pickupPendingNotice` „Abgeholt am {date} – Booklet nicht versendet" zusammengelegt. Kein `<form>`, kein `any`. `pnpm typecheck` + `pnpm build` grün.

- **Content-Seiten-Kosmetik: Logo zentriert/kleiner + Website am Seitenfuß ✅ (NUR Frontend, keine Migration/Logik)** — finale Kosmetik an den **Content-/Bild-Seiten** der öffentlichen Web-Story (zwischen Intro und Outro, `MediaSection` in [app/b/[token]/page.tsx](app/b/[token]/page.tsx) + [booklet.css](app/b/[token]/booklet.css)); **Intro/Outro NICHT angefasst**, kein `<form>`, kein `any`. **(1) Per-Seite-Logo zentriert + 20 % kleiner:** `.booklet-page-logo` (nur bei `branding.logo_per_page`) saß oben **links** (`left: 18px`, `max-height: 68px`) → jetzt **horizontal zentriert** (`left: 50%; transform: translateX(-50%)`) + **54px** (68 × 0,8). Eigene Klasse, sauber gescoped — Intro/Outro nutzen `.booklet-logo` und bleiben unverändert. **(2) Website am unteren Rand jeder Content-Seite:** `settings.website_url` als **reiner, NICHT klickbarer Text** (kein Link, kein Tracking — das bleibt Outro-Sache), **zentriert**, **leichter** als die Caption/Content-Text (`font-weight: 400` vs. Caption 600; `300` ist in [fonts.ts](lib/booklet/fonts.ts) nicht geladen ⇒ bewusst 400) und **eine Spur kleiner** (`clamp(0.85rem, 3.6vw, 1rem)`); Anzeige ohne Protokoll/Trailing-Slash über den bestehenden `displayHost`-Helfer (konsistent mit Outro + `normalizeWebsite` aus [booklet-email.ts](lib/email/booklet-email.ts)). **Nur wenn gesetzt** (`{websiteUrl ? … : null}`) — sonst keine Zeile/kein Leerraum. Neue Klasse `.booklet-page-website` (`position: absolute`, unten in der Safe-Zone, eigener Text-Schatten für Lesbarkeit auch ohne Scrim; unter der bei ~16 % schwebenden Caption ⇒ kein Überlapp). `pnpm typecheck` + `pnpm build` grün.

- **Praxistest-Fixes: fixe Bottom-Nav (iOS Safe-Area) + Review-Hinweis/Clipboard ✅ (NUR Frontend, keine Migration/Logik)** — zwei kleine Korrekturen, kein `<form>`, kein `any`. **FIX 1 — Bottom-Tab-Nav klebt fix unten:** die mobile `.portal-tabnav` ([globals.css](app/globals.css)) war bereits `position: fixed; bottom: 0` — Ursache des „schwebt mitten im Content"-Verhaltens war **nicht** ein Containing-Block-Trap (kein `transform`/`filter`/`contain` auf `html`/`body`/`.portal-shell`, kein zweites globales Stylesheet, Nav ist Geschwister von `.portal-main`, **nicht** Nachfahre des Seiteninhalts), sondern **fehlendes `viewport-fit=cover`**: ohne es liefert `env(safe-area-inset-bottom)` auf iOS **`0`** (die vorhandene Safe-Area-Polsterung war toter Code) und eine fixe Bottom-Bar destabilisiert auf iOS Safari gegen die dynamische Toolbar. Drei Änderungen: (1) **`viewportFit: "cover"`** im `viewport`-Export ([app/layout.tsx](app/layout.tsx)); (2) **`.portal-tabnav` Höhe** = `calc(var(--portal-tabnav-h) + env(safe-area-inset-bottom, 0px))` zusätzlich zum bestehenden `padding-bottom: env(...)` — wegen des globalen `box-sizing: border-box` muss der erst jetzt > 0 werdende Inset in die Höhe **mitgerechnet** werden, sonst stauchte er die Tab-Reihe auf Notch-Geräten; (3) **`.portal-main` `padding-bottom`** um `+ env(safe-area-inset-bottom, 0px)` erweitert (letztes Element räumt Nav **inkl.** Home-Indicator). `position: fixed` bleibt (war korrekt); nur Mobile (≤ 768px), Desktop-Sidebar unberührt, Inset auf Nicht-Notch `0` ⇒ keine Regression. **FIX 2 — Google-Bewertungs-Hinweis + Clipboard-Garantie** ([share-bar.tsx](app/b/[token]/share-bar.tsx) + [de.ts](lib/i18n/de.ts)): `review.hint` neu **„Textvorschlag (KI) ist kopiert. Bei Google ins Feld einfügen (paste)."** (kurz/handlungsorientiert; **§8.6 gewahrt** — Vorschlag-Charakter, im Google-Feld editierbar, **kein** Belohnungsbezug, **keine** Sterne-Vorgabe; In-Code-Kommentare angepasst). Damit „ist kopiert" stimmt, wurde `copyText` gehärtet: neuer Helfer `writeToClipboard` versucht `navigator.clipboard.writeText`, fällt sonst auf **`legacyCopy`** (verstecktes `<textarea>` + `document.execCommand("copy")`) zurück — entscheidend in **In-App-Webviews** (WhatsApp/Instagram), wo `navigator.clipboard` fehlt/blockiert ist; „✓ kopiert"-Flash feuert nur noch bei **tatsächlichem** Erfolg. Reihenfolge in `writeReview` (erst kopieren, dann `window.open` aufs Google-Profil) unverändert (Doc-Fokus bleibt). Gilt für alle Kopier-Aktionen (Link/IG-Caption/Review). `pnpm typecheck` + `pnpm build` grün.

- **Backfill-Script `scripts/import-roapp-orders.ts` ✅ (CLI-only, einmalig, keine Migration/Live-Code-Berührung)** — legt bestehende roapp-Aufträge (die nicht mehr per `order.created`-Webhook reinkommen) als `status='draft'`-Orders an. **Importmenge = GENAU das hartkodierte `OBJECT_IDS`-Array** (interne roapp-Order-IDs, vom Nutzer aus den Web-UI-URLs abgelesen; KEIN Listen-/Such-Endpoint, KEIN Auto-Discovery). **Betrieb** über `BUSINESS_EMAIL="office@alinadax.com"` aufgelöst (`select id,name,default_language … where business_email=…`); fehlt ⇒ harter Abbruch; `business.id` = EINZIGE Vertrauensquelle (§14.2), jede Query darauf gescoped. **Pro Auftrag** (Webhook-`handleOrderCreated`-Felder repliziert): `customer_name` (REPLIZIERTE `buildCustomerName`: first+last → name → external_ref → "Kunde"; [webhook/[secret]/route.ts](app/api/webhook/[secret]/route.ts) **unangefasst**, kein Export), `customer_email`(||null), `customer_phone=null`, `external_ref=id_label`, `item_description=raw_description`, `short_summary` (Haiku, wie Live-Anlage; Fehler ⇒ null, Order trotzdem angelegt), `language=default_language`, `status='draft'`, `consent_given=false`/`consent_at=null` (§13.5). **Idempotenz:** SELECT auf `(business_id, external_ref)` vor Insert ⇒ existiert ⇒ skip (KEIN upsert — keine UNIQUE, nur Index), re-runnbar. **Nicht-blockierend:** je objectId try/catch ⇒ loggen+weiter; `external_ref` fehlt ⇒ skip; `getRoappOrder`-Fehler ⇒ skip+log; `short_summary`-Fehler ⇒ null; Abschluss-Log `created N / skipped M / failed K` je mit `external_ref`. **HARTE GRENZE:** NUR die `draft`-Zeile — KEINE Booklet-Generierung/Reel/E-Mail/Versand/Status über `draft` (`roappOrder.status` gelesen, ignoriert), **KEIN** `analytics_events`-Insert (Backfill ≠ Anlage-Event). `service_role` CLI-only (`.env.local`, dependency-freier Loader wie [upload-ffmpeg.ts](scripts/upload-ffmpeg.ts)), relative Imports (`../lib/...`), kein `any`. Run: `pnpm dlx tsx scripts/import-roapp-orders.ts`. `pnpm typecheck` + `pnpm build` grün.

- **Reichweiten-/VIP-Analyse-Sektion auf dem Dashboard + CSV-Export ✅ (keine Migration, RLS-only, TS-Aggregation)** — zweite Analytics-Sektion auf der `/portal`-Startseite ([app/portal/page.tsx](app/portal/page.tsx)), **pro-Kunde** statt nur business-weit (10b): welcher Auftrag wie viele Personen erreicht hat (eindeutige Aufrufe), plus Gesamt-Öffnungen + Teilen-Aktivitäten, mit Datumsbereich-/Sortier-Filter + CSV-Export der vollen Liste. **Keine Migration.** Ausschließlich **AUTHENTICATED Client** (RLS) + defensiver `business_id`-Filter — **KEIN** `service_role`, `business_id` nur aus Session; **KEIN RPC/View** — Aggregation in TypeScript im Server Component (wie 10b). **(1) Geteilte Aggregation** ([lib/analytics/reach.ts](lib/analytics/reach.ts)): EINE Quelle für Seite (Top 10) UND Export (volle Liste) — `loadReachRows(supabase, businessId, {from,to,sort})`, drei Queries (RLS + defensiver `business_id`-Filter) + TS-Aggregation. **Zeilen** = `orders` mit `status ∈ {sent,viewed,shared}`, die ein **Booklet** haben (1:1 über `order_id`; Booklet liefert `booklet_id` für die Events + `sent_at` als Versanddatum; Aufträge ohne Booklet übersprungen). **Events** = EINE `booklet_events`-Query (`booklet_id,event_type,ip_hash`) im optionalen Datumsbereich (`from` ⇒ `gte $fromT00:00Z`, `to` **inklusiv** ⇒ `lt` Folgetag 00:00Z, UTC-Grenzen, bewusst MVP; ohne Filter = alle), je `booklet_id`: **Reichweite** = distinct nicht-null `ip_hash` bei `viewed` (`Set`), **Gesamt-Öffnungen** = `count(viewed)` (roh, inkl. null-IP), **Teilen-Aktivitäten** = `count(shared)` (alle Kanäle); Booklets ohne Events ⇒ 0. **Sortierung** nach der Aggregation: `reichweite` (Default desc) | `oeffnungen` (desc) | `teilen` (desc) | `versanddatum` (desc, null ans Ende) | `name` (A–Z `localeCompare`), Sekundärschlüssel immer Name (deterministisch). `ReachSort`/`REACH_SORTS`/`isReachSort`/`DEFAULT_REACH_SORT`, `parseDateParam` (YYYY-MM-DD, Round-trip-Check ⇒ null bei unmöglichem Tag), `formatReachDate` (ISO → TT.MM.JJJJ) ebenfalls hier; Client-Typ via `Awaited<ReturnType<typeof createClient>>` (kein `any`). **(2) Spalten** (Tabelle + CSV, gleiche Reihenfolge): Kundenname | E-Mail | roapp-Nr. (`external_ref`) | Beschreibung (`short_summary ?? item_description`) | Booklet-Versanddatum (`booklets.sent_at`) | Reichweite (eindeutig) | Gesamt-Öffnungen | Teilen-Aktivitäten. **(3) Filter** ([components/reach-filter-bar.tsx](components/reach-filter-bar.tsx), kleine Client-Insel, **kein `<form>`**): zwei `type="date"`-Inputs (von/bis, wirken auf `event.created_at`, Default = alles) + Sortier-`<select>`; jede Änderung setzt den Param via **`router.replace`** (bestehende Params über `useSearchParams` erhalten), Server filtert/sortiert. Sortier-Labels über statische `Record<ReachSort,…>`-Map (Muster wie die Quick-Filter, typsicherer `t`-Key). **(4) UI** (`page.tsx`): liest jetzt `searchParams` (`from`/`to`/`sort`, Next-15-`Promise`), parst + ruft `loadReachRows`; neue `<ReachSection>` (Server Component im selben File, Muster wie `Stat`/`BarRow`) unter dem `.dashboard-grid`: Kopf (Titel „Reichweite / VIP-Analyse" + Hinweis + Export-`<a download>`), Filter-Leiste, Tabelle mit den **TOP-10** Zeilen (`rows.slice(0,10)`); leer ⇒ `reach.empty`-Hinweis, Filter bleibt; erscheint im Daten-Zustand (der `!hasData`-Early-Return unberührt); 8-Spalten-Tabelle horizontal scrollbar (`.reach-table-wrap{overflow-x:auto}`). **(5) Export-Route** ([app/portal/analytics/reichweite/export/route.ts](app/portal/analytics/reichweite/export/route.ts), GET): AUTHENTICATED (401/403) + RLS + business-scoped über **dieselbe** `loadReachRows`-Aggregation, honoriert `?from=`/`?to=`/`?sort=` (Anchor mit den aktuellen Params lädt die **VOLLE** Liste, nicht nur 10); unter `/portal/*` ⇒ Middleware-Schutz zusätzlich. **CSV-Ausgabe** `text/csv; charset=utf-8`, **UTF-8-BOM (U+FEFF) vorangestellt**, **Semikolon-getrennt**, **CRLF**, deutsche Header; Felder mit `;`/`"`/Zeilenumbruch in `"…"` gequotet (interne `"` verdoppelt); `Content-Disposition: attachment; filename="reichweite_<YYYY-MM-DD>.csv"` — **BOM+Semikolon, damit österreichisches Excel Umlaute + Spalten korrekt öffnet**. Export enthält Name+E-Mail (eigene Kundendaten, DSGVO-konform) — kein zusätzliches Gate. i18n-Block `reach.*` ([de.ts](lib/i18n/de.ts), keine Inline-Strings; CSV-Header bewusst ausführlicher, Route-lokal), CSS `.reach-*` ([globals.css](app/globals.css)). `pnpm typecheck` + `pnpm build` grün.

- **Teilen-Box auf 3 Aktionen reduziert ✅ (NUR Frontend + i18n, Taxonomie unberührt)** — die über 9a/9b/Block A gewachsene Teilen-Sektion im Outro der öffentlichen Web-Story ([share-bar.tsx](app/b/[token]/share-bar.tsx)) wird verschlankt. **Keine Migration, KEIN Eingriff** in den Event-Endpoint ([app/api/b/[token]/event/route.ts](app/api/b/[token]/event/route.ts)), die Taxonomie ([lib/booklet/events.ts](lib/booklet/events.ts)), den Track-Helfer ([lib/booklet/track.ts](lib/booklet/track.ts)), den Lifecycle oder einen Status-Pfad; `?c=1`-Box-Gating (§9d) + `p=1`-No-Track (§10a.1) unverändert; kein `<form>`, kein `any`. **Ziel-Zustand = Überschrift + GENAU diese 3 Aktionen:** (1) **„Booklet teilen"** (PRIMÄR, zuerst) = die bisherige „Story teilen"-Aktion (`handleShareStory`→`handleShareBooklet`, `navigator.share({url})` mit der **nackten** `storyUrl` ohne Marker §9d, Copy-Fallback) — nur umbenannt + nach oben, feuert **unverändert** `shared/story`, nutzt `.booklet-share-primary` (gold) + zeigt bei Copy-Fallback kurz „✓ Link kopiert" (`copiedKey==="link"`, reuse `share.copied`); (2) **„Als Insta/TikTok-Story teilen"** (zweitens) = die bisherige „Reel teilen"-Aktion (`handleShareReel` unverändert, Reel als **Datei** → IG/TikTok-Composer, Download-Fallback bei Desktop/kein `canShare`) — nur umbenannt, **Reel-Existenz-Gating beibehalten** (nur bei `reelSignedUrl`), feuert **unverändert** `shared/reel`, neue dezentere `.booklet-share-secondary`-Outline-Pill; (3) **„Google-Bewertung schreiben"** = **unverändert** (`link_click/review`, §8.6). **Entfernt (nur UI-Buttons):** „Story teilen" als eigener Button (wird #1), „WhatsApp" (`shared/whatsapp`), „Link kopieren" (`shared/copy`), IG-Caption-Panel (`link_click/ig`, war schon per `SHOW_IG_CAPTION=false` ausgeblendet) — samt Handlern (`handleWhatsApp`/`handleCopyLink`/`copyIgCaption`), Icons (`SendIcon`/`WhatsAppIcon`/`LinkIcon`/`CopyIcon`), `igCaption`-Prop ([page.tsx](app/b/[token]/page.tsx) reicht sie nicht mehr durch; `data.igCaption` wird in [load.ts](lib/booklet/load.ts) weiter geladen, nur nicht angezeigt; `buildIgCaption`-Generierung unangetastet) und `CopiedKey` `"ig"`-Variante. **Taxonomie NICHT angefasst:** `whatsapp`/`copy`/`ig` bleiben in [events.ts](lib/booklet/events.ts) **gültig** (Whitelist + historische Daten + Dashboard intakt) — nur die UI feuert sie nicht mehr. **i18n** ([de.ts](lib/i18n/de.ts)): `share.shareBooklet` neu, `share.shareReel` umbenannt; verwaiste `share.shareStory`/`share.whatsapp`/`share.copyLink` + ganzer `igCaption.*`-Block (sauber ungenutzt) entfernt. **CSS** ([booklet.css](app/b/[token]/booklet.css)): `.booklet-share-secondary` neu, verwaiste `.booklet-share-row`/`.booklet-share-btn`/`.booklet-ig*` entfernt, Primary-Kommentar angepasst. `pnpm typecheck` + `pnpm build` grün.

- **Bugfix: „Als Insta/TikTok-Story teilen" brauchte 2 Taps ✅ (NUR Frontend, keine Migration/Logik)** — auf iOS Safari öffnete der Reel-Datei-Share ([share-bar.tsx](app/b/[token]/share-bar.tsx)) das native Share-Sheet erst beim **zweiten** Tap. **Bestätigte Ursache:** `handleShareReel` machte `await fetch(reelSignedUrl)` → `blob()` → `new File(...)` und rief **erst danach** `navigator.share({files})` — das `await` zwischen User-Geste und `navigator.share` verbraucht die transient activation ⇒ 1. Tap wirft `NotAllowedError` (im `catch` geschluckt), 2. Tap teilt die inzwischen geladene Datei in der frischen Geste. **Fix — Prefetch + synchroner Share:** der bestehende Capability-Probe-`useEffect` lädt die Reel-Datei jetzt **beim Mount** vorab (`fetch`→`blob`→`File` in `reelFileRef`, `AbortController`+`cancelled`-Cleanup) — **nur** wenn `reelSignedUrl` gesetzt **und** `canShare` (sonst greift eh Download über die URL); die ShareBar rendert nur in der Kunden-Sicht (`?c=1`), also genau für den Teiler — Prefetch-Cost fällt nicht bei Empfängern an. `handleShareReel` ist nicht mehr `async` und ruft `navigator.share({files:[reelFileRef.current], title})` **synchron** aus der Geste (kein `await` davor); `reelBusy` entfällt. Neuer `reelLoading = canShareFiles && !reelReady` ⇒ Button `disabled` (vorhandenes `[aria-disabled]`-CSS) + Label `share.sharing`, solange der Prefetch läuft → kein Tippen vor Bereitschaft, 2-Tap-Fall eliminiert. Prefetch-Fehler (Netz/CORS) ⇒ `setCanShareFiles(false)` ⇒ sauberer Download-Fallback (Button bleibt bedienbar). **Unverändert:** Download-Fallback, Capability-Probe + Label-Logik, `shared/reel`-Event feuert beim Tippen, „nur sichtbar wenn `reelSignedUrl`", `?c=1`/`p=1`-Verhalten; keine CSS-Änderung (Disabled-Look existierte schon). Kein `<form>`, kein `any`. `pnpm typecheck` + `pnpm build` grün.

- **i18n: `capture.discard` „Verwerfen" → „Aufnahme verwerfen" ✅ (NUR i18n)** — Wertänderung in [lib/i18n/de.ts](lib/i18n/de.ts), keine Logik/weiteren Dateien/Migration. `pnpm typecheck` + `pnpm build` grün.

- **Entwurfs-Status gesplittet: „Neu" / „In Arbeit" (abgeleitet, KEIN neuer DB-Status, KEINE Migration)** — die Anzeige des DB-Status `draft` wird in **zwei abgeleitete** Zustände aufgeteilt, **ohne** die Status-Maschine zu ändern: **kein** neuer `orders.status`-Wert, **keine** Migration, **kein** Eingriff in Webhook/Lifecycle/Delivery/Check-Constraint. Reine Präsentations-/Filter-Ableitung aus `(status='draft', hat Medien)`: `draft`+0 Medien → **„Neu"** (grau, wie bisher Entwurf), `draft`+≥1 Medium → **„In Arbeit"** (amber, bestehende `--amber-*`-Tokens); `generated`/`sent`/`viewed`/`shared` unverändert. Status-Maschine bleibt `draft → generated → sent → viewed → shared`; `OrderStatus` + DB-Constraint **nicht** erweitert. **(1) Badge** ([components/order-status-badge.tsx](components/order-status-badge.tsx)): optionales Prop `hasMedia` (Default false), wirkt **nur** auf `draft` → Label „In Arbeit" + neuer amber `BadgeStyle` bzw. „Neu" + neutraler Stil; übrige Status unberührt. i18n `orderStatus.draft` „Entwurf"→**„Neu"**, neuer Key `orderStatus.inProgress`→**„In Arbeit"**. **(2) Medien-Existenz = EINE Query (Badge UND Filter)** ([app/portal/orders/page.tsx](app/portal/orders/page.tsx), Muster wie die `reel_status`-Zweitquery): **geschäftsweite** Menge der Entwürfe mit ≥1 Medium via `orders` + `order_media!inner(id)`, `status='draft'`, `business_id`-skopiert (AUTHENTICATED, RLS, **kein** `service_role`) → `Set` (`draftWithMedia`); je Zeile `hasMedia={draftWithMedia.has(order.id)}` ans Badge. Geschäftsweit (nicht seiten-skopiert), weil der „Neu"-Filter ein `id NOT IN (…)` über die GESAMTE Menge braucht (Pagination greift erst nach dem WHERE). **(3) Detailseite** ([app/portal/orders/[id]/page.tsx](app/portal/orders/[id]/page.tsx)): lädt Medien ohnehin ⇒ `hasMedia={media.length > 0}` ans Header-Badge, **keine** Extra-Query. **(4) Status-Filter 6-teilig** ([lib/orders/filters.ts](lib/orders/filters.ts), [order-status-filter.tsx](components/order-status-filter.tsx)): neue Achse `STATUS_FILTERS = ['new','in_progress','generated','sent','viewed','shared']` (Typ `StatusFilter` + `isStatusFilter`) **neben** `ORDER_STATUSES`; Dropdown **Alle | Neu | In Arbeit | Generiert | Gesendet | Angesehen | Geteilt** (`FILTER_LABEL_KEY` mappt `new→draft`/`in_progress→inProgress` auf dieselben `orderStatus.*`-Labels wie das Badge). Server-seitige Übersetzung (Medien-Bedingung **IN** die Query, sonst bricht Pagination): **`new`** → `status='draft'` + (Set nicht leer ⇒ `id NOT IN (draftWithMedia)`); **`in_progress`** → `status='draft'` + `order_media!inner(id)` **direkt in der Hauptquery** (`selectCols` bekommt den Embed nur hier; PostgREST nestet die Kinder ⇒ `count`/`.range()` zählen Eltern-Zeilen ⇒ Pagination korrekt, leere Menge ⇒ inner-Join liefert nichts); **übrige** → `.eq('status', …)` (über `isOrderStatus`-Narrowing). `buildOrdersUrl`-/`OrdersPagination`-`status` nun `StatusFilter | null`; `onChange` nutzt `isStatusFilter`. Der jetzt redundante Quick-Filter **`drafts`/„Entwürfe" entfernt** (`QUICK_FILTERS = ['flagged']`, i18n `orders.quickDrafts` raus); **`flagged`/„Geflaggt" unverändert**; `?status`/`?quick`-Achsen-Mechanik sonst unverändert. **Unverändert:** Warn-Badge/Flag-Logik, `reel_status`-Pill (nur `generated`), Webhook, Lifecycle, Delivery, Constraint. Kein `<form>`, kein `any`, `--amber-*` wiederverwendet. `pnpm typecheck` + `pnpm build` grün.

- **Video-Vorschau-Frames — Phase 1: Extraktion (client) + Verifizieren ✅ (keine Migration, Konventions-Pfad, RLS-only)** — beweist, dass auf iOS Safari aus einem frisch aufgenommenen Video **3 brauchbare (nicht schwarze) Standbilder** entstehen und sicher im Storage landen. **Phase 2** (noch NICHT gebaut) schickt diese Frames an die KI (Vision-Input fürs Caption/Reel-Frame); Phase 1 fasst die Caption-Pipeline (`captions.ts`/`media-caption.ts`/Caption-Routes/Booklet) **NICHT** an — Frames gehen noch **nicht** an die KI. **Keine Migration, keine DB-Zeile:** Frames sind **reine Storage-Objekte** unter einem **Konventions-Pfad**, Existenz allein über den Pfad. **(1) Pfad-Helfer** ([lib/media/video-frames.ts](lib/media/video-frames.ts), kein DOM/Server-Import → client+server-importierbar): `VIDEO_FRAME_POSITIONS = [0.1,0.5,0.9]` (≈10/50/90 % — bewusst nicht 0/100 %, dort oft schwarz), `videoFramePath(videoStoragePath, i)` = `{video-pfad-ohne-endung}.frame-{i}.jpg`, `videoFramePaths()`; Frames behalten das `{business_id}/{order_id}/`-Präfix des Videos ⇒ **Storage-RLS 0002** greift unverändert, Eindeutigkeit über das Video-`{uuid}`. **(2) Extraktion** ([lib/media/extract-frames.ts](lib/media/extract-frames.ts), Client/DOM): `extractVideoFrames(file) → {index,blob}[]` — verstecktes `<video>` (gemutet, `playsInline`, `preload="auto"`) an `duration*position` seeken → `seeked` abwarten → **2× rAF** (iOS-Paint) → `drawImage` auf Canvas (skaliert wie Foto-Kompression, `MAX_IMAGE_DIM`/`JPEG_QUALITY`) → JPEG; DRY-Helfer `scaledSize`/`canvasToJpeg` aus [compress.ts](lib/media/compress.ts) **exportiert+wiederverwendet**. **Schwarz-/Leer-Guard** (`isMostlyBlack`, ~4 k Pixel-Stichprobe, > 98 % Luma < 16 ⇒ verworfen). **Best-effort+graceful:** jeder Fehler (Metadaten-/Seek-Timeout, Decode, nur-schwarz) ⇒ weniger/keine Frames, **nie** ein Throw nach außen (Seek/Metadaten timeout-geschützt). **(3) Upload** ([capture.tsx](app/portal/orders/[id]/capture.tsx)): `extractAndUploadFrames` läuft in `runUpload` **nach** dem Video-Upload+`router.refresh()` (Video sofort sichtbar), **fire-and-forget**; jeder gültige Frame per **`uploadWithRetry`** (wiederverwendet) direkt in `order-media` unter `videoFramePath(...)` (`image/jpeg`, `upsert`), danach **einmal** `router.refresh()`. **ISOLATION:** Direkt-Upload unter dem RLS-skopierten `{business_id}/{order_id}/`-Präfix (aus dem Video-Pfad), **keine** business_id aus dem Client, **kein** Metadaten-POST (keine `order_media`-Zeile). Schlägt alles fehl ⇒ Video bleibt unverändert (heutiges Verhalten), nur ohne Frames — **kein Breaking**. **(4) Verifizier-Ansicht** ([page.tsx](app/portal/orders/[id]/page.tsx) + [media-list.tsx](app/portal/orders/[id]/media-list.tsx)): Server listet den Auftrags-Ordner `{business_id}/{order_id}` **einmal** (`list()`, AUTHENTICATED, RLS + 0002-Präfix-Policy, **kein** service_role) → vorhandene Pfade-Set → je Video die existierenden `videoFramePaths` → **batch** `createSignedUrls` (3600 s) → `MediaWithUrl.frameUrls: string[]` (Foto/frameloses Video ⇒ leer). Client: im **Vollbild-Video-Viewer** bei `frameUrls.length>0` eine kleine **Thumbnail-Reihe** (72×96, i18n `assembler.videoFrames` „Erkannte Video-Bilder") unter dem Medium — damit am iPhone **sichtbar** ist, dass echte Frames entstanden sind. **Reine Anzeige** (keine eigenen Kacheln). Kein `<form>`, kein `any`. `pnpm typecheck` + `pnpm build` grün.

- **Video-Vorschau-Frames — Phase 2: Frames als Vision-Input in die Caption-Generierung ✅ (keine Migration, RLS-only, kein service_role)** — die in Phase 1 extrahierten Frames speisen jetzt die **KI-Caption-Erzeugung** (Haiku). **Phase-1-Extraktion/-Upload/-Anzeige unverändert; Foto-Caption-Pfad unverändert** (außer dem unten geteilten Guard); **Routes-Auth/Isolation, Lifecycle, Webhook unverändert**. **Keine Caption-Route-Änderung** — die Routes erben alles über `captionForMedia` → `generateCaption`. **(1) Frame-Lader** ([lib/ai/media-caption.ts](lib/ai/media-caption.ts), `loadVideoFrames`, Muster wie der Foto-Download): für ein Video die ≤ 3 Konventions-Pfade via `videoFramePaths(storage_path)` (Phase 1) ableiten, jeden über den **AUTHENTICATED** Client (RLS) aus dem privaten `order-media`-Bucket `download`-en → base64; nicht vorhandene (404 ⇒ `data` null) **überspringen**, Reihenfolge der vorhandenen erhalten. `captionForMedia` lädt im Video-Zweig die Frames und reicht sie an `generateCaption({…, frames})`; **keine Frames ⇒ Stichwort-only** (bisheriges Verhalten). **ISOLATION:** dieselbe `{business_id}/{order_id}/`-Präfix-Policy (0002) wie der Foto-Download, **kein** `service_role`. **(2) `generateCaption`-Video-Erweiterung** ([lib/ai/captions.ts](lib/ai/captions.ts)): `CaptionInput.frames?: string[]` (base64); liegt **≥ 1 Frame** vor ⇒ mehrere `{type:'image'}`-Blöcke + Stichwort an Haiku — **GENAU wie der Foto-Pfad**, nur mehrere Bilder (Frames sind per Konvention immer JPEG ⇒ `image/jpeg`); Prompt nennt den Clip „gezeigt als mehrere Standbilder daraus" (Haiku behandelt sie als **einen** Clip). **0 Frames:** unverändert — Stichwort-only, Video **ohne Frames UND ohne Stichwort** ⇒ leerer String ohne API-Call. Kein `any` (`frames: string[]`, Bild-Liste `{data, mediaType: ImageMediaType}`). **(3) Stufe-1-Guard gegen Meta-Antworten** (`isMetaResponse`, [lib/ai/captions.ts](lib/ai/captions.ts)): **zentral in `generateCaption`** auf dem **Roh-Text** (vor `cleanCaption`/Kürzung) ⇒ deckt **FOTO UND VIDEO** ab (beide Medientypen laufen durch dieselbe Funktion). Sieht die Antwort wie eine Rückfrage/Meta aus statt einer Caption — **enthält `?`**, **beginnt** mit Meta-/Ich-/Bitte-/Entschuldigungs-Präfix (`ich `, `bitte`, `leider`, `tut mir leid`, `entschuldigung`, `als ki`, …) oder ist **auffällig lang/satzartig** (> 40 Wörter statt ~12–15-Wort-Fragment) — ⇒ als **leer** behandelt (`""`), **NICHT gespeichert**; Kachel bleibt „Caption fehlt" zum manuellen Nachtragen. **(4) Routes** (Batch/Regenerate, [captions/route.ts](app/api/portal/orders/[id]/captions/route.ts) / [regenerate/route.ts](app/api/portal/orders/[id]/media/[mediaId]/caption/regenerate/route.ts)): **unverändert** — erben Frame-Vision + Guard über `captionForMedia`; leere Caption wird wie gehabt nicht gespeichert (Batch `if (!caption) return null`, Regenerate `"" ⇒ null`). AUTHENTICATED + RLS, kein `service_role`, kein `<form>`, kein `any`. `pnpm typecheck` + `pnpm build` grün.

- **Auftragskontext (`item_description`) als Caption-Guardrail ✅ (keine Migration, RLS-only, kein service_role, KEINE Route-Änderung)** — die KI-Caption-Erzeugung (Haiku) bekommt den Auftragskontext (`orders.item_description`) als **Guardrail**, damit Captions thematisch zum Auftrag passen, ohne den Kontext mit dem Bildinhalt zu verwechseln. **(1) `generateCaption`** ([lib/ai/captions.ts](lib/ai/captions.ts)): optionales `CaptionInput.orderContext?: string`; liegt es vor, hängt `orderContextBlock` im **User-Prompt** (Text-Block, hinter Stichwort + `categoryHint`) einen klar abgegrenzten KONTEXT-Block an — **analog zum `<<<…>>>`-Block in [intro.ts](lib/ai/intro.ts)/[review.ts](lib/ai/review.ts)**. Konkreter Block: `` Auftragskontext: <<<{orderContext}>>> Dieser Kontext ist ein Guardrail — er beschreibt den Auftrag, nicht das Bild. Beschreibe was auf diesem Bild/diesen Frames konkret zu sehen ist, bleibe dabei im Rahmen des Auftragskontexts. Übernehme den Auftragskontext NICHT wörtlich als Caption.`` Leer/`undefined` ⇒ **kein** Block (leerer String), Verhalten exakt wie bisher — kein Fallback, kein leerer `<<<>>>`-Block; gilt FOTO **und** VIDEO (beide laufen durch `generateCaption`). System-Prompt + `isMetaResponse`-Guard unverändert. **(2) `captionForMedia`** ([lib/ai/media-caption.ts](lib/ai/media-caption.ts)): lädt zusätzlich `orders.item_description` über den übergebenen **AUTHENTICATED** Client (RLS — fremde/fehlende Order ⇒ kein Treffer) und reicht es als `orderContext` an **alle** `generateCaption`-Aufrufe (Video / Foto-ohne-Blob / Foto-mit-Bild). Die `order_id` wird **defensiv** aus dem Storage-Pfad abgeleitet (`orderIdFromStoragePath` = zweites Segment von `{business_id}/{order_id}/{uuid}.ext`; der Pfad ist beim Upload serverseitig auf dieses Präfix validiert) — daher **KEINE** Änderung an `CaptionableMedia` oder den Routes nötig. Ohne gültige id / ohne (getrimmte) Beschreibung ⇒ `undefined` (kein Block). **(3) Routes** (Batch [captions/route.ts](app/api/portal/orders/[id]/captions/route.ts) + Regenerate [regenerate/route.ts](app/api/portal/orders/[id]/media/[mediaId]/caption/regenerate/route.ts)): **unverändert** — erben den Auftragskontext automatisch über `captionForMedia` → `generateCaption`. Die Daten-Abgrenzung des Guardrail-Blocks deckt sich mit dem deferierten Härtungspunkt P4 (Prompt-Injection-Schutz für `item_description`). Kein `<form>`, kein `any`. `pnpm typecheck` + `pnpm build` grün.

- **Bugfix: „Neu generieren" nicht-destruktiv ✅ (keine Migration, RLS-only, kein service_role)** — behebt Caption-Verlust bei leerem/Meta-KI-Ergebnis. **Route** ([regenerate/route.ts](app/api/portal/orders/[id]/media/[mediaId]/caption/regenerate/route.ts)): leeres `generateCaption`-Ergebnis (nach `isMetaResponse`-Guard, Video ohne Stichwort/Frames o. ä.) ⇒ **kein DB-Update**, Antwort `{ id, caption: null, empty: true }` — die bestehende Caption in der DB bleibt unangetastet. Nicht-leer: Verhalten unverändert (überschreibt). **Client** (`CaptionEditor`, [media-list.tsx](app/portal/orders/[id]/media-list.tsx)): `handleRegenerate` wendet ein leeres/`empty`-Ergebnis **nicht** an (`setText`/`onCaptionChange` bleiben aus), zeigt stattdessen `captions.emptyResult` „Keine Bildunterschrift erzeugt – bitte manuell ergänzen." im Feedback-Label; Feld und Kachel-Indikator unangetastet. **Echo-Clobber-Race (§4):** `textarea` ist `disabled={busy}` während eines laufenden Requests — kein zurückkommendes `setText` überschreibt während des Requests getippte Zeichen. i18n `captions.emptyResult` neu. Batch-Route, `isMetaResponse`-Guard, Lifecycle/Webhook **unverändert**. `pnpm typecheck` + `pnpm build` grün.

- **Bugfix: HEIC-Foto vom iPhone-Kameraroll-Upload schlägt „sofort" fehl ✅ (keine Migration, kein `any`, AUTHENTICATED + RLS unverändert)** — Kameraroll-Fotos sind auf iOS standardmäßig **HEIC/HEIF**; **bestätigte Ursache:** [compress.ts](lib/media/compress.ts) dekodierte ausschließlich über `createImageBitmap(file, { imageOrientation: "from-image" })` ([compress.ts:17](lib/media/compress.ts#L17), alt) — manche iOS-Versionen können HEIC darüber **nicht** dekodieren und werfen sofort; der Throw fiel in die „Schritt 0 — Bildaufbereitung"-Catch in [capture.tsx](app/portal/orders/[id]/capture.tsx) → `markError()` → Item kippt instant auf „error" mit dem generischen `capture.uploadError` (= der „sofort"-Fehler). **KEINE** MIME-Sperre irgendwo (`handlePhotoFile`/`compressImage` akzeptieren alles, Input bleibt `accept="image/*"`); HEIC wird **angenommen** und scheitert erst beim Dekodieren. **Frische Live-Aufnahmen** (`capture="environment"`) sind JPEG und **nicht betroffen** (nur Kameraroll-/Galerie-Upload). **Anmerkung zum Bug-Report:** der Report nahm an, der Code nutze `<img src=objectURL>` und solle auf `createImageBitmap` umstellen — es ist **umgekehrt** (Code nutzte schon `createImageBitmap`); der zuverlässige HEIC-Pfad auf iOS ist gerade das **`<img>`-Element** (die Entwurf-Vorschau rendert HEIC bereits über `<img>` ⇒ iOS-Safari-`<img>`-Decoder kann HEIC). **Fix** (`compressImage`): `createImageBitmap` bleibt schneller Primärpfad; scheitert er, greift ein **`<img>`-Element-Fallback** (`compressViaImageElement`: objectURL → `onload` → gemeinsamer `drawScaledToJpeg`-Schritt), den iOS Safari für HEIC nativ unterstützt. Scheitern **beide** und ist die Datei HEIC/HEIF (MIME oder `.heic`/`.heif`-Endung) ⇒ neuer typisierter `UnsupportedImageError` (älteres iOS < 17). `capture.tsx` fängt diesen ab und zeigt den **konkreten, ehrlichen** Hinweis `capture.heicUnsupported` (Tipp: iPhone-Einstellungen → Kamera → Formate → „Maximale Kompatibilität" = JPEG) als `notice`-Banner statt des generischen Fehlers; `markError()` bleibt. **`scaledSize`/`canvasToJpeg` weiter exportiert** (Frame-Extraktion [extract-frames.ts](lib/media/extract-frames.ts) unberührt). **Nicht angefasst:** Video-Upload-Pfad, `capture="environment"`-Pfad, Frame-Extraktion, Caption-Pipeline. i18n `capture.heicUnsupported` neu. `pnpm typecheck` + `pnpm build` grün.

- **Direkt-Aufnahme-Buttons aus dem Capture entfernt ✅ (NUR Frontend + i18n, keine Migration/Logik)** — die zwei „Foto/Video **aufnehmen**"-Buttons (versteckte `<input … capture="environment">`-Inputs) sind aus [capture.tsx](app/portal/orders/[id]/capture.tsx) **entfernt**; es bleiben nur „Foto/Video **hochladen**" (`<input type="file" accept="image/*|video/*">` **ohne** `capture`). Begründung: der native System-Picker dieser Upload-Inputs öffnet auf Mobile ohnehin ein Popup, das **Kamera + Galerie** abdeckt — die separaten Kamera-Buttons waren redundant. **Mit aufgeräumt** (tot nach der Entfernung): Refs `photoInputRef`/`videoInputRef`, Öffner `openPhoto`/`openVideo`, die beiden `capture="environment"`-Inputs, die erste `.capture-row` (Kamera-Buttons), die Icon-Komponenten `CameraIcon`/`VideoIcon` und die i18n-Keys `capture.photo`/`capture.video`. **NICHT angefasst:** die geteilten Handler `handlePhotoFile`/`handleVideoFile`, die gesamte nachgelagerte Pipeline (Kompression / Längen-Check / Hintergrund-Upload + Queue/Retry / Metadaten-POST), Frame-Extraktion, Limit-/Kategorie-Logik, `UploadIcon`, `capture.uploadPhoto`/`capture.uploadVideo` — die Upload-Buttons hängen weiter an denselben Handlern. Kein `<form>`, kein `any`. `pnpm typecheck` + `pnpm build` grün.

- **Bugfix: Video-Tile-Regression (zweiter router.refresh nach Frame-Extraktion) ✅ (NUR Frontend, keine Migration/Logik)** — der zweite `router.refresh()` am Ende von `extractAndUploadFrames` ([capture.tsx](app/portal/orders/[id]/capture.tsx)) re-signierte alle Signed-URLs → swappte `<video src>` aller Kacheln → iOS-Video-Layer malte über die z-index-Controls + Poster-Reload. **Kern-Fix (A, deterministisch):** nach dem Frame-Upload ruft `extractAndUploadFrames` jetzt **`createSignedUrls`** (batch, AUTHENTICATED, RLS) für die eben hochgeladenen Frame-Pfade auf und übergibt die URLs via `onFramesUploaded?.(storagePath, urls)` — **kein `router.refresh()`** mehr. Neuer **Client-Wrapper `MediaSection`** ([media-section.tsx](app/portal/orders/[id]/media-section.tsx), `"use client"`): hält `extraFrames: Map<mediaId, string[]>` State, nimmt `onFramesUploaded`-Callback von `Capture` entgegen (findet die media.id per stable `itemsRef` via `storage_path`), merged die URLs in die `initialItems` bevor sie an `MediaList` gehen; entfernt supplementale Einträge sobald der Server nach dem ersten `router.refresh()` server-seitige `frameUrls` mitliefert (Duplikat-Schutz). `page.tsx` rendert jetzt `<MediaSection>` statt `<Capture>` + `<MediaList>` getrennt. **Härtung (A, billig):** `.media-tile-delete` + `.media-tile-indicator` bekommen `transform: translateZ(0)` ([globals.css](app/globals.css)) → eigene GPU-Compositing-Layer → rendert zuverlässig über dem `<video>`-Layer auf iOS. **Display-Fix (B):** `MediaViewer` in [media-list.tsx](app/portal/orders/[id]/media-list.tsx) bekommt `overflowY: "auto"` am Outer-Container; Medien-Bereich von `flex:1/minHeight:0` auf `flexShrink:0` + `maxHeight:"55dvh"` an Video/Bild — Kategorie, Caption-Editor und Frame-Thumbnails auf kurzen Handy-Schirmen ohne Scrollen erreichbar. Kein `<form>`, kein `any`, AUTHENTICATED + RLS (createSignedUrl mit Session). `pnpm typecheck` + `pnpm build` grün.

- **Video-Ton aus + Speichern schließt den Caption-Viewer ✅ (NUR Frontend, keine Migration/Logik)** — zwei kleine UX-Fixes. **(1) `muted` auf allen Playback-`<video>`-Elementen:** Vollbild-`MediaViewer` ([media-list.tsx](app/portal/orders/[id]/media-list.tsx)), Capture-Entwurf-Vorschau ([capture.tsx](app/portal/orders/[id]/capture.tsx)) und der Clip in der öffentlichen Web-Story ([app/b/[token]/page.tsx](app/b/[token]/page.tsx)). Tile-Poster und Queue-Thumbnail waren bereits stumm; das **Reel ist seit 8b-1a mute-by-design** (FFmpeg `-an`, Audio-Strip) — das Booklet trägt seine Story über Bild + Caption, nicht über Ton. **(2) Caption-Viewer schließt nach dem Speichern automatisch:** `CaptionEditor` ([media-list.tsx](app/portal/orders/[id]/media-list.tsx)) bekommt einen `onClose?.()`-Prop und ruft ihn **nach erfolgreichem PATCH-Speichern** auf → der `MediaViewer` schließt automatisch. **Nur bei Erfolg** — **nicht** bei „Neu generieren" (Regenerate) oder leerem KI-Ergebnis (dort bleibt der Viewer offen). Kein `<form>`, kein `any`. `pnpm typecheck` + `pnpm build` grün.

- **Leere Vorher/Nachher-Box startet den Foto-Upload für ihre Kategorie ✅ (NUR Frontend, keine Migration/Logik)** — Klick auf einen **leeren** Vorher-/Nachher-Slot öffnet den bestehenden Foto-Picker und weist das hochgeladene Foto **direkt** dieser Kategorie zu. **STOPP-Schalter NICHT ausgelöst:** die Kategorie wandert bereits end-to-end durch die Upload-Pipeline (Entwurf-`category` → Metadaten-`POST` `category` → `resolveCategory` → Insert, 0010) — die Zuweisung beim Upload ist also nur ein **Durchreich-Parameter**, keine Statusmaschine/Migration. **Boxen:** die `BeforeAfterSlot`-Slots ([media-list.tsx](app/portal/orders/[id]/media-list.tsx), `.media-ba-row`) zeigen Medien nach `category`; der leere Zweig hatte bisher **kein** `onClick`. **Kategorie-Zuweisung heute:** beim Upload (Entwurf-Selektor in [capture.tsx](app/portal/orders/[id]/capture.tsx), Default `process`, nur Foto) **und** post-upload (Viewer-Toggle → PATCH `…/media/[mediaId]/category`). **Gemeinsamer Handler, KEINE Duplikation:** der leere Slot ruft denselben `handlePhotoFile`/`runUpload`-Pfad — die Box-Klicks werden über eine Brücke an Capture verdrahtet. **(1) `capture.tsx`:** neuer `pendingPhotoCategoryRef` (Default `process`); `handlePhotoFile` setzt `category: pendingPhotoCategoryRef.current` (statt hartkodiert `"process"`) und resettet danach; Buttons öffnen via `openPhotoUpload` weiter als `process` (unverändertes Verhalten); neuer `openPhotoForCategory(category)` (useCallback) mit **denselben UX-Guards** wie die Buttons (belegter Slot ⇒ `capture.categoryTakenNotice`, Foto-Limit ⇒ `capture.limitReached`; der harte Riegel bleibt der Server-Guard `category_taken`/`limit_reached`), setzt den Ref und klickt den **bestehenden** versteckten Foto-Input; neuer optionaler Prop `openPhotoControlRef` + Effekt, der diesen Opener registriert (Aufruf synchron im Box-Klick ⇒ **User-Geste bleibt erhalten**, Datei-Picker öffnet zuverlässig; null sobald Capture unmountet). **(2) `media-section.tsx`** (Bridge): hält `openPhotoControlRef`, gibt ihn an `Capture` und reicht `onRequestPhotoForSlot(category)` an `MediaList` (ruft `openPhotoControlRef.current?.(category)`). **(3) `media-list.tsx`:** neuer Prop `onRequestPhotoForSlot`; `BeforeAfterSlot` bekommt `onAddPhoto` — der **leere** Slot ist im Editier-Modus tappbar (`role="button"`, Enter/Space, `+`-Icon `PlusIcon`, Hinweis `assembler.slotAddHint`, aria `assembler.slotAdd {category}`) und startet den Upload für genau diese Kategorie; **readOnly oder ohne Handler ⇒ reiner Platzhalter** wie bisher; **gefüllte Slots unverändert**. Der Entwurf-Dialog zeigt die Kategorie **vorbelegt** (z. B. „Vorher") und bleibt änderbar. CSS `.media-ba-empty--add` (Cursor + Gold-Hover). i18n `assembler.slotAdd`/`slotAddHint`, `capture.categoryTakenNotice`. Kein `<form>`, kein `any`, AUTHENTICATED + RLS. `pnpm typecheck` + `pnpm build` grün.

- **Kunden-Kontakt (E-Mail + Telefon) auf der Detailseite editierbar ✅ (keine Migration)** — `customer_email`/`customer_phone` (0001) waren bisher nur **read-only** als `MetaRow` sichtbar, `customer_phone` nach der Anlage **nirgends** editierbar; es gab **keine** Order-PATCH-Route (alle `[id]`-PATCH waren media-only). **(1) Neue Route** [app/api/portal/orders/[id]/route.ts](app/api/portal/orders/[id]/route.ts) (`PATCH`): erste Order-PATCH-Route — **keine** statische/dynamische Kollision (`[id]` ist bestehendes dynamisches Segment, `route.ts` direkt darin = Standard-Handler für `/api/portal/orders/[id]`). **ISOLATION:** AUTHENTICATED Server-Client (**kein** `service_role`), 401/403 ohne User/Betrieb; Order über RLS geladen (fremde/fehlende id ⇒ 404), `business_id` aus der geladenen Order (**nie** Body), Update defensiv auf `id` + `business_id` gefiltert. **Partielles PATCH:** nur die im Body enthaltenen Felder (`"customer_email" in payload` / `"customer_phone" in payload`); keines vorhanden ⇒ 400 `no_fields`. **E-Mail:** getrimmt, leer ⇒ `null` (entfernen erlaubt), gesetzt ⇒ Format via `isEmailFormat` (sonst 400 `invalid_email`). **Telefon:** Freitext, getrimmt, leer ⇒ `null`, **KEINE Normalisierung** (erst beim SMS-Versand). Antwort `{ customer_email, customer_phone }`. **(2) UI** [contact-editor.tsx](app/portal/orders/[id]/contact-editor.tsx) (Client, `div + onClick`/`<button>`, **kein `<form>`**) ersetzt die zwei read-only `MetaRow`s im Stammdaten-Block ([page.tsx](app/portal/orders/[id]/page.tsx)): **Anzeige-Modus** = belegte Felder als Zeilen (leer ⇒ Zeile nicht zeigen, wie bisher) + dezenter **„Kontakt bearbeiten"**-Button (IMMER sichtbar, auch wenn beide leer ⇒ Nachtragen möglich); **Editier-Modus** = E-Mail-/Telefon-Felder (`form-input`, auch wenn leer) + Speichern/Abbrechen, Client-Validierung der E-Mail = Server (`isEmailFormat` aus [lib/settings/options.ts](lib/settings/options.ts), bereits client-importierbar). Speichern ⇒ `PATCH` ⇒ lokale Anzeige aktualisiert + `router.refresh()` (hält den server-gerenderten Rest konsistent, z. B. `hasEmail`-Warnung des Ausliefern-Buttons). i18n-Block `contact.*`. Kein `<form>`, kein `any`. `pnpm typecheck` + `pnpm build` grün.

- **Feature 3a ✅ BulkSMS-Versand + Kanal-Logik (NUR manueller Auslieferungs-Pfad)** — der manuelle Auslieferungs-Pfad (9c-1) bekommt einen **SMS-Fallback**: kein `customer_email`, aber `customer_phone` ⇒ Booklet-Link per **BulkSMS**. **Der Webhook-Auto-Pfad (3b) ist NICHT angefasst** ([webhook/[secret]/route.ts](app/api/webhook/[secret]/route.ts) ruft weiter direkt `sendBookletEmail`; 3b folgt separat). **Keine Migration** (`customer_phone`/`external_ref` aus 0001, `settings.contact_phone` aus dem `settings`-jsonb), kein `<form>`, kein `any`, AUTHENTICATED + RLS; `service_role` nur für den **unveränderten** Billing-Insert. **(1) BulkSMS-Client** ([lib/sms/bulksms.ts](lib/sms/bulksms.ts), server-only): `POST https://api.bulksms.com/v1/messages`, **HTTP Basic Auth** = base64(`BULKSMS_TOKEN_ID:BULKSMS_TOKEN_SECRET`) — server-only Envs, NIE `NEXT_PUBLIC`/Client (Muster wie [resend.ts](lib/email/resend.ts)); Body `{ to, body, from? }`, Guard `isSmsConfigured()`, AbortController-Timeout 10 s. **Fehlerverhalten bewusst:** AUTH-/401-Fehler (falsches ID/Secret-Paar) werden **KLAR geloggt UND zurückgegeben** (`{ ok:false, error }`), **NICHT geschluckt** — kaputte Config muss sofort sichtbar sein; `sendSms` wirft nie (`SmsSendResult`). **(2) Telefon-Normalisierung** ([lib/sms/phone.ts](lib/sms/phone.ts), plain): `normalizePhone` → **BulkSMS-MSISDN** (reine Ziffern, beginnend mit Ländervorwahl, **OHNE `+`/`00`/führende `0`**, z. B. `436645785569` / `491721627058`), **Default-Land AT (43)**; Whitespace/Bindestriche/Klammern/Punkte **und ein evtl. führendes `+`** raus, danach nur reine Ziffern erlaubt (sonst Müll ⇒ Fehler); `00…`→`00` abschneiden, führende `0…`→`0` durch `43` ersetzen, Ziffer 1–9 vorn → bereits MSISDN (unverändert), **alles andere ⇒ Fehler** (keine Müll-Nummer senden); finale Validierung `\d{8,15}`; Rückgabe `{ ok, msisdn }`. Auch der Absender (`settings.contact_phone`) wird so normalisiert (ebenfalls ohne `+`). *(FIX nach 3a: ursprünglich E.164 mit `+`/Feld `e164` — BulkSMS erwartet MSISDN ohne `+`; Testfälle als Doc-Beispiele im Code verankert.)* **(3) Booklet-SMS** ([lib/sms/booklet-sms.ts](lib/sms/booklet-sms.ts), server-only): `sendBookletSms(order, business, link)` — Empfänger = normalisierte `order.customer_phone` (scheitert ⇒ kein Versand), Absender = normalisierte `settings.contact_phone` (best effort: scheitert ⇒ ohne expliziten Absender → BulkSMS-Standard); Text mit **EXAKTEN Zeilenumbrüchen** (6 Zeilen, Z. 2 + Z. 5 leer) + Schreibweise „Spass"/„Grüsse": Z.1 `business.name.toUpperCase()`, Z.3 „Wir haben für Sie ein Booklet Ihrer Änderung {external_ref} angefertigt. Viel Spass beim Ansehen!", Z.4 `link` (= `bookletShareLink`), Z.6 „Liebe Grüsse!". **(4) Geteilte Kanal-Logik** ([lib/delivery/deliver-booklet.ts](lib/delivery/deliver-booklet.ts), server-only): `deliverBooklet(order, business, link, supabase)` → `{ channel, ok, error? }` — **E-Mail bevorzugt** (`customer_email` → `sendBookletEmail`, bestehend) → **sonst SMS** (`customer_phone` → `sendBookletSms`) → **sonst `{ channel:'none', ok:false }`** (kein Kontakt, nichts senden); `supabase` ist Teil der einheitlichen Signatur (spätere Versand-Persistenz), **aktuell ungenutzt** (Status/Billing/`sent_at` bleiben im Route Handler). **(5) Manueller Pfad** ([deliver/route.ts](app/api/portal/orders/[id]/deliver/route.ts)): der frühere E-Mail-Block (Schritt 4) ist durch **einen** `deliverBooklet`-Aufruf ersetzt; **Status-/Billing-/`sent_at`-Sequenz UNVERÄNDERT** (repliziert; `business_id` weiter NUR aus der RLS-geladenen Order, §14.2), Order-Query lädt zusätzlich `customer_phone`+`external_ref`; Versand weiter **NICHT-BLOCKIEREND** (`sent` steht in jedem Fall), das Ergebnis (`{ delivery: { channel, ok, error? } }`) wandert in die Antwort, damit der Operator den Kanal bzw. Fehlergrund sieht. **(6) Ausliefern-Button** ([deliver-controls.tsx](app/portal/orders/[id]/deliver-controls.tsx) + [page.tsx](app/portal/orders/[id]/page.tsx)): neuer Prop `hasPhone`, **„kann zustellen?" = `hasEmail || hasPhone`**; Bestätigung `confirmText` (E-Mail) bzw. `confirmTextSms` (SMS), ohne beide Wege `noContact` (QR); nach Erfolg einmaliger `window.alert` (`sentEmail`/`sentSms`/`noContactSent`/`sendFailed {reason}`) → `router.refresh()`. i18n `deliver.confirmTextSms`/`noContact`/`sentEmail`/`sentSms`/`noContactSent`/`sendFailed` (ersetzt `noEmail`/`emailFailed`). **Env:** `BULKSMS_TOKEN_ID`/`BULKSMS_TOKEN_SECRET` ([.env.example](.env.example)). `pnpm typecheck` + `pnpm build` grün.

- **Feature 3b ✅ deliverBooklet in den Webhook-Auto-Pfad verdrahtet (NUR die eine Versand-Stelle)** — der Webhook-Auto-Pfad ([webhook/[secret]/route.ts](app/api/webhook/[secret]/route.ts), `handleOrderPickedUp`) nutzt jetzt denselben geteilten `deliverBooklet`-Helfer (3a) wie der manuelle Pfad: ein roapp-„Abgeholt"-Auftrag erreicht den Kunden über den **passenden Kanal** (E-Mail bevorzugt, sonst SMS) statt nur per E-Mail. **Keine Migration, kein neuer Status, kein `any`.** **Geändert: NUR der direkte `sendBookletEmail`-Aufruf (Schritt 4)** ist durch **einen** `deliverBooklet(order, business, bookletUrl, service)`-Aufruf ersetzt — mit dem bereits aufgelösten `service_role`-Client. **Status-/Billing-/`sent_at`-Sequenz UNVERÄNDERT** (Schritte 1–3 bleiben repliziert: `generated→sent` defensiv gefiltert + `count`-Check gegen Doppelversand, dann `sent_at` + Billing-Event `'booklet_sent'`, nicht-blockierend; `business_id` weiter NUR aus der service_role-aufgelösten Order, §14.2). Order-Query lädt zusätzlich `customer_phone` (SMS-Kanal); `external_ref` ist schon Funktions-Parameter und fließt in den `DeliverableOrder`. **business-Parameter strukturell verengt:** `deliverBooklet`/`sendBookletSms` nehmen jetzt `Pick<CurrentBusiness, "name" | "settings">` statt der vollen `CurrentBusiness` — der manuelle Pfad reicht weiter eine `CurrentBusiness` durch (strukturell zuweisbar), der Auto-Pfad baut den Ausschnitt aus `{ name: business.name, settings: normalizeSettings(business.settings) }` (**keine** Duplikation der `CurrentBusiness`-Assemblierung, keine zusätzlichen Spalten-Loads); der reservierte/ungenutzte `supabase`-Parameter akzeptiert beide Clients (gleicher `SupabaseClient`-Typ — Service-Client zuweisbar). **Kein Operator im Loop ⇒ Ergebnis `{channel, ok, error?}` KLAR geloggt:** Erfolg `console.log("webhook: auto delivery ok", {business_id, order_id, channel})`, Fehlschlag `console.error("webhook: auto delivery failed", {business_id, order_id, step:"deliver", channel, message: error ?? "no_contact"})` (in Vercel-Logs diagnostizierbar). **Versand bleibt NICHT-BLOCKIEREND** (`sent` steht in jedem Fall); Response-Status-String je Ergebnis: `sent` (ok) / `sent_no_contact` (Kanal `none`) / `sent_delivery_failed` (E-Mail/SMS fehlgeschlagen) — ersetzt das frühere `sent`/`sent_no_email`. **E-Mail-Auto-Zustellung unverändert:** `deliverBooklet` versendet bei gesetztem `customer_email` per `sendBookletEmail` (gleiche reply-to-Logik); SMS ist nur der Fallback ohne E-Mail. **Sonst NICHTS angefasst** (`handleOrderCreated`, Status-Maschine, Webhook-Robustheit/404-Gate, manueller Pfad, `scripts/import-roapp-orders.ts`). `pnpm typecheck` + `pnpm build` grün.

- **Schritt 2/Teil 2 ✅ roapp-Telefon übernehmen + leere E-Mail glattziehen + Debug-Log raus** — der order.created-Auto-Pfad speichert jetzt die Kunden-Telefonnummer aus roapp (für den SMS-Kanal 3a/3b), leere E-Mails landen sauber als `null`, und das temporäre Diagnose-Log ist entfernt. **Keine Migration, kein `any`**, `scripts/import-roapp-orders.ts` bewusst **unangetastet** (bleibt `customer_phone=null`). Log-Befund (Teil 1): die Nummer steckt im roapp-**Standardfeld `phone`** als **ARRAY** (z. B. `['436646185531']`, bereits MSISDN); `email` kommt als **leerer String `''`** (nicht `null`). **(1) `RoappOrderClient.phone: string[]`** ([lib/roapp/client.ts](lib/roapp/client.ts)) ergänzt; defensiv via neuem `asStringArray` geparst (Nicht-Array ⇒ `[]`, Nicht-Strings raus). **(2) `firstClientPhone(client)`** (NEU, exportiert): erste **nicht-leere** (getrimmte) Nummer, sonst `null` — **ROH** gespeichert, **keine** Ländervorwahl-/MSISDN-Normalisierung hier (die passiert erst beim SMS-Versand, [lib/sms/phone.ts](lib/sms/phone.ts)). **(3) E-Mail leer → null:** `parseRoappOrder` normalisiert leere Strings bereits via `asString` zu `null` (`asString('') → null`) ⇒ `client.email = ''` wird als `null` geparst und so gespeichert (kein Code-Zusatz nötig, verifiziert + dokumentiert) — wichtig, damit nicht fälschlich der E-Mail-Kanal greift, wo nur eine Telefonnummer existiert. **(4) Webhook-Mapping** ([app/api/webhook/[secret]/route.ts](app/api/webhook/[secret]/route.ts), `handleOrderCreated`-Insert): `customer_phone: null` → `customer_phone: firstClientPhone(roappOrder.client)`; `customer_email: roappOrder.client?.email ?? null` bleibt (Doppel-Schutz). **(5) Debug-Log entfernt** (`console.log("roapp client raw", …)` aus `parseRoappOrder`). **deliverBooklet-Prüfung bestätigt:** `if (order.customer_email)` ist **truthy/non-empty**, nicht null-basiert ⇒ `''` zählt korrekt als „keine E-Mail" und fällt auf SMS durch (`customer_phone` analog truthy-geprüft) — zusammen mit (3) doppelt abgesichert, **keine** Härtung nötig. `pnpm typecheck` + `pnpm build` grün.

- **Reichweiten-Analyse: Kontaktspalte um Telefon erweitert ✅ (keine Migration, kein `any`, AUTHENTICATED + RLS)** — die Dashboard-Tabelle „Reichweite/VIP-Analyse" zeigte nur die E-Mail; SMS-Kunden (E-Mail leer, Nummer vorhanden) erschienen mit „—". **(1) Reach-Query** ([lib/analytics/reach.ts](lib/analytics/reach.ts)): Order-Select um `customer_phone` erweitert (zusätzlich zu `customer_email`); neues `ReachRow.phone: string | null` + `OrderRow.customer_phone` (kein `any`). **(2) Dashboard-Tabelle** ([app/portal/page.tsx](app/portal/page.tsx)): Kontaktspalte zeigt jetzt **`r.email ?? r.phone ?? "—"`** (E-Mail bevorzugt, sonst Telefon); **EINE** Spalte (bewusst keine neue — die 8-Spalten-Tabelle ist am iPhone ohnehin rechts abgeschnitten), Label `reach.colEmail` „E-Mail" → **„E-Mail/SMS"** ([lib/i18n/de.ts](lib/i18n/de.ts)). **(3) CSV-Export** ([app/portal/analytics/reichweite/export/route.ts](app/portal/analytics/reichweite/export/route.ts)): **separate** „Telefon"-Spalte **zusätzlich** zur bestehenden „E-Mail"-Spalte (`CSV_HEADER` = `Kundenname; E-Mail; Telefon; roapp-Nr.; …`, Body-Reihe um `r.phone` ergänzt) — im CSV ist die Breite egal, getrennte Felder sind besser für die Datenarbeit; **UTF-8-BOM + Semikolon + CRLF** unverändert (österr. Excel). `scripts/import-roapp-orders.ts` **nicht** angefasst. `pnpm typecheck` + `pnpm build` grün.

## Roadmap / Offene Schritte

**Architektur — zwei Bühnen statt eines Desktop-Sequenz-Builders:** Der ursprünglich geplante **Desktop-Sequenz-Builder entfällt**. Stattdessen mobiler Assembler:
- **Pro Auftrag = mobil** (am Werkstück/Tresen): Capture → **Reorder/Löschen (6a ✅)** → **Captions (6b ✅)** → **Abschließen (6c ✅)**. **Mobiler Assembler abgeschlossen.**
- **Pro Betrieb = Desktop** (einmalige Einrichtung): Settings (5a ✅), Logo-Upload (7a ✅), Intro/Outro-Text (7b ✅), Hintergründe/Backgrounds (7c ✅). **Desktop-Business-Config abgeschlossen.**

- **Schritt 7 Desktop-Business-Config ✅:** **7a** Logo-Upload (`branding.logo_url`, privater Bucket) ✅, **7b** Intro/Outro-Text (`settings.intro_tagline`/`outro_message`/`contact_email`/`contact_phone`) ✅, **7c** Intro/Outro-Hintergründe (`branding.intro_bg_url`/`outro_bg_url`, `branding`-Bucket wiederverwendet) ✅. (Lösen die ursprünglich als 5b/5c/5d skizzierten Slices ab.) **Offen/deferred:** der **Per-Item-/Slot-Editor** (renderer-gekoppelt) kommt **nach Step 8**, nicht in Schritt 7.
- **Schritt 8 Generierung — in Arbeit:** **8a-1** Web-Story-**Daten** (Sonnet-Intro + `access_token` + Status `generated`, render-frei) ✅, **8a-1b** Betriebs-KI-Kontext (`settings.ai_context`) ins Intro verdrahtet (Kontext, nicht Anweisung; in Step 9 für Review wiederverwendet) ✅, **8a-2** `/b/[token]`-**Render** (öffentliche Web-Story; Token-als-Trust-Quelle, `service_role`-Reads + frische Signed-URLs, Vollbild-Scroll, full-bleed Medien, Caption-Overlay, Intro/Outro; `displayCaption`-Helfer `lib/booklet/caption.ts` angelegt; Generate setzt `web_story_ready=true`; Portal-Vorschau-Link) ✅. **8b in Arbeit:** **8b-0** FFmpeg-Infra-Spike mit gebündeltem Binary ⚠️ **verworfen** (Binary-Bundling via `outputFileTracingIncludes` sprengt den Vercel-Deploy). **8b-0v2** FFmpeg-**Runtime-Download** ✅ — Binary (gz, ~28 MB) im privaten Supabase-Bucket `assets` (per Script), zur Laufzeit nach `/tmp` geladen + entpackt + gecacht; keine `next.config`-/Dependency-/Migrations-Änderung, Deploy schlank; provisorische `render-reel-test`-Route. **8b-1a** echtes **Foto**-Reel ✅ — FFmpeg-Assembly (Fotos → 9:16, 3 s/Foto, harte Schnitte, mute), **asynchroner `after()`-Job** mit `booklets.reel_status`-Lifecycle (Migration 0004) + Poll, `ensureFfmpeg()` nach `lib/reel/ffmpeg.ts` ausgelagert; ersetzt `render-reel-test`. **8b-1b** Caption-Overlays auf den Foto-Frames ✅ — pro Foto Caption (`displayCaption`) in die Safe-Zone gebrannt, **mitgelieferte** Schrift via `drawtext fontfile=` (kein fontconfig; **nicht** Sharp), statischer Scrim-PNG + Branding-Akzentbalken, mehrzeilig (`textfile`), via `outputFileTracingIncludes` getraced. **8b-1c** Intro/Outro-Frames im Reel ✅ — Intro/Outro (je ~2,5 s, Logo + Branding + Text, Verlauf-Fallback, Vollflächen-Scrim) + optionales Logo-Wasserzeichen pro Foto (`logo_per_page`); Filtergraph-Logik nach `lib/reel/frames.ts` ausgelagert, Assembly mit Per-Frame-Dauer (Intro → Fotos → Outro), keine Migration/UI-Änderung. **8b-2a** Video-Clips ins Reel ✅ — jetzt **alle** `order_media` (Fotos + Clips) nach `sort_order`, Clips auf 9:16 normalisiert (cover, 30 fps, yuv420p, **stumm**, 6 s-Cap) + interleaved; Assembly auf **concat-Demuxer** umgestellt (jedes Item ein formatgleiches mp4-Segment, `-c copy`; `assembleReel`→`concatSegments` + `encodeStillSegment`/`normalizeClip`); Guard `need_photos`→`need_media`; keine Migration/UI-Änderung. **8c** Medien-Anzahl-Limit ✅ — pro-Betrieb Foto-/Video-Anzahl je Auftrag (`settings.photo_max_count` 10 / `video_max_count` 3 default) unter Plattform-Ceiling 20/10; Settings-Felder + Capture-Disable (in-flight-bewusst) + **Server-Guard** (`limit_reached`, harter Riegel), keine Migration. **8b-2b** Clip-Captions/Wasserzeichen ✅ — die Clip-Segmente bekommen dieselbe Overlay-Kette wie die Fotos (Caption + optionales Logo-Wasserzeichen) über den Video-Stream gebrannt; geteilter `buildCaptionOverlay`-Helfer (Foto + Clip, nicht dupliziert), kanonische Form unverändert (concat-Demuxer bleibt heil), `displayCaption`-Quelle, 6.0.1-sichere drawtext-Methode, keine Migration. **8d** Reel-Status in der Auftragsliste ✅ — Amber-Pill „Reel fehlt" (+ rendering/ready/failed) **nur** bei `order.status='generated'`; zweite Query auf `booklets.reel_status` (AUTHENTICATED, RLS), neue `<ReelStatePill>` + `--amber-*`/`--blue-*`/`--red-*`-Tokens; zwei Achsen `order.status` vs. `reel_status`, keine Migration. **Offen:** **8b-3** Ken-Burns. Musik, IG-Handle/Review-URL fließen in Reel/Step 9 ein. Der **Slot-Editor** (pro-Item-Hintergründe/Feinlayout) folgt **nach** Step 8.
- **Schritt 9 Verteilung & Teilen — in Arbeit:** **9a** Share-Sheet auf der Web-Story ✅ — Reel als **Datei** (`navigator.share({files})` → IG/TikTok-Composer, Download-Fallback), Story als **URL**, WhatsApp-Deeplink, Link kopieren; neue Client-Komponente `<ShareBar>` im Outro, Reel server-seitig per `service_role` signiert (nur `reel_status='ready'`), kanonische Story-URL aus Request-Headern, keine Migration. **9b** Review-Entwurf (Sonnet) + IG-Caption ✅ — bei der Generierung erzeugt + im Booklet gespeichert (`review_draft`/`ig_caption`), **öffentliche Seite KI-frei**; `generateReviewDraft` (Ich-Perspektive, konkreter Booklet-Inhalt, `ai_context` als Kontext) + `buildIgCaption` (Template, `@ig_handle` + Hashtags); UI im Outro (`<ShareBar>` erweitert): IG-Caption-Kopieren nahe „Reel teilen", „Google-Bewertung schreiben" (Clipboard + `google_review_url`-Deeplink). **§8.6-PFLICHT eingehalten:** Framing „Vorschlag, in deinen Worten" (nie „einfügen"), **nie** an Belohnung gekoppelt. Review-Generierung NON-FATAL (Web-Story funktioniert ohne); keine Migration. **9d** Teilen-Sektion nur für die Kunden-Sicht ✅ — URL-Marker `?c=1` (geteilte `lib/booklet/customer-view.ts`) schaltet die Teilen-Sektion EIN, **nur** auf dem ausgelieferten Kunden-Link; der nackte geteilte Link zeigt das normale Settings-Outro ohne Teilen-Schicht. **Marker = UI-Schalter, KEIN Auth-Gate** (Token bleibt alleiniger Schutz, kein DB-Zugriff hängt daran), **kein** Share-State/`first_shared_at`; Share-Aktionen teilen die **nackte** URL; Vorschau-Link hängt den Marker an; keine Migration. **9c-1** Ausliefern ✅ — manueller Pfad: `generated → sent` + **Billing-Event** `'booklet_sent'` (via `service_role` — `billing_events` hat kein `authenticated`-INSERT) + **Booklet-E-Mail** (Resend, `lib/email/`; Absender `"{Betrieb}" <hello@valooro.com>`, reply-to = `contact_email`; Link `…/b/${token}?c=1`); E-Mail/`sent_at`/Billing **nicht-blockierend** (sent steht trotzdem, `emailFailed:true`); `<DeliverButton>` (generated, mit bewussten Reel-/E-Mail-Warnungen, kein Block) + `<DeliveredBanner>` (sent, read-only); Env `RESEND_API_KEY`/`BOOKLET_BASE_URL`; keine Migration. **9c-2** QR-Druckansicht ✅ — Bon-taugliche Druckseite (`app/portal/orders/[id]/qr/`, Server Component): server-seitig erzeugter QR (SVG, `qrcode`) kodiert den **`?c=1`-Kunden-Link** (gleiche Basis-URL-/Marker-Logik wie die E-Mail); Guard Status ∈ {generated,sent} + Booklet via RLS ⇒ sonst `notFound()`; Client-`window.print()` + `@media print` blendet Portal-Chrome + Druck-Button aus (nur die Karte druckt); Zugang per „QR drucken"-Link auf der Detailseite; schließt den No-E-Mail-Pfad; keine Migration.
- **Schritt 10 Analytics — in Arbeit:** **10a** Write-Pfad ✅ — öffentlicher Endpoint `POST /api/b/[token]/event` (service_role, Token-als-Vertrauensquelle, `ip_hash` gesalzen) schreibt `booklet_events` (Taxonomie `event_type`/`channel`, geteilt in `lib/booklet/events.ts`) und rückt `orders.status` monoton `sent → viewed → shared` vor; Client feuert fire-and-forget (`lib/booklet/track.ts`): `<ViewTracker>` `viewed` beim Mount (alle Öffner), `share-bar.tsx` je Teilen-Aktion, `<TrackedLink>` `link_click/website`; Env `IP_HASH_SALT`; keine Migration. **10a.1** No-Track-Marker ✅ — Marker `p=1` (geteilt in `lib/booklet/events.ts`, `hasNoTrackMarker`) unterdrückt **zentral** in `trackBookletEvent` (Early-Return vor dem `fetch`) alle Events betriebs-eigener Aufrufe → kein viewed/shared, kein Status-Vorrücken; Betriebs-Links (Portal-Vorschau, Ausgeliefert-Banner) hängen `&p=1` an `c=1` an, Kunden-Links (E-Mail/QR) bleiben nur `c=1` (weiter getrackt); **kein** Auth-Gate, keine Migration. **10b** Dashboard ✅ — die `/portal`-Startseite ist die business-weite Analytics-Übersicht: **Funnel** (3 head-`count`-Queries auf `orders`) + **Engagement** (1 `booklet_events`-Query, JS-Aggregation: Views total/unique, Shares/Klicks je Kanal), alles über den AUTHENTICATED Client (RLS, kein `service_role`); **Headline = Share-Rate** (Geteilt/Ausgeliefert), Zahlen-Karten + CSS-Balken (keine Chart-Library), leerer Zustand, mobil stapelnd; single-fetch + JS-Aggregation bewusst MVP. Plus **Valooro-Logo** im Sidebar-Header (`/public/valooro.png`). i18n `dashboard.*`. **Schritt 10 abgeschlossen.**

## Konventionen / Notes

- **Launch-Fahrplan + Härtungs-Block stehen in [TECH.md](TECH.md) („Launch-Fahrplan & deferierte Härtung")**, Detail-Referenz in [SECURITY_REVIEW.md](SECURITY_REVIEW.md). Sequenz: E2E-Test (Migrationen 0006–0009 angewendet) → Atelier Dax live → **Härtungs-Block** → Flow-Redesign/Tuning. **Der Härtungs-Block (P1 IP-Rate-Limit, P2 `IP_HASH_SALT`-Pflicht, P3 Webhook-Kostenhebel kappen, P4 `item_description` im Prompt fencen) ist PFLICHT, BEVOR ein zweiter, fremder Betrieb onboardet.** HMAC, short_code-Entropie und Register-Enumeration sind bewusst deferiert (akzeptables Restrisiko für Einzelbetrieb).
- `Logo.png` im Repo-Root bleibt dauerhaft **untracked** — nie in Feature-Commits ziehen, nicht nachfragen.
- `HANDWERK_PFLICHTENHEFT.md` bleibt untracked (interne Strategie, public Repo) — nie committen, nicht nachfragen.
- `b.valooro.com` = öffentliche Kunden-Booklets (`/b/[token]`). DNS: CNAME auf `b0a70b96f4f2cb9e.vercel-dns-016.com`. Kein Portal, kein Login.
- `handwerk.valooro.com` = Betriebsportal. DNS: CNAME auf `b0a70b96f4f2cb9e.vercel-dns-016.com`. Supabase Auth: Site URL + Redirect URLs auf `https://handwerk.valooro.com/**` gesetzt.
